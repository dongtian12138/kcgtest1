# 三指工业连接器抓取：研究问题与近年评分基线对比方案

> 状态：主算法冻结前的只读研究方案（2026-08-29）。本文件不宣布新抓取结果，也不冻结或修改主执行轨道的算法、控制器、配置和扰动参数。

## 0. 本轮实际发生了什么

- **已验证事实**：现有无指甲三指手已在 Isaac Sim 中对两款连接器完成名义三指合法接触、离桌、超过 50 mm 抬升和 2 s 保持。这两款 CAD 都参与过方法形成或修改，不能充当盲测型号。
- **本轮没有发生**：没有启动 Isaac、没有运行 CUDA 抓取算法、没有产生新的接触、抬升或扰动结果。
- **本轮产物的证据性质**：这里只给出可反驳的科学问题、文献核查、基线取舍和冻结后的实验协议；文档本身不证明所提方法更准确或更成功。

## 1. 第一性原理决策门

### 1.1 论文真正要观察的结果

在未参与方法修改的新连接器 CAD 上，给定完全相同的三指手、候选集合、控制器、物理参数和有限扰动：

1. 综合评价分数是否比名义静态分数更准确地排序有限条件下的动态成功比例；
2. 按综合评价选出的抓法，是否真的取得更高的动态成功率，而不只是离线分数更高。

“完整指腹接触保持”在本文中指三块指定完整指腹各自形成合法接触片；每块指腹首次建立合法接触后，在预先规定的闭合、抬升和保持阶段按共同时间容差维持接触。它不要求尚未碰到物体的接近阶段已有接触，也不等于整块指腹表面每一点都接触；接近阶段只检查路径和非法碰撞。

### 1.2 可反驳假设

- **预测假设 H-P**：在共同候选上，综合评价与有限条件动态成功比例的逐 CAD Spearman 排序相关，高于名义力闭合分数及名义任务方向/载荷分数。这是唯一预测主指标。
- **选择假设 H-S**：各方法在共同候选中各选一个最高分抓法后，综合评价所选抓法在共同有限扰动集上的配对动态成功率更高。
- **零假设**：上述预测差异或成功率差异为零，或方向相反。若零假设未被数据排除，就不能写“更准确”或“提高成功率”。

H-P 与 H-S 必须分别检验。AUROC 只作预先定义二元真值后的次指标，不能在 Spearman 不支持时用来替换主指标。分数相关性较高不等于它选出的抓法更成功；一次最高分抓法成功也不证明总体预测更准。

### 1.3 事实、未知量和最早证据缺口

| 类别 | 当前内容 |
|---|---|
| 已验证 | 两款开发型号的名义动态抓取成功；当前名义算法只是在冻结有限集合 \(G_\delta\) 内完整枚举的最优解。 |
| 根据证据推断 | 完整指腹、任务载荷、执行和有限扰动可能包含名义静态指标遗漏的失败信息；近年工作分别证明这些因素有研究价值，但尚不能推出本项目的组合分数一定更好。 |
| 尚未验证 | 综合分数的预测增益、选抓成功率增益、最坏扰动表现、跨未见连接器型号一致性，以及任何实物有效性。 |
| 最早缺口 | 没有在**同一候选、同一控制器、同一动态评价**下，把综合分数与名义分数对照；现有两款成功不能回答评分优劣。 |

最短因果链是：共同合法候选 → 只替换离线评分 → 各评分排序/选抓 → 同一控制器执行 → 同一有限扰动 → 三指接触、离桌、50 mm、2 s 及滑移/力矩观测 → 比较预测与选择结果。

当前最小行动只是预先锁定这个协议。主执行者明确宣布算法和控制器冻结并给出最终配置后，才在独立 Git worktree 和独立结果目录中一次实现一个评分基线。不同结果直接改变结论：

- 两项假设都支持：可以在有限域、指定手/CAD/控制器/扰动下声称“预测更准且所选抓法成功率更高”；
- 只支持 H-P：只能声称预测排序改善，不能声称提高实际成功率；
- 只支持 H-S：只能报告选抓结果改善，不能声称评分总体更准确；
- 都不支持或方向相反：核心优越性主张被否定，应报告负结果，不扩张搜索或补建框架来挽救结论。

## 2. 一手资料核查范围

必审论文均阅读全文和论文内附录，并核对作者项目页、补充视频及官方仓库；没有独立补充文件或完整代码时明确记为缺失，不用项目页摘要替代论文。

| 方法 | 一手资料和版本核查 | 代码可复现边界 |
|---|---|---|
| **FRoGGeR**，IROS 2023 | [论文/附录](https://arxiv.org/html/2302.13687)、[项目页](https://alberthli.github.io/frogger/)、[官方仓库](https://github.com/alberthli/frogger)、[论文实验分支](https://github.com/alberthli/frogger/tree/iros-release) | 论文分支是原实验版本；主分支后来重构。仓库 `LICENSE` 为 MIT，但包元数据曾标 Apache-2.0，复用前需按许可证文件并澄清冲突。 |
| **TaskDexGrasp**，IROS 2024 | [论文 PDF（含附录）](https://arxiv.org/pdf/2309.13586)、[项目页](https://pku-epic.github.io/TaskDexGrasp/)、[补充视频](https://pku-epic.github.io/TaskDexGrasp/videos/full_video.mp4)、[官方仓库](https://github.com/JYChen18/TaskDexGrasp) | 仓库只有 GWS 边界/任务损失的最小示例，没有完整手姿生成、碰撞、轨迹或评价管线；未见 `LICENSE`，不能直接复制并称为有许可的官方复现。 |
| **SpringGrasp**，RSS 2024 | [RSS 正式论文](https://www.roboticsproceedings.org/rss20/p042.html)、[项目页](https://tml.stanford.edu/SpringGrasp/)、[官方仓库](https://github.com/Stanford-TML/SpringGrasp_release) | 发布的是 Allegro/KUKA、GPIS 和阻抗控制相关实现；未见明确仓库许可证。其控制律是方法的一部分，不能在“共同控制器”实验中悄悄替换。 |
| **BODex**，ICRA 2025 | [论文全文（未发现独立附录）](https://arxiv.org/html/2412.16490)、[项目页](https://pku-epic.github.io/BODex/)、[官方仓库](https://github.com/JYChen18/BODex) | 完整批量优化管线公开，依赖 PyTorch/CUDA、cuRobo 和手/网格预处理；代码标为 CC BY-NC 4.0。新增三指手仍需建立接触球、运动学和碰撞配置。 |
| **Grasp to Act**，RA-L 2026 | [论文全文](https://arxiv.org/html/2602.20466)、[官方项目页/视频](https://grasp2act.github.io/) | 项目页当前提供论文和视频，未见官方代码链接；其 Isaac 候选动态评分和任务专用强化学习控制器不可拆成一个现成的共同候选评分函数。 |

另外核查了与创新边界最接近的工作：

- [PONG / probabilistic force closure](https://arxiv.org/abs/2309.16930) 已研究接触法向不确定性下的概率力闭合，因此本文不能声称首次把“不确定性”用于解析抓取评价。
- [Get a Grip](https://proceedings.mlr.press/v270/lum25b.html) 用数百万正负动态样本训练多指抓取评价器，并在仿真扰动和实物上验证选择能力，因此本文不能声称首次用动态失败数据改善抓取评价。其数据规模、视觉输入和学习范式与本项目不匹配，不作为直接基线。
- **Grasp to Act** 的结构化差异列于下表；它界定了“动态扰动评分”不是本文可独占的新概念。

## 3. 文献物理对比

| 方法 | 研究问题 | 手模型 | 接触模型 | 目标函数 | 不确定性 | 仿真/实物 | 开放代码 | 能否公平适配本项目 |
|---|---|---|---|---|---|---|---|---|
| **FRoGGeR** | 用可微 min-weight 指标快速合成运动学可行、无碰撞的强力闭合精密抓法 | 论文为 Franka + 四指 Allegro；当前代码另有三指 Barrett 表达 | 每指一个硬点摩擦接触，多棱锥摩擦锥；无接触面积和独立扭转力矩 | 最大化构成零合力/力矩凸组合中的最小权重；是任务无关的名义静态力闭合代理 | 假设几何和位姿精确；动态正弦扰动只用于事后评价 | Drake 仿真；无论文实物结果 | 完整仓库；论文分支可读；`LICENSE` 为 MIT、包元数据有 Apache-2.0 冲突 | **高（评分级）**：在共同 \(G_\delta\) 上用相同三个合力点重排。完整连续生成器会混入搜索域、初值、Drake 几何和控制器差异，不公平。 |
| **TaskDexGrasp** | 给定任务所需的力和力矩方向，合成对应的静态灵巧手姿 | 展示 Shadow、LEAP、Allegro；数学接触数不限定，未验证三指手 | 主实验为点摩擦接触；附录给软指扩展；每接触法向力归一化上限 | 用 GWS 支撑边界与任务载荷扇区的方向一致性评分，另加接触距离和碰撞项 | 任务方向半角不是位姿、摩擦、质量、质心或执行误差的鲁棒集合 | Isaac Gym 固定手姿抗载荷；四个实物任务各 10 次 | 仅 235 行评分最小示例；无完整生成管线，未见 `LICENSE` | **高（评分级）**：代表名义任务方向对照。需按论文独立重实现，不能称官方完整管线复现。 |
| **SpringGrasp** | 从不完整点云和形状不确定性下，同时规划预抓姿态和柔顺抓取控制 | KUKA + 四指 Allegro | 点式指尖，虚拟弹簧连接预抓目标与预计平衡接触；推导假设接触持续粘着、无滑移 | 共同优化预抓、每指笛卡尔刚度及形状/碰撞/接触等加权能量 | GPIS 表示感知形状不确定性 | 仿真与 14 个日常物体实物实验 | 官方仓库；入口和配置多处四指硬编码，未见 `LICENSE` | **低**：方法贡献依赖专用阻抗控制器和部分点云不确定性；换成项目冻结控制器就删掉核心，保留它又破坏共同控制器。 |
| **BODex** | 用双层优化和可微 QP 大规模生成静态灵巧手抓法 | Shadow、Allegro、LEAP；可增加自定义手但需预处理 | 指定指尖接触球的点摩擦接触，八边摩擦锥；无完整指腹面积 | 下层 QP 拟合目标载荷，上层优化手姿、接触和碰撞；官方设置只有六个正负平移纯力，非零目标力矩代码未实现 | 没有有限位姿、摩擦、质量、质心或执行扰动鲁棒目标 | 解析优化器主评价为 MuJoCo；论文实物结果来自用 BODex 数据训练的学习模型，不是优化器直接执行 | 完整仓库，CC BY-NC 4.0；依赖 CUDA 12.1、修改版 cuRobo 和手型预处理 | **中高（物理/代码）**：CAD、点接触和预抓/挤压接口接近本项目，但完整移植需 CUDA、cuRobo 和新三指手配置，且会改变搜索器。 |
| **Grasp to Act** | 为动态工具使用选择抗任务载荷抓法，并在执行中在线修正滑移 | UR5 + 16-DoF LEAP；依赖人类示范给功能抓取区和任务轨迹 | Isaac Lab 中由 PD 闭指形成包络接触，允许手掌/多连杆接触；不是三点解析接触 | 每候选沿 12 个正负力/力矩轴递增施扰，以稳定时间平均评分；随后用任务专用残差强化学习控制 | 强化学习时对质量、摩擦和外力系数做 \(\pm30\%\) 随机化 | Isaac Lab；五项工具任务各 10 次实物，另测三个锤子变体 | 项目页提供论文/视频，未见官方实现 | **低（直接基线）/高（创新边界）**：动态评分思想最接近，但人类示范、包络接触、Isaac rollout 和专用在线控制器都会改变核心变量，不能放进同控制器评分实验。 |

### 3.1 SpringGrasp 与 BODex 的二选一

选择 **BODex** 作为与当前三指手更兼容的候选，理由不是论文排名，而是物理接口：它从对象网格和指定指尖接触区域出发，使用点摩擦接触和静态预抓/挤压姿态，能够把本项目三个合法指腹合力点映射进去；自定义手虽有工作量，但不要求把在线控制律换成其论文贡献的一部分。

SpringGrasp 的“虚拟弹簧”既是评价也是控制方法：它共同优化预抓目标和每指阻抗。若统一使用本项目控制器，SpringGrasp 不再是原方法；若保留其控制器，动态差异无法归因于抓法评价。它还针对部分点云的形状不确定性，而本项目当前问题是已知 CAD 下的有限物理和执行扰动。因此本研究不强行移植 SpringGrasp。

BODex 只作为兼容性选择和近年强方法背景，**不进入当前最小的两个直接评分基线**：其完整生成器会同时改变连续搜索、运动学、碰撞和 CUDA 管线；官方代码可忠实执行的六个纯力方向又与 FRoGGeR 的名义静态对照重叠，而非零任务力矩尚未实现。TaskDexGrasp 评分提供了不同的名义任务方向对照。若初始对比已经回答 H-P/H-S，增加 BODex 不会消除最早证据缺口。

## 4. 冻结后最多两个直接外部基线

### 基线 A：FRoGGeR-derived min-weight 评分

- 输入：共同合法 \(G_\delta\) 中每个候选的同一三个合力作用点、法向、对象质心坐标系、\(\mu\) 和八边摩擦锥。
- 变化量：只用论文的归一化 min-weight 连续分数替换排序。
- 不变化：完整指腹合法性、路径/碰撞、单指力、关节力矩、控制器和 Isaac 评价仍是共同约束。
- 准确命名：**FRoGGeR-derived min-weight score baseline**，不是完整 FRoGGeR 生成器或论文数值复现。

FRoGGeR 论文的经验阈值不能当成物理常数；主实验只用连续排序。力与力矩混合还依赖原点和长度尺度，因此只比较同一 CAD 内排序，不直接比较不同 CAD 的原始分值。

### 基线 B：TaskDexGrasp-inspired 名义任务方向评分

- 输入：同一合法候选和三个点摩擦接触，使用预先登记的任务力/力矩方向。
- 变化量：只用论文式 GWS/TWS 名义任务方向评分替换排序。
- 共同限制：统一 \(\mu\)、每指 8 N、关节 0.90 N·m、完整指腹和碰撞约束；这些是公平实验的共同可行性边界，不伪称为原论文目标。
- 准确命名：**TaskDexGrasp objective，独立重实现**。该目标主要评价任务方向匹配，不能写成绝对载荷裕度；项目自身冻结的名义任务 LP 才是载荷能力对照。

官方最小代码的归一化与论文有差异，而且示例把力矩采样置零。进入盲测前必须一次性锁定：任务方向的正负号和幅值、TWS 半角、多个方向的聚合规则、力矩的特征长度缩放、GWS 边界采样数及随机种子；这些值不能逐 CAD 手调。独立实现还需先对论文公式和官方最小示例做一次约定范围内的一致性检查：若不一致就先修正实现，若一致才运行盲测；该检查只证明评分实现性质，不证明动态抓取。

这两个外部基线分别代表名义力闭合和名义任务方向评价。项目冻结后的自身名义任务 LP 作为内部载荷裕度对照，但不计入两个外部基线。

## 5. 公平对比协议

### 5.1 冻结和信息隔离

主执行者必须先书面宣布算法、控制器和最终配置冻结，并给出可定位版本。随后：

1. 在独立 Git worktree 和独立结果目录中一次实现一个基线；
2. 先冻结所有方法的公式、归一化、平局规则、候选预算和评价判据，再查看盲测 CAD 的动态结果；
3. 不按单个连接器修改任务方向、初姿、阈值或权重；
4. 基线和综合方法都不得把 Isaac 物体真值或运行后评价量送入在线控制；
5. 盲测候选的离线排序不得读取该候选的 Isaac 动态成败、滑移或姿态真值；若综合方法必须用 Isaac rollout 才能评分，它就不再是“离线预测”，必须另立仿真优化预算，不能沿用本文结论；
6. 若最终冻结配置与本文件列出的当前参数不同，以冻结配置为共同输入并在论文中如实记录，不反向修改历史结果。

### 5.2 共同输入、预算和执行

| 必须相同 | 协议 |
|---|---|
| CAD 与手 | 完全相同的连接器 CAD、无指甲三指手几何、关节映射和指定三块指腹。 |
| 合法候选 | 由冻结后的、与方法无关的几何/运动学/安全硬约束一次生成 \(G_\delta\)；不得先用综合鲁棒分数或扰动结果剪枝。所有评分读取完全相同的候选及接触点/法向，不各自删除失败样本。 |
| 搜索预算 | 主比较对整个共同有限集合各评分一次；若算力迫使子采样，则在看分数前固定同一候选索引和相同数量。只允许声称该有限域内的枚举最优。 |
| 接触与载荷 | 同一对象质心坐标、长度单位、摩擦锥离散、摩擦系数、每指法向力上限、关节力矩上限和任务力/力矩。 |
| 控制器与路径 | 同一预抓、闭合、有限夹持、抬升轨迹和冻结控制器；方法只能提供候选排序/所选抓法。 |
| Isaac 评价 | 同一 Isaac 版本、物理步长、材料、质量/惯量、初态、种子、持续时间和成功判据；方法运行顺序随机化，条件成对。 |
| 运行时间 | 在同一硬件分别报告一次性预处理、全候选评分、选抓总时间；Isaac 评价时间不混入离线评分。失败、超时和预处理均不能从分母消失。 |

这是一项**评分对比**，不是三篇论文完整生成器的排行榜。这样才能把动态差异归因到评价信息，而不是不同手模型、连续优化器或控制器。

所有方法共同使用的完整指腹合法性和执行安全约束只定义“哪些抓法允许参赛”。论文必须另列每种方法真正参与连续排序的信息；一个只作为共同硬约束的因素，不能再被写成综合评分增益的独立原因。若要声称某个单项因素有因果贡献，需要另做冻结消融，本最小实验不提供该证据。

冻结声明后、盲测前必须填实以下“评分读取信息”表，不能保留概念名称：

| 评分 | 真正进入连续排序的信息 |
|---|---|
| FRoGGeR-derived | 三个名义合力点/法向、统一摩擦锥和对象坐标尺度下的 min-weight。 |
| TaskDexGrasp-inspired | 同一名义接触，加预登记的任务方向、半角、聚合与力矩尺度。 |
| 项目内部名义 LP | 冻结实现实际使用的名义任务力/力矩、单指力和关节力矩裕度。 |
| 综合评分 | **冻结后逐字段列出实际读入量及聚合公式**；不得把只用于共同参赛资格的指腹/路径约束填入本栏。 |

如果冻结后的综合评分并未用完整指腹保持量或执行过程量参与排序，论文问题必须同步收窄为“有限扰动评分是否优于名义评分”；不能继续声称四类信息联合评价。反之，即使四类信息都进入综合分数，本最小实验也只检验组合的增量价值，不识别各单项贡献。

### 5.3 共同有限扰动

冻结后复制主执行轨道的最终扰动集合，不在此先行改值。当前上下文中的待冻结研究假设为：横向位置 \(\pm0.17\) mm、轴向角 \(\pm2^\circ\)、质量 \(\pm5\%\)、质心 x 方向 \(\pm1\) mm、第三指目标 \(\pm0.004\) rad；摩擦仿真合同为 \([0.45,0.55]\)，但没有实物标定。

- 每种方法在完全相同的有限条件及相同种子下运行；不为失败方法另加重试。
- 单因素曲线一次只改变一个扰动，其他量保持名义；最坏工况使用预先登记的有限联合集合。
- 扰动重复是同一 CAD 内的条件，不是新的独立型号，不能扩大“跨型号”样本量。
- 结果只证明这组离散扰动上的表现，不外推到集合外或连续最坏情况。

### 5.4 动态观测和成功判据

每次运行先保存原始时间序列，再据冻结判据评价。成功必须同时满足：

1. 三块指定指腹建立并保持合法手—连接器接触；
2. 无错误手部表面/桌面接触、无不可接受穿透；
3. 连接器离桌并净抬升至少 50 mm；
4. 在目标高度保持至少 2 s 且未掉落；
5. 滑移、姿态变化、单指力和关节力矩不越过冻结安全边界。

程序退出、结果文件生成或离线分数可计算都不算动态成功。若冻结控制面尚未给出某个接触时序或穿透容差，就在首次盲测前补充一个共同数值，不能看结果后调整。

### 5.5 “预测更准”和“选得更好”的统计单位

**预测实验**：在查看任何方法分数前，从共同合法候选中确定一个覆盖掌位、轴向位置和绕轴姿态的固定候选面板；所有评分都给这些候选打分，每个候选都经过同一扰动评价。每个 CAD 分别报告：

- 主指标：分数与候选有限条件动态成功比例的 Spearman 排序相关；H-P 只按这个预登记主指标判断；
- 次指标：把“全部预登记必测条件均成功”定义为 1、否则为 0 后的 AUROC；若某个 CAD 没有正例或负例，AUROC 记为未定义，不补造数值；
- 若分数不是概率，不报告似是而非的概率校准。

**选择实验**：每种评分按预先登记的平局规则选 top-1，比较它们在共同扰动集合上的成功比例。报告逐 CAD 配对差、最坏条件和失败阶段，不只报汇总平均。

CAD 是跨型号结论的分析单位；同一 CAD 的候选、扰动和仿真种子不能当成大量独立样本。相关性和 AUROC 先逐 CAD 计算，再用 CAD 级配对差和区间汇总。候选面板大小由冻结后的单次运行成本决定并在盲测前写死，所有方法相同。

## 6. 最小结果图与表

只需要一张 2×3 主图和一张逐 CAD 数值表，不为每篇论文建设独立绘图管线：

| 面板 | 必须展示的实际量 | 回答的问题 |
|---|---|---|
| A | 各方法动态成功率随单一归一化扰动强度的曲线，逐 CAD 淡线、汇总粗线及 CAD 级区间 | 综合评价是否在扰动增大时更慢失效？ |
| B | 方法 × CAD 的预登记有限集合最坏成功率/最早失败阶段热图 | 平均值是否掩盖某个连接器或最坏工况？ |
| C | 成功与未掉落运行中的最大平移滑移和姿态变化分布；掉落单列，不能删掉 | “成功”是否伴随明显手内位姿漂移？ |
| D | 每个关节峰值力矩相对 0.90 N·m 上限，标出最坏关节和越限次数 | 改善是否靠更高或不安全的关节负载换来？ |
| E | 同硬件的全候选评分时间与含预处理的选抓总时间；失败/超时计入 | 预测增益的计算代价是什么？ |
| F | 离线分数对动态成功比例散点/秩图，附逐 CAD Spearman、AUROC 和配对区间 | 离线评分是否真的预测动态结果？ |

逐 CAD 表至少列出 top-1 抓法、名义成功、有限扰动成功比例、最坏条件、最大滑移/转角、峰值关节力矩和运行时间。图片、视频或轨迹只用于解释最早失败点，不替代数值。

## 7. 至少需要多少个未参与方法修改的 CAD

**最低需要 6 个全新、在方法和阈值冻结前未查看动态结果的连接器 CAD**，并使它们覆盖至少 3 类有物理差异的几何构型，每类至少 2 个型号。现有两款开发 CAD 不计入这 6 个。

这个下限有明确但很窄的统计含义：若把 CAD 视为相互独立且可交换的型号，对逐 CAD 配对改善方向做双侧精确符号检验，5 个 CAD 即使全部同向，最小 \(p=0.0625\)；6 个全部同向时才到 \(p=0.03125\)。因此 6 只是在该假设和最理想结果下检验“所测型号改善方向一致”的机械下限，不是充分的普遍泛化样本量。刻意覆盖不同几何构型有助于物理覆盖，但不等于从工业连接器总体随机抽样；相应 \(p\) 值不能被包装成总体代表性。若出现平局或任一反向结果，6 个就不足以得到该检验结论，应依据冻结后的效应和方差扩大样本，而不是改方法。

几何构型必须按会改变抓取物理的因素预先登记，例如外壳直径/长径比、法兰或螺纹凸起、可用指腹带宽和质心偏置；不能仅用厂商名凑类别。即使 6 个全部支持，也只能说“在这 6 个未见型号上观察到一致方向”，并描述它们覆盖了哪些构型；每类两个型号不能支持整个家族外推。若任何盲测结果被用来修改方法，该 CAD 立即转为开发型号，重新冻结后必须另取未见 CAD 补足 6 个。

不存在一个不依赖效应大小、方差、抽样框和型号独立性的固定数量，能够保证跨家族推广。若资源允许，10–12 个真正独立的盲测 CAD 会给效应大小更可信的区间，但抽样独立性比单纯增加相近变体更重要；这不改变当前“最低盲测面板为 6”的回答。

## 8. 可以声称与不能声称

### 数据支持后可以声称

- 在冻结的有限 \(G_\delta\)、指定无指甲三指手、所测连接器 CAD、共同控制器、Isaac 版本和已声明有限扰动集内，综合评价的逐 CAD 预测指标更高——前提是 H-P 的实际数据支持。
- 在完全相同的候选预算和动态协议下，综合评价所选 top-1 抓法的有限扰动成功率更高——前提是 H-S 的实际数据支持。
- 对 \(G_\delta\) 的完整枚举只证明该**有限集合内**按各自目标的最优；可以给出集合大小、步长和边界以保证可复核。
- 若盲测 CAD 达到预登记判据，可以说“在所测未见型号上观察到一致改善”；必须同时报告反例、区间和型号构型。不能据两个相近型号外推整个家族。

### 不能声称

- 不能写连续姿态空间、任意接触模式或通用抓法的“全局最优”。
- 不能把两款开发型号，或最低 6 款盲测型号，写成所有工业电连接器的普遍泛化。
- 不能把 Isaac 结果写成真实硬件、实物摩擦标定或制造公差认证。
- 不能声称首次考虑任务载荷、不确定性、执行过程或动态扰动；TaskDexGrasp、PONG、SpringGrasp、Get a Grip 和 Grasp to Act 已分别覆盖这些部分。
- 不能把评分级适配写成完整 FRoGGeR/TaskDexGrasp 官方管线复现，也不能用原论文成功率与本项目数值直接排名。
- 不做冻结的逐因素消融时，只能评价“组合方法”，不能把增益分别因果归功于完整指腹、任务力/力矩、执行或某一扰动。
- 不论程序退出、静态检查或绘图是否成功，都不能在没有真实动态数值时写 `DYNAMIC_PASS` 或“提高成功率”。

## 9. 创新性边界和论文定位

现有工作已经分别覆盖：名义力闭合的快速连续指标（FRoGGeR）、名义任务方向评价（TaskDexGrasp）、形状不确定性与柔顺接触建立（SpringGrasp）、可微 QP 大规模抓法合成（BODex）、概率力闭合（PONG）、动态数据学习评价（Get a Grip）以及动态任务扰动加在线适应（Grasp to Act）。

本轮检索**尚未证明新的评分算法创新**。尤其 Grasp to Act 已把 Isaac 中的 12 轴动态施扰、候选稳定时间评分、执行过程、在线适应以及质量/摩擦随机化放在同一系统中；盲测数值只能证明本项目方法是否有效，不能把已有思想变成新思想。

因此当前论文应定位为：**工业电连接器三指抓取上的受控增量实证研究**。可检验的价值在于使用共同有限候选、冻结控制器和同一 Isaac 合同，隔离完整指腹合法接触、有限任务载荷/关节限制、执行信息及明确有限物理扰动相对于名义评分的增量预测价值。完整指腹和工业连接器场景本身最多构成领域化贡献，不自动构成算法创新。

只有冻结后的综合评分相对 Grasp to Act 等先行工作具有明确不同的新数学形式、真正新增的物理信息或可证明性质，并经更完整检索核实后，才讨论方法创新；否则标题、摘要和结论都应保持“受控实证/评价协议”的定位。

## 10. 冻结后的唯一实施顺序

1. 接收主执行者的冻结声明和最终配置；在任何盲测动态结果出现前，一次锁定两项外部评分、内部名义 LP、综合评分、候选面板、统计主指标和全部判据；不从本文件反向改主轨道。
2. 在独立 worktree 中只实现 FRoGGeR-derived 评分，先产生共同候选上的离线分数和真实动态数值/图；最早失败就停在该物理阶段。
3. 在同一冻结接口上只实现已经预先锁定的 TaskDexGrasp-inspired 评分，产生同样的真实数值/图；不得根据第 2 步结果改公式或参数。
4. 最后才计算 H-P/H-S 的配对对比和写结论；若结果不支持，保留负结果，不增加第三基线、批量盲扫或新框架。

## 11. 只读反方审稿记录

一个独立反方角色在未改文件、未运行 Isaac/CUDA 的条件下，专门检查了创新性、比较公平性和结论强度。它提出的主要反例及本文修正如下：

| 反方问题 | 本文处理 |
|---|---|
| Grasp to Act 已覆盖动态施扰评分和执行适应，盲测成功不能自动构成算法创新 | 将其加入结构化对比，并把论文定位收窄为受控增量实证；算法创新保留为尚未证明。 |
| 共同硬约束可能被重复包装成综合评分贡献 | 增加冻结后的“评分读取信息”表；未进入连续排序的量不能归因。 |
| TaskDexGrasp 方向、半角、聚合、尺度和采样未唯一确定 | 要求所有定义在盲测前锁定，并只称独立重实现的任务方向评分。 |
| 动态成功比例不是 AUROC 的二元标签 | Spearman 定为唯一预测主指标；AUROC 只用于“全部必测条件均通过”的预定义二元标签。 |
| 接近阶段尚未接触，原“持续接触”定义不成立 | 改为首次合法接触后，在闭合、抬升和保持阶段维持合法接触片。 |
| 6 个 CAD 和每类两个型号不能证明家族泛化 | 改称最低盲测面板，只允许报告所测未见型号；明确不存在保证跨家族推广的固定数量。 |

反方审稿后仍未解决、必须等待冻结配置的事实有两个：综合评分究竟有哪些量真正进入排序，以及 TaskDexGrasp 独立实现的最终任务方向定义。在这两项填实前，不得启动盲测或写创新/优越性结论。

用户当前不需要操作。下一次外部依赖是主执行者明确宣布算法与控制器冻结，并提供最终配置和独立结果路径。

## 12. 当前鲁棒算法的数学与物理审计（未冻结工作树快照，2026-08-29）

> 本节追加于主算法仍在开发期间，只描述当前工作树和已落盘证据。它不把当前实现写成最终方法，
> 不宣布新的动态结果，也不修改前文冻结后才允许执行外部基线的边界。本节所称“当前鲁棒算法”
> 特指显式使用 `--selection-mode robust` 时的代码路径；同一文件默认的 `nominal` 路径排序不同。

### 12.1 第一性原理问题与当前证据边界

这次审计只回答一个最早缺口：当前离线选择式中是否存在能够约束抬升姿态变化或对象相对手转动的
数学量。若不存在，就不能用已有静态分数事后解释动态姿态差异。

当前最新动态事实经原始 JSON 核对如下：

| 现有运行 | 抓法来源 | 抬升 | 保持 | 世界姿态变化 | 对象中心相对手基座位移 | 最大手指实测力矩 | 手基座峰值加速度 |
|---|---|---:|---:|---:|---:|---:|---:|
| TE | V5 鲁棒选择 | 55.549971 mm | 2.000 s | 0.2949° | 0.394565 mm | 0.621856 N·m | 0.016801 m/s²，通过门限 |
| 当前公开型号 | V4 名义选择 | 55.557858 mm | 2.000 s | 3.7677° | 0.461386 mm | 0.477620 N·m | 0.025962 m/s²，未通过门限 |

两次运行使用相同的控制器源码、`preload_increment=0.075 rad` 和运行时
`lift damping=200`，但**抓法来源并不相同**：TE 使用已完成的 V5 鲁棒结果，当前公开型号仍使用
V4 名义结果。当前公开型号的 V5 鲁棒搜索在审计时仍在运行，目标目录尚无
`search_result.json`。所以现有差异能证明“抬起不等于姿态稳定”，却不能证明 V5 鲁棒排序在两个
型号间失效，也不能把差异单独归因于评分或控制器。

动态字段本身还有三条不可省略的边界：

1. `maximum_orientation_change_rad` 是对象参考部件世界四元数相对抓取基线的变化，
   不是对象相对手基座的转角；见 `evaluate_run.py:699-714`。它可能同时包含手臂姿态跟踪和
   抓持内转动。两次运行省略了完整轨迹，现有聚合 JSON 不能把两者分开。
2. `lift_acceleration_consistent` 对手基座世界 z 位置做有限差分，不是连接器的加速度或姿态指标；
   判据为
   \[
   a_{H,z}^{\max}\le 0.0008316403+0.020=0.0208316403\ \mathrm{m/s^2},
   \]
   见 `evaluate_run.py:807-825`。当前公开型号的接触、离桌、50 mm、2 s 和安全项均通过，
   名义物理判据仅由这个手基座加速度门否决；不能把该 `false` 写成抓法评分失败。
3. 离线 LP 的默认摩擦系数是对象合同下界 \(\mu=0.45\)，而两次名义动态 JSON 记录三块指腹
   材料的静/动摩擦均为 1.4、没有显式摩擦覆写；材料合同还声明 `frictionCombineMode=max`。
   因而这两次名义动态不能写成与离线 \(\mu=0.45\) 完全匹配的物理验证。

### 12.2 候选集合不是连续抓法空间

一个候选只有三个自由索引：共享掌关节角、沿连接器轴线的 CAD 截面，以及绕该轴的手部转角：

\[
g=(p_i,\psi_j,z_k)\in G_\delta=P\times\Psi\times Z_o,
\qquad \Delta x_{\rm task}=\Delta y_{\rm task}=0,
\qquad \alpha_{\rm tilt}=\beta_{\rm tilt}=0.
\]

掌关节网格为

\[
P=\operatorname{unique}\!\left(
\{0.7+0.1n\mid q_{\min}\le0.7+0.1n\le q_{\max}\}
\cup\{q_{\min},q_{\max}\}\right),
\]

当前手模型实际得到 17 点 \(0,0.1,\ldots,1.5,1.57\) rad。绕轴转角为

\[
\Psi=\left\{\operatorname{mod}\!\left(j\frac{\pi}{12},2\pi\right)
\mid j=0,\ldots,23\right\}.
\]

令允许接触 CAD 面顶点在任务坐标系中的轴向范围为
\([z_{\min}^{A},z_{\max}^{A}]\)，对象质心轴向坐标为 \(z_{\rm COM}\)，则

\[
Z_o=\operatorname{unique}\!\left(
\{z_{\rm COM}+0.001k\mid z_{\min}^{A}\le z_{\rm COM}+0.001k\le z_{\max}^{A}\}
\cup\{z_{\min}^{A},z_{\max}^{A}\}\right).
\]

因此当前公开型号有 \(17\times24\times34=13{,}872\) 个声明成员，TE 有
\(17\times24\times33=13{,}464\) 个。实现见 `finite_cad_search.py:226-346` 和
`carts_grasp_v2.yaml:68-94`。

对每个 \((p_i,z_k)\)，生成器在该 CAD 截面发出 72 条径向射线，每条取最外正向交点，再取

\[
R(z_k)=\operatorname{median}_{\ell=1}^{72}
\left(\max_{h\in\operatorname{hit}(\ell,z_k)}\rho_h\right).
\]

对三块完整指腹分别求“指腹内向面第一次到达半径 \(R(z_k)\)”的关节根；预抓根使用
\(R(z_k)+7\) mm，固定闭合端点是在首次接触根后再闭合至多 0.23 rad。最后施加
\(\psi_j\)，并只为 1 mm 预抓手—桌间隙做世界 z 平移。见
`task_grip_surface.py:402-740` 和 `carts_grasp_v2.yaml:40-51`。

所以 \(G_\delta\) 不是任意三接触点、连续 SE(3) 或横移/倾斜抓法集合；任何“最优”都只能指
这个对象依赖的有限轴对齐集合。

### 12.3 每个候选实际计算的接触、力和力矩

#### 12.3.1 离散接近与顺序闭合

接近路径取 51 个离散手位姿。三指按 1、2、3 的固定顺序闭合，单步最大关节增量为

\[
\Delta q_{\max}=v_{\rm finger}\Delta t
=0.18\times\frac1{240}=0.00075\ \mathrm{rad}.
\]

接近阶段任何指腹—对象接触都被拒绝；顺序闭合时每指必须先碰到允许 CAD 面；第三指接触后，
三块指腹必须同时仍有允许接触。路径沿途只做离散硬碰撞判定，不做连续时间碰撞证明，也不做
柔顺接触动力学。

#### 12.3.2 完整指腹在几何上保留，在受力上压成三个点

第 \(i\) 块完整指腹的 FCL 碰撞见证集合记为

\[
\mathcal P_i=\{(x_{ih},n_{ih},d_{ih},f_{ih})\}_{h=1}^{N_i},
\]

其中包括见证位置、对象外法向、穿透深度和对象面编号。进入受力模型的只有一个等权合力点和
一个平均法向：

\[
r_i=\frac1{N_i}\sum_{h=1}^{N_i}x_{ih},
\qquad
n_i=\frac{\sum_h n_{ih}}{\left\|\sum_h n_{ih}\right\|}.
\]

穿透深度、接触斑面积、压力分布和压力中心迁移不进入约束或排序；每块指腹也不允许独立扭转
力矩。因此“完整指腹”准确地说是完整网格的碰撞与合法表面检查，不是有限面接触力矩模型。
实现见 `finite_cad_search.py:820-850,1044-1062`。

#### 12.3.3 八边摩擦锥、对象合力/力矩和手关节力矩

给定对象外法向 \(n_i\) 及其切向正交基 \(t_{i1},t_{i2}\)，第 \(e\) 条摩擦锥边为

\[
d_{ie}=-n_i+\mu\left[
\cos\left(\frac{2\pi e}{8}\right)t_{i1}
+\sin\left(\frac{2\pi e}{8}\right)t_{i2}
\right],\qquad e=0,\ldots,7.
\]

以对象质心 \(c\) 为力矩原点，对象 wrench 列和独立手关节的静态等效力矩列分别为

\[
w_{ie}=\begin{bmatrix}d_{ie}\\(r_i-c)\times d_{ie}\end{bmatrix},
\qquad
t_{ie}=J_i^\top(-d_{ie}).
\]

把全部列组成 \(W=[w_{ie}]\in\mathbb R^{6\times24}\) 与
\(T=[t_{ie}]\)。这里的关节力矩只覆盖四个独立手关节，不覆盖七个机械臂关节。

#### 12.3.4 保持/抬升任务 LP 的精确形式

代码从命令抬升距离 \(D=0.056\) m 和时长 \(T_L=19\) s 计算

\[
a_{\rm LP}=\frac{10}{\sqrt3}\frac{D}{T_L^2}
\approx 8.95613\times10^{-4}\ \mathrm{m/s^2}.
\]

注意它与评价器登记的 \(8.31640\times10^{-4}\ \mathrm{m/s^2}\) 不是同一个数。
令 \(R_{OW}\) 把世界向量旋转到对象坐标，则

\[
F_{\rm hold}=mR_{OW}(-g_W),
\]

\[
F_{\rm lift}=mR_{OW}
\left(\begin{bmatrix}0\\0\\a_{\rm LP}\end{bmatrix}-g_W\right).
\]

对阶段 \(s\in\{\mathrm{hold},\mathrm{lift}\}\)，代码解下列线性规划：

\[
\begin{aligned}
\lambda_s(g,u)=\max_{\alpha,\lambda}\quad &\lambda\\
\mathrm{s.t.}\quad
&W(g,u)\alpha=\lambda
\begin{bmatrix}F_s(u)\\0_3\end{bmatrix},\\
&\sum_{e=0}^{7}\alpha_{ie}\le 8\ \mathrm N,
&& i=1,2,3,\\
&-\bar\tau_j\le (T(g,u)\alpha)_j\le\bar\tau_j,
&&\forall j,\\
&\alpha\ge0,\quad\lambda\ge0.
\end{aligned}
\]

三个闭合关节取 \(\bar\tau_j=0.90\) N·m，独立掌关节取 1.0 N·m。阶段聚合量为

\[
Q(g,u)=\min\{\lambda_{\rm hold}(g,u),\lambda_{\rm lift}(g,u)\}.
\]

该 LP 的物理含义是：在所建静态点接触模型中，是否**存在**一种接触力分配，能承受若干倍的
竖直保持/理想抬升力，并使质心处合力矩恰为零。它不包含对象转动惯量、角速度/角加速度、
接触刚度/阻尼、实际控制器能否实现该力分配，或满足竖直力后还能抵抗多少附加力矩。

每个阶段还从 HiGHS 返回的一组最优解计算单位任务力矩。三闭合关节写入选中配置的预紧努力为

\[
e_j(g)=\max_{u,s}
\left|T_j(g,u)\frac{\alpha^*_{g,u,s}}{\lambda^*_{g,u,s}}\right|.
\]

这个量**不进入候选排序**。LP 没有用于选择唯一 \(\alpha^*\) 的第二目标；若最优力分配不唯一，
该预紧努力也不是经过证明的唯一最小力矩。动态控制器用它作为停止继续预紧的阈值，但抬升前的
接触确认只要求三指超过共同 0.020 N·m 门限，并不保证实际接触力等于 LP 解。

### 12.4 硬约束、排序量与只记录量必须分开

对候选 \(g\) 和离线场景 \(u\)，剩余闭合行程为

\[
r(g,u)=\min_{j\in\{f1j2,f2j1,f3j2\}}
\operatorname{sgn}(q_j^f-q_j^{pre})
(q_j^f-q_j^{contact}),
\]

硬要求 \(r(g,u)\ge0.075\) rad。命令关节位置裕量为

\[
m_q(g,u)=
\min_{\substack{q\in\{q^{pre},q^{contact},q^{maxpreload}\}\\j}}
\min\{q_j-q_j^{\min},q_j^{\max}-q_j\}>0,
\]

其中
\(q^{maxpreload}=q^{contact}+0.075\operatorname{sgn}(q^f-q^{pre})\)。这是位置限位余量，
不是力矩余量。

禁止几何间隙为

\[
c(g,u)=\min_{(A,B)\in\mathcal F}d_{\rm FCL}(A,B),
\]

\(\mathcal F\) 包含非相邻手部自碰撞对、非指腹—对象、手—桌、以及指腹—对象禁止面。
尽管字段名是 `minimum_forbidden_clearance_m`，它只在三指刚形成共同接触的**最终离散状态**求值，
不是整条接近/闭合路径的最小净空；路径只逐样本拒绝碰撞。

| 类别 | 实际内容 | 是否决定最终抓法 |
|---|---|---|
| 候选生成硬约束 | CAD 截面/径向根存在，预抓—接触—端点顺序有效，三预测点环绕轴，桌面平移后仍在轴向范围 | 失败即排除 |
| 离散路径硬约束 | 51 点接近和逐指闭合无自碰、非指腹碰物、碰桌或禁区接触；指腹不提前碰物；最终三块指腹均接触 | 失败即排除 |
| 行程/位置硬约束 | \(r\ge0.075\) rad，\(m_q>0\) | 失败即排除；通过后的最坏值还进入排序 |
| 静态任务硬约束 | LP 可解且每个离线场景 \(Q\ge1\) | 失败即排除；通过后的最坏值还进入排序 |
| 机械臂名义可行性 | 名义接近和抬升 IK 路点可生成 | 失败即排除；不形成连续分数 |
| 最终机械臂路线 | 候选选定后才从有限种子中选自碰安全路线 | 不比较不同抓法；若第一名失败，程序报错而非自动选第二名 |
| 真正排序量 | 各场景最小的 \(c,Q,m_q,r\) | 按下节的字典序决定 |
| 诊断量 | hold/lift 各自倍率、限制阶段、单位任务手关节力矩/余量、指腹力/余量、wrench rank、名义力闭合布尔量、见证数、接触斑质心和 \(e_j\) | 不直接进入鲁棒 key |
| 完全未计算 | 动态接触保持、对象相对手转角、滑移位移、实际瞬态力矩、对象角动力学、damping=200 的跟踪响应 | 不能决定离线选择 |

配置中的顶层 `task_quality:` 段虽然列出 16 个随机场景、法向误差和接触位置误差，当前
`finite_cad_search.py` 并未读取该段；活动代码读取的是 `finite_cad_search`、
`robustness_evaluation`、`dynamic` 及对象合同。不能把那个未使用配置段写成当前算法已经评价的
不确定性。

### 12.5 有限扰动集合与最终选择公式

离线集合 \(U_{off}\) 是 nominal 加 14 个列举场景：世界 x 平移 \(\pm0.17\) mm、世界 z 偏航
\(\pm2^\circ\)、\(\mu=0.45/0.55\)、质量 \(0.95/1.05\)、对象坐标 x 质心偏移
\(\pm1\) mm、第三指预抓和终点同时偏移 \(\pm0.004\) rad，以及“低摩擦+高质量”和
“+0.17 mm+第三指迟到”两个组合。它不是这些边界的笛卡尔积。

对象平移、偏航和第三指关节误差会重新执行离散手部路径/接触检查；摩擦、质量和质心只重算
静态 LP。第三指预紧比例 \(\pm5\%\) 被明确标为 dynamic-only，完全不进入离线可行性或排序。
默认 nominal 已使用 \(\mu=0.45\)，所以 `friction_lower_0p45` 在离线 LP 中与 nominal 重复。
扰动场景也不重新验证完整机械臂环境路径。

鲁棒可行集合可写为

\[
\mathcal F_{rob}=\{g\in G_\delta:\ H_{path}(g,u)=1,\ Q(g,u)\ge1,
\ \forall u\in U_{off},\ \text{且名义机械臂路点可解}\}.
\]

对四个量分别取有限场景最小值：

\[
\underline c(g)=\min_{u\in U_{off}}c(g,u),\quad
\underline Q(g)=\min_{u\in U_{off}}Q(g,u),
\]

\[
\underline m_q(g)=\min_{u\in U_{off}}m_q(g,u),\quad
\underline r(g)=\min_{u\in U_{off}}r(g,u).
\]

最终选择式是严格的 Python 元组字典序最大化：

\[
\boxed{
g^*=\operatorname*{lexargmax}_{g\in\mathcal F_{rob}}
\left(
\underline c(g),\underline Q(g),\underline m_q(g),\underline r(g),
-i_p,-i_\psi,-i_z
\right)}.
\]

它不是加权“综合分数”，没有归一化，也没有浮点平局容差。只要两个候选的
\(\underline c\) 不完全相等，全部任务承载差异都不会参与选择。例如 TE 结果中一个候选的
\(\underline c\) 仅低约 \(2\times10^{-19}\) m、而 \(\underline Q\) 略高，仍因第一项而落后。
若前四项完全相同，才依次选择更小的掌关节、绕轴和轴向网格索引。

作为对照，`nominal` 模式使用不同顺序：

\[
K_{nom}(g)=\left(Q(g,nom),c(g,nom),m_q(g,nom),r(g,nom),
-i_p,-i_\psi,-i_z\right).
\]

TE 已完成的鲁棒候选给出

\[
(\underline c,\underline Q,\underline m_q,\underline r)
=(0.000888589,1.090637,0.459725,0.229762),
\]

四个最小值分别来自 `yaw_plus_2deg`、`yaw_minus_2deg`、nominal 和
`pose_plus_0p17mm_finger3_late`。这说明它们不是一个共同“最先失败工况”。当前公开型号的 V5
搜索尚未结束，不能填入它的鲁棒键或选择结果。

候选一旦在某场景失败就立即返回；输出的 `failure_scenario` 只是配置顺序中第一个遇到的失败，
不是把扰动幅度连续增大后最先达到物理极限的条件。

### 12.6 当前算法能解释和不能解释什么

| 实际问题 | 代码中有关的信息 | 审计结论 |
|---|---|---|
| 三块指腹能否建立接触 | 完整指腹 FCL；名义及几何扰动下离散顺序闭合；最终三块允许接触；0.075 rad 剩余行程 | 能排除在这些离散状态下不能建立三接触的候选。没有接触压力下界、预紧后的几何复核、柔顺性或时间演化；**当前评分不能直接预测三块指腹在抬升和 2 s 保持中能否持续接触。** |
| 连接器能否离桌 | \(Q\ge1\) 表示存在满足摩擦、8 N 指腹上限和静态手关节力矩上限的重力/理想抬升平衡；名义抬升 IK 可解 | 这是所建模型中的静态可行性证据，不包含桌面释放、实际力分配和跟踪动态；**当前评分不能直接预测实际离桌。** |
| 抬升时姿态变化 | LP 只要求存在零合力矩解 | 没有对象惯量、角动力学、旋转刚度/阻尼、压力中心迁移、附加力矩余量或实际瞬态净力矩；**当前评分不能直接预测姿态变化。** |
| 较大相对滑移 | 八边库仑锥只约束一个静态无滑移力解是否存在 | 没有切向位移、粘滑切换、动摩擦、速度、累计时间或接触顺应性；**当前评分不能直接预测滑移量。** |
| 关节力矩是否接近上限 | LP 对四个独立手关节设静态上限，并记录某个最优力分配下的单位任务力矩；\(e_j\) 写入控制配置 | 能指出静态模型是否被手关节上限约束；不含机械臂力矩、冲击、阻尼和实际时序峰值，而且这些诊断不进 key；**当前评分不能直接预测动态峰值力矩。** |
| 位姿、摩擦、质量、质心、关节误差中谁先失效 | 对列举场景分别得到 \(c,Q,m_q,r\) 的有限最小值；失败时记第一个遍历到的场景 | 没有连续严重度、完整组合、离线预紧误差或动态判据；**当前评分不能直接预测真实动态中哪个条件最先失效。** |
| 加速度一致性 | LP 使用理想命令加速度；评价门使用手基座 z 加速度 | damping=200、实际手臂跟踪和评价器的有限差分量均不进选择；**当前评分不能直接预测该门是否通过。** |

现有两个数值更像反例线索而非因果证据：当前 V4 名义抓法的
\(Q=1.967809\) 高于 TE V5 鲁棒抓法的名义 \(Q=1.281565\)，但其世界姿态变化更大。
这足以否定“这两个运行中，较高竖直静态承载倍率必然对应较小世界转角”的朴素解释；由于对象、
抓法版本、动态摩擦输入和机械臂运动均未完全配平，它不能进一步证明某个遗漏量就是原因。

### 12.7 2022—2026 年方法的逐项数学边界

以下只比较真正进入目标函数或标签的物理信息，不把论文后验仿真结果倒灌成优化输入。

| 方法 | 实际数学目标 | 接触建立/动态执行是否进入方法 | 不确定性 | 已覆盖的思想与本项目剩余差异 |
|---|---|---|---|---|
| [FRoGGeR](https://arxiv.org/html/2302.13687) | 对名义点接触 wrench 矩阵 \(W(q)\)，\(\ell^*(q)=\max_{\alpha,\ell}\ell\)，约束 \(W(q)\alpha=0,\ \mathbf1^T\alpha=1,\ \alpha\ge\ell\mathbf1\)；外层最大化 \(\ell^*\) 并满足运动学/碰撞。\(\ell^*>0\) 判名义力闭合，数值大小只是经验代理。 | 评分是静态最终接触；Drake 抬升/摇晃是后验评价，而且另有对象位姿反馈、质量、接触力 QP 和力矩限制。 | 无参数不确定性进入评分。 | 已覆盖可微名义力闭合代理。当前项目不同在有限枚举、完整指腹合法几何、实际力/力矩上限和列举扰动，但这些组合尚未证明形成更好评分。 |
| [TaskDexGrasp](https://arxiv.org/html/2309.13586) | 任务集合 \(\mathcal W_t(w_t,\gamma)=\{t:\angle(t,w_t)\le\gamma,\|t\|=1\}\)。实际任务项为 \(E_t=-\sum_k\cos\beta_k\)，其中 \(\beta_k=\angle(s_{\mathcal W_t}(u_k),s_{\mathcal W_g}(u_k))\)；另有接触距离、穿透和自碰项。余弦目标刻意不保留 GWS 幅值。 | 静态手姿；最近表面距离不是闭合轨迹。Isaac 固定手姿受载 100 步只是评价。 | \(\gamma\) 是任务方向扇区，不是位姿/摩擦/质量/执行误差。 | 已覆盖任务力和任务力矩方向，故“加入任务力矩”不是新思想；它不直接给绝对载荷、关节力矩、滑移或姿态动态。 |
| [PONG](https://arxiv.org/html/2309.16930) | 固定接触位置，法向满足 \(n_i=\bar n_i+T_i\epsilon_i,\ \epsilon_i\sim\mathcal N(0,\Sigma_i)\)。目标是力闭合概率 \(P_{fc}\)，算法最大化由各指保守法向区域构造的下界 \(L_{fc}=\prod_i\int_{\mathcal A_i}p_i(n_i)dn_i\le P_{fc}\)。 | 静态力闭合概率；拾取/摇晃为后验评价。 | 只覆盖已知接触位置处相互独立的高斯法向误差。 | 已覆盖解析概率式不确定力闭合。当前有限位姿、摩擦、质量、质心和关节集合不同，但不能声称首次考虑不确定性。 |
| [SpringGrasp](https://arxiv.org/html/2404.13532) | 以虚拟弹簧 \(f_i=k_i(o_i-p_i)-2\sqrt{k_i}\dot p_i\) 建模；平衡位姿由 \(\arg\min_{R,t}\sum_i\frac12k_i\|RR_0^T(p_i(t_0)-t_0)+t-o_i\|^2\) 得到，目标同时含初始/平衡摩擦锥角裕度、GPIS 表面概率、预抓、增益、碰撞、最小夹力等项。 | 明确联合优化预抓和柔顺接触建立，但不做完整前向接触仿真；假设粘着无滑移、小转角、阻尼充分，并明确忽略重力。 | GPIS 形状/表面位置不确定性。 | 已覆盖接触建立过程与控制增益联合优化。冻结本项目控制器会删掉其核心，保留其控制器又破坏公平对照；“考虑过程”不能作为本项目首创。 |
| [BODex](https://arxiv.org/html/2412.16490) | 对目标方向 \(t_j\)，下层 \(Q_j(x)=\min_f\|\beta t_j-\sum_iG_i(x)f_i\|^2\)，约束点摩擦锥和法向力；上层最小化 \(\sum_jQ_j\) 及姿态/接触/碰撞能量。官方 force-closure 设置只用六个正负纯力方向、零目标力矩。 | 生成 1 cm 预抓、接触和挤压目标；QP 仍是静态力分配，MuJoCo 执行是后验验证。 | 无参数不确定性进入目标。 | 已覆盖可微 QP 载荷拟合、预抓—挤压，以及离线能量对动态成功的 ROC-AUC。当前项目的实际 N/N·m 上限和有限扰动更具体，但 QP 承载和评分—动态相关性都不是新思想。 |
| [Get a Grip](https://arxiv.org/html/2410.23701) | 每个抓法的 5 次腕部扰动得到 \(y_{coll},y_{pick},y_{PGS}=y_{coll}\land y_{pick}\)，各自取均值作软标签；网络用三项等权 L2 训练，并按 \(\hat y_{PGS}\) 局部改进后取最大值。 | 标签完整包含预抓碰撞、PD 闭指、接触建立和 20 cm 抬升，故模拟器/控制器动态通过监督标签进入评分。 | 腕部每轴 \(\pm5\) mm、每角 \(\pm2.5^\circ\) 的 5 次经验扰动；摩擦 0.9、密度 500 kg/m³ 固定。 | 已覆盖从正负动态执行数据学习成功评分。其视觉/NeRF、数百万样本和学习范式不适合作为当前最小直接基线；当前项目的优势若存在，是小规模可审计物理合同，不是首次用动态失败。 |
| [Grasp to Act](https://arxiv.org/html/2602.20466) | 候选分别承受 12 个正负力/力矩轴的线性递增施扰，越过平移/转角阈值停止；\(S_G=\frac1{12}\sum_{i=1}^{12}t_{stable}^{(i)}/t_{max}\)。随后用任务轨迹和残差 PPO 执行。 | 候选由力矩停止阈值闭指形成包络接触、释放沉降；动态力/力矩施扰直接进入评分，执行中还有在线关节修正。 | 质量、摩擦和任务力 \(\pm30\%\) 只在后续 RL 训练随机化，不是 \(S_G\) 的有限最坏值。 | 已覆盖显式接触形成、动态力/力矩评分、姿态/滑移判据和在线适应，是最接近的创新性边界。当前项目只能强调完整合法指腹、明确执行器上限、列举参数最坏值和固定控制器；不能声称首次做动态稳定评分。 |

### 12.8 真实创新缺口

近年工作已分别覆盖名义力闭合、任务力/力矩、概率或形状不确定性、预抓和接触建立、动态标签、
动态力/力矩施扰、在线执行适应，以及离线评分与动态成功相关性。对有限场景取最小值、完整枚举
有限集合或使用字典序选择，也都是标准优化/实现选择，不会因为应用到连接器而自动成为新算法。

当前实现还不能称为“完整指腹接触保持 + 任务力/力矩 + 执行过程 + 动态扰动的联合评分”：

- 完整指腹只进入几何合法性，受力仍是三点且没有接触保持动态；
- 任务 LP 只要求竖直力加零力矩，没有一般任务力矩方向或附加转矩余量；
- 执行过程只由离散运动学/碰撞硬约束表示，控制器瞬态不进入评分；
- 鲁棒部分是稀疏列举场景的硬可行性与四项最小值，预紧误差和动态失败不进排序；
- 最终 key 首先优化最终接触状态的禁止几何净空，而不是动态成功、姿态或滑移。

因此目前没有证据支持“新评分算法、算法优越、跨型号泛化或论文已经成立”。仍可能成立的窄贡献
只是：在同一有限候选和固定控制器下，用完整合法指腹、三指法向力上限、四个独立手关节的
准静态雅可比力矩上限、任务载荷及预先声明的有限位姿—摩擦—质量—质心—关节误差，做一个
可审计的工业连接器动态预测实证。它是否有增量预测价值，仍必须由冻结后的同候选、同控制器
动态相关性和选抓结果证明；上述上限不覆盖机械臂、瞬态手指动力学或实际控制输出。

### 12.9 唯一可反驳算法假设：竖直承载后的最弱附加转矩余量

本审计只保留一个开发假设，不据此修改算法：当前 \(Q\) 同时缩放竖直力并强制目标力矩为零，
却没有量化在承担一倍真实 hold/lift 载荷后还能抵抗多少附加转矩。对场景 \(u\)、阶段
\(s\) 和对象坐标六个正负轴
\(d\in\mathcal D_\tau=\{\pm e_x,\pm e_y,\pm e_z\}\)，定义

\[
\begin{aligned}
\rho_\tau(g,u,s,d)=\max_{\alpha,\rho}\quad&\rho\\
\mathrm{s.t.}\quad
&W_F(g,u)\alpha=F_s(u),\\
&W_M(g,u)\alpha=\rho d,\\
&\sum_e\alpha_{ie}\le8\ \mathrm N,\\
&-\bar\tau_j\le(T(g,u)\alpha)_j\le\bar\tau_j,\\
&\alpha\ge0,\quad\rho\ge0,
\end{aligned}
\]

并取

\[
\underline\rho_\tau(g)=
\min_{u\in U_{off},\ s\in\{hold,lift\},\ d\in\mathcal D_\tau}
\rho_\tau(g,u,s,d).
\]

若要与下述单一动态重放对应，不能拿这个跨场景/跨阶段最小值去解释一个固定工况。令 \(u_0\)
表示“显式并经运行证据确认 \(\mu=0.45\)，其余扰动量为名义值”，定义匹配 lift 工况的量

\[
\rho_{\tau,0}^{lift}(g)=
\min_{d\in\mathcal D_\tau}\rho_\tau(g,u_0,\mathrm{lift},d).
\]

这里 \(W_F\) 和 \(W_M\) 分别是 \(W\) 的力与力矩三行，
\(\underline\rho_\tau\) 的单位是 N·m。它问的是“一倍任务力已经满足后，最弱方向还剩多少转矩
容量”，与当前的 \(Q\) 不是同一量。但它仍是一个假定接触力能针对每个方向自由最优重分配的
静态存在性 LP；没有每指正压力下界、接触刚度、实际控制器力分配、对象惯量或动态净转矩。
所以它既不是旋转刚度，也不能单独预测动态转角。

**假设 H-R：** 在现有两个固定的“对象—抓法—控制器”组合上，较小的
\(\rho_{\tau,0}^{lift}\) 是否伴随较大的对象相对手转角，并且当前公开型号的主要相对转轴是否落在
其静态最弱离散轴上。这个描述性假设由“当前抓法 \(Q\) 更高但世界姿态变化更大”的观测提出，
但它不预设预紧、力分配误差或控制器就是原因。

最小验证不增加 CAD、不改控制器、不重选抓法，也不做参数扫描：

1. 先对**这两个已经执行过的候选**用同一三个接触合力点、八边摩擦锥、8 N 和 0.90/1.0 N·m
   上限，在相同 \(u_0\) 和 lift 阶段计算 \(\rho_{\tau,0}^{lift}\)。若
   \(\rho_{\tau,0,current}^{lift}\ge\rho_{\tau,0,TE}^{lift}\)，立即否定 H-R 并停止，不启动动态。
2. 只有第一步方向支持时，才在固定种子、相同显式 \(\mu=0.45\)、相同预紧饱和控制和
   damping=200 下，对两个原候选各执行一次相同抬升并保留完整轨迹。运行记录必须证明最终有效的
   手—对象摩擦确为 0.45；只出现命令行值而材料审计仍为 1.4 不算配平。以三指接触和预紧完成时
   为共同基准 \(t_0\)，评价对象相对手的旋转
   \[
   R_{HO}(t)=R_{WH}(t)^TR_{WO}(t),\qquad
   \theta_{rel}^{max}=\max_t d_{SO(3)}(R_{HO}(t_0),R_{HO}(t)),
   \]
   而不再用对象世界转角代替抓持内转动。

令 \(t^*=\arg\max_t\theta_{rel}(t)\)，把
\(\log(R_{HO}(t_0)^TR_{HO}(t^*))\) 的单位轴 \(a_{rel}\) 表达在对象初始坐标系中。令
\(\mathcal A=\{e_x,e_y,e_z\}\) 为三个无符号对象轴；每个轴的静态容量取两个转矩方向中较小者。
定义观测离散轴和预测最弱轴集合

\[
\mathcal A_{obs}=\arg\max_{a\in\mathcal A}|a^Ta_{rel}|,
\qquad
\mathcal A_{weak}=\arg\min_{a\in\mathcal A}
\min_{\sigma\in\{-1,+1\}}\rho_\tau(g,u_0,\mathrm{lift},\sigma a).
\]

只有同时满足

\[
\rho_{\tau,0,current}^{lift}<\rho_{\tau,0,TE}^{lift},
\qquad
\theta_{rel,current}^{max}>\theta_{rel,TE}^{max},
\qquad
\mathcal A_{obs,current}\cap\mathcal A_{weak,current}\ne\varnothing,
\]

才把 H-R 记为开发性支持，同时连续报告观测轴与最弱轴的无符号夹角，不另设事后角度阈值。
任何一项不满足，或 3.768° 主要来自手基座/手臂一起转而非对象相对手转动，都立即否定并停止该
方向，不增加权重、候选、神经网络、CAD 或第三种解释来挽救它。若仿真确定性尚未由合同证明，
每个条件一次固定种子重放只具有开发筛查意义，不提供统计重复性。

即使得到支持，也只能说“两组固定对象—抓法上观察到与静态余量一致的描述性排序”。两个 CAD 的
质量、质心和惯量，V4/V5 抓法版本，对象专属机械臂路线/手基座激励，以及各抓法不同的
LP-derived 预紧阈值 \(e_j\) 都没有被消除；\(R_{HO}\) 也不能消除手基座加速度施加给对象的惯性
载荷。因此不能把支持结果写成预紧误差机制、评分因果改进或 V5 有效性。附加转矩/任务 wrench
已被 TaskDexGrasp、BODex 和 Grasp to Act 覆盖，\(\underline\rho_\tau\) 也不能包装成
“首次任务力矩评分”或既成算法创新。

本次审计不启动前文的 6-CAD 盲测面板，也不实现七篇论文的任何基线；在上述最小假设尚未通过
以前，扩大 CAD、训练数据或基线数量都不会回答当前最早的评分—控制归因问题。

### 12.10 本节的一次只读反方审查

独立反方角色没有修改文件，也没有运行 Isaac、CUDA 或测试。它的结论及本节处理如下：

| 反方攻击 | 处理结果 |
|---|---|
| 手基座加速度门和对象世界转角可能被错归因于抓法评分 | 保留“加速度量属于手基座、世界转角不等于对象相对手转角”的边界；H-R 先要求完整轨迹中的 \(R_{HO}\)。 |
| 两个不等式和转轴相符不能证明预紧误差导致转动 | 删除因果措辞，把 H-R 收窄为两组固定对象—抓法的描述性关联；明确列出惯量、抓法版本、机械臂路线、基座激励和不同 \(e_j\) 等未消除混杂。 |
| \(\underline\rho_\tau\) 可能被包装成动态稳定量或新算法 | 明确它仍是静态存在性 LP，不是旋转刚度或动态转角预测；TaskDexGrasp、BODex 和 Grasp to Act 已覆盖任务转矩/动态施扰思想。 |
| “显式 \(\mu=0.45\)”可能只是命令而非有效物理输入 | 要求运行证据确认最终有效手—对象摩擦；材料仍为 1.4 时直接判定未配平。 |
| 30° 转轴阈值没有物理依据 | 删除任意角阈值；用预先定义的 SO(3) 对数轴、初始对象坐标和无符号离散轴集合判定，并连续报告夹角。 |
| 在最早归因未解决时扩到 6 个 CAD 或大规模基线 | 本节不启动新 CAD、外部基线或训练；唯一下一证据仍是 H-R 的离线先验否定门和两次固定条件轨迹。 |

反方未发现 \(G_\delta\)、接触合力点、\(W/T\)、hold/lift LP、硬约束、稀疏
\(U_{off}\) 或 Python 字典序公式与当前源码不一致。这个结论只说明本节还原了当前代码，不说明该
代码能预测动态成功。

### 12.11 H-R 第一步离线否定门

下表全部使用冻结的离线条件，单位为 N·m；正负号表示对象坐标轴上的附加转矩方向。

| 已执行的固定候选 | $\rho_\tau(+x)$ | $\rho_\tau(-x)$ | $\rho_\tau(+y)$ | $\rho_\tau(-y)$ | $\rho_\tau(+z)$ | $\rho_\tau(-z)$ | $\rho_{\tau,0}^{lift}$ | 最弱方向 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 当前公开型号 V4 名义候选 | 0.046065 | 0.064751 | 0.055729 | **0.027713** | 0.113354 | 0.113332 | **0.027713** | $-y$ |
| TE V5 鲁棒候选 | 0.028588 | 0.062636 | **0.013902** | 0.056574 | 0.098145 | 0.058995 | **0.013902** | $+y$ |

结论：$\rho_{\tau,0,current}^{lift}=0.027713\ge
\rho_{\tau,0,TE}^{lift}=0.013902$，因此按提前冻结的判据，**H-R 在第一步被否定并立即停止**；
不启动后续动态配平实验，也不修改公式。LP 可能存在多个同样最优的接触力分配，因而不能物理
解读某一个最优分配，但这不改变其标量最优值；接触点和法向来自指腹接触见证点的算术平均，
把完整指腹压缩成了点接触；并且每个方向都允许接触力静态最优重分配。因此这些数值是该静态
点接触模型内的存在性容量，不是实际控制器的力分配、旋转刚度或动态可实现余量。本次
$\mu=0.45$ 仅是冻结的离线 LP 条件，不代表此前动态运行的实际材料摩擦。

### 12.12 V4/V5 配对实验就绪审计

本节不恢复已经否定的 H-R，只检查同一型号内 V4 与 V5 是否已有严格可比证据。共同源码版本锁定为
`78a44af3a4e12aeb9831fcca27102055bba4d403`；冻结控制器在 0.5 s（120 个
$1/240$ s 物理步）内用 minimum-jerk 从 60 平滑切换到指定 lift damping=200。四个目标抓法还
必须由配置内容哈希单独锁定，因为这些配置不是仅凭该 Git commit 就能唯一恢复的。

| 核对项 | 当前型号 V4 | 当前型号 V5 | TE V4 | TE V5 |
|---|---|---|---|---|
| 抓法与当前配置身份 | `p15_y13_z07`；`finite_gdelta_v4/current_run01/selected_config.yaml`；SHA-256 `72a43e33a0bfcb9758906f1bb9f2b80139260e3582d0d4fe2f1df3b0e4db967d` | `p10_y10_z14`；`finite_gdelta_v5_robust/current_run01/selected_config.yaml`；SHA-256 `92530106ecc5c0ef293d9c87eaf33b923cf4c0e48971a622c7311716963e4534` | `p14_y15_z26`；`finite_gdelta_v4/te_run01/selected_config.yaml`；SHA-256 `38be60964fb4e5d603e59010086c27ba1d4b5d63c1a6fe206e6e31641ff08a54` | `p05_y02_z26`；`finite_gdelta_v5_robust/te_run01/selected_config.yaml`；SHA-256 `2e9dabcc27ca33843aefde4757a5c9427c2aa51a6f400a40f0c8eed4294b8172` |
| 既有评价绑定的代码/配置 | 父提交 `e9d05e0`；旧配置 SHA `0eadd504…`，不是上列配置 | 源码哈希对应 `78a44af`；旧配置 SHA `e254a0c6…`，不是上列配置 | 父提交 `e9d05e0`；旧配置 SHA `23e1b4fa…`，不是上列配置 | 源码哈希对应 `78a44af`；旧配置 SHA `641840f2…`，不是上列配置 |
| 控制器源码 | `c9c747…`，不是冻结控制器 | `c3bf4a…`，冻结共同控制器 | `c9c747…`，不是冻结控制器 | `c3bf4a…`，冻结共同控制器 |
| `preload_increment` | 0.075 rad | 0.075 rad | 0.075 rad | 0.075 rad |
| lift damping / 切换 | 160；瞬时切换 | 200；平滑切换 | 160；瞬时切换 | 200；平滑切换 |
| 物理步长 | $1/240$ s | $1/240$ s | $1/240$ s | $1/240$ s |
| 既有运行的摩擦证据 | 只读回指腹 1.4/1.4；最终材料对未审计 | 只读回指腹 1.4/1.4；最终材料对未审计 | 只读回指腹 1.4/1.4；最终材料对未审计 | 只读回指腹 1.4/1.4；最终材料对未审计 |
| 质量和质心运行时证据 | 未保存完整读回 | 质量 0.31 kg；保存分件质心读回 | 未保存完整读回 | 质量 0.310000002 kg；质心约 $[3.365\times10^{-6},1.408\times10^{-6},-0.01813548]$ m |
| 扰动 | 名义运行 | 名义运行 | 名义运行 | 名义运行 |
| 动态随机种子 | runner 无随机种子接口或记录；搜索种子不能代替动态种子 | 同左 | 同左 | 同左 |
| 完整轨迹 / $R_{HO}$ | 未保存 / 不能计算 | 未保存 / 不能计算 | 未保存 / 不能计算 | 未保存 / 不能计算 |
| 严格配对判定 | **与当前 V5 不可比较** | **与当前 V4 不可比较** | **与 TE V5 不可比较** | **与 TE V4 不可比较** |

现有 V4 与 V5 在代码、控制器、阻尼值、阻尼切换、配置绑定和轨迹证据中至少一项不同，因此没有
任何现有的同型号 V4/V5 结果可以直接比较。较晚的一次当前型号 V4 damping=200 运行仍使用旧控制器、
瞬时切换且无轨迹，也不能补齐配对。最小缺失是四组新实验：每个目标抓法各做一次与其绑定的新
preflight 和一次 grasp-lift，共四组、八次 Isaac 进程调用。

当前还有一个运行前硬阻塞。`friction_lower_0p45` 确实把共享指腹材料从 1.4/1.4 写入并读回为
约 0.45/0.45；问题不是指腹仍为 1.4。但是它既不写对象材料，也不读回最终材料对。指腹的
`frictionCombineMode=max`；当前型号实际接触的 CouplingNut 材料为静摩擦 0.55、动摩擦
0.45，且组合模式也是 `max`，故最终有效静/动摩擦是 **0.55/0.45**，不是 0.45/0.45。TE
资产没有显式 PhysicsMaterialAPI/材料绑定，现有证据也没有读回其对象侧材料或组合结果，所以其
有效摩擦未知。由此，冻结 runner 目前不能把两款对象的最终有效静、动摩擦都固定并证明为 0.45；
按预先判据，下面四条只能作为**被摩擦审计阻塞的命令草案，不得执行**，本节不修改源码或资产来
绕过阻塞。

四条草案共用以下前置量；平滑切换是冻结控制器的固定行为，不存在另一个 CLI 开关。命令没有使用
`--omit-trace-json`，并同时锁定源码和抓法配置哈希，输出目录也拒绝覆盖已有证据。

~~~bash
REPO=/home/noob/WorkPlace/kcgtest1
COMMIT=78a44af3a4e12aeb9831fcca27102055bba4d403
ISAAC_PY=/home/noob/WorkPlace/isaacsim/.conda-env/bin/python
RUNNER="$REPO/src/kcg_connector/isaac/carts_v2/run_grasp_lift.py"
RUNTIME="$REPO/src/kcg_connector/config/carts_v2_isaac_runtime.json"
ROBOT="$REPO/artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/nailfree_three_finger_direct_v2_shrinkwrap/handarm_nailfree_three_direct.usda"
OUT_ROOT="$REPO/artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/v4_v5_paired_mu0p45_78a44af_20260829"

test "$(git -C "$REPO" rev-parse HEAD)" = "$COMMIT"
git -C "$REPO" diff --quiet "$COMMIT" -- \
  src/kcg_connector/isaac/carts_v2/controller.py \
  src/kcg_connector/isaac/carts_v2/run_grasp_lift.py \
  src/kcg_connector/isaac/carts_v2/evaluate_run.py \
  src/kcg_connector/isaac/carts_v2/engine_health.py \
  src/kcg_connector/config/carts_v2_isaac_runtime.json
test "$(sha256sum "$ROBOT" | cut -d' ' -f1)" = "d4ec8ada91bf948824bdf2c50d007ef86e03834c234059e45ef4785493781747"
~~~

当前型号 V4：

~~~bash
(
  set -eu
  CFG="$REPO/artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/finite_gdelta_v4/current_run01/selected_config.yaml"
  OUT="$OUT_ROOT/current_v4"
  test "$(sha256sum "$CFG" | cut -d' ' -f1)" = "72a43e33a0bfcb9758906f1bb9f2b80139260e3582d0d4fe2f1df3b0e4db967d"
  test ! -e "$OUT"
  COMMON=(--object-id current_d38999_26kj61sn_public_spec --config "$CFG"
    --runtime-resources "$RUNTIME" --robot-asset "$ROBOT"
    --robustness-scenario friction_lower_0p45
    --preload-increment-rad 0.075 --finger-preload-scales 1 1 1
    --lift-arm-damping-nm-s-rad 200)
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode preflight "${COMMON[@]}" --output-directory "$OUT/preflight"
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode grasp-lift "${COMMON[@]}" \
    --preflight-evaluation "$OUT/preflight/evaluation.json" \
    --output-directory "$OUT/grasp_lift"
)
~~~

当前型号 V5：

~~~bash
(
  set -eu
  CFG="$REPO/artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/finite_gdelta_v5_robust/current_run01/selected_config.yaml"
  OUT="$OUT_ROOT/current_v5"
  test "$(sha256sum "$CFG" | cut -d' ' -f1)" = "92530106ecc5c0ef293d9c87eaf33b923cf4c0e48971a622c7311716963e4534"
  test ! -e "$OUT"
  COMMON=(--object-id current_d38999_26kj61sn_public_spec --config "$CFG"
    --runtime-resources "$RUNTIME" --robot-asset "$ROBOT"
    --robustness-scenario friction_lower_0p45
    --preload-increment-rad 0.075 --finger-preload-scales 1 1 1
    --lift-arm-damping-nm-s-rad 200)
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode preflight "${COMMON[@]}" --output-directory "$OUT/preflight"
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode grasp-lift "${COMMON[@]}" \
    --preflight-evaluation "$OUT/preflight/evaluation.json" \
    --output-directory "$OUT/grasp_lift"
)
~~~

TE V4：

~~~bash
(
  set -eu
  CFG="$REPO/artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/finite_gdelta_v4/te_run01/selected_config.yaml"
  OUT="$OUT_ROOT/te_v4"
  test "$(sha256sum "$CFG" | cut -d' ' -f1)" = "38be60964fb4e5d603e59010086c27ba1d4b5d63c1a6fe206e6e31641ff08a54"
  test ! -e "$OUT"
  COMMON=(--object-id te_deutsch_d38999_26fj35pn_step --config "$CFG"
    --runtime-resources "$RUNTIME" --robot-asset "$ROBOT"
    --robustness-scenario friction_lower_0p45
    --preload-increment-rad 0.075 --finger-preload-scales 1 1 1
    --lift-arm-damping-nm-s-rad 200)
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode preflight "${COMMON[@]}" --output-directory "$OUT/preflight"
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode grasp-lift "${COMMON[@]}" \
    --preflight-evaluation "$OUT/preflight/evaluation.json" \
    --output-directory "$OUT/grasp_lift"
)
~~~

TE V5：

~~~bash
(
  set -eu
  CFG="$REPO/artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/finite_gdelta_v5_robust/te_run01/selected_config.yaml"
  OUT="$OUT_ROOT/te_v5"
  test "$(sha256sum "$CFG" | cut -d' ' -f1)" = "2e9dabcc27ca33843aefde4757a5c9427c2aa51a6f400a40f0c8eed4294b8172"
  test ! -e "$OUT"
  COMMON=(--object-id te_deutsch_d38999_26fj35pn_step --config "$CFG"
    --runtime-resources "$RUNTIME" --robot-asset "$ROBOT"
    --robustness-scenario friction_lower_0p45
    --preload-increment-rad 0.075 --finger-preload-scales 1 1 1
    --lift-arm-damping-nm-s-rad 200)
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode preflight "${COMMON[@]}" --output-directory "$OUT/preflight"
  env PYTHONPATH="$REPO/src/kcg_connector" "$ISAAC_PY" "$RUNNER" \
    --mode grasp-lift "${COMMON[@]}" \
    --preflight-evaluation "$OUT/preflight/evaluation.json" \
    --output-directory "$OUT/grasp_lift"
)
~~~

若且仅若主轨道先用运行时读回证明两款对象的最终有效静、动摩擦均为 0.45，四组才进入执行资格。
比较量提前固定为：三块指腹是否合法接触并保持、是否离桌、抬升是否达到 50 mm、是否保持 2 s、
最大相对滑移、$R_{HO}(t)=R_{WH}(t)^TR_{WO}(t)$ 相对共同预紧完成时刻的最大转角、三根闭合关节
的峰值去零手指力矩、错误接触，以及轨迹中的最早失败阶段；对象世界转角不替代 $R_{HO}$。

结论分支同时冻结：两款型号上 V5 都优于或不差于 V4，才继续扰动对比；只改善一款，只能写
“型号依赖”；V5 没有改善或更差，接受负结果并检查当前净空优先目标，不增加指标；四次都成功但
差异很小，只能写“等效”，不得写显著提高。

## 13. 冻结扰动的真实结果：H1 为型号依赖的混合结果

运行时材料审计已解决 12.12 的阻塞：在 `low_friction_high_mass` 中，三块完整指腹和连接器实际
碰撞面的静、动摩擦均读回 0.449999988，双方组合规则为 `max`，所以接触对的计算有效静、动摩擦
也为 0.449999988；对象质量与对角惯量均按 1.05 倍读回。控制器、手模型和安全门没有修改。

冻结动态面板选择在当前型号和 TE 上各评价 10 个离散扰动点，20 次单次运行均完成三块完整指腹、
离桌、50 mm 和悬空 2 s。该结果支持“面板选择在这些点可执行”，但没有测到其连续失效边界，也
没有统计重复。

公平对照首先选择面板中负载侧最窄的 `low_friction_high_mass`：

| 型号 | V4 名义结果 | 冻结面板选择结果 | 对 H1 的判别 |
|---|---|---|---|
| 当前 | 55.421 mm、2 s、滑移 0.756 mm、姿态 0.631° | 54.400 mm、2 s、滑移 1.469 mm、姿态 5.429° | 不支持；两者都完成任务，名义连续余量更好 |
| TE | 第三指在 20.275 s 抬升阶段迁出指腹；未离桌，1.906 mm、悬空 0 s | 55.007 mm、悬空 2 s | 支持该离散点 |

TE 的失败只有完整动态执行才能观察到。这与 Get a Grip 和 Grasp to Act 将动态接触形成/稳定时间
纳入评价的机理一致；它不证明学习方法本身是唯一解。PONG 以给定接触的法向不确定性为前提，不能
直接预测这次第三指沿完整指腹迁移。SpringGrasp 的虚拟弹簧/阻抗会改变本轮冻结控制器，因此仍不
移植其控制律。

当前型号随后只检验了由离线禁碰余量直接预测的 x=±0.17 mm 两端。V4 名义在 −0.17 mm 和
+0.17 mm 分别抬升 55.923 mm、55.945 mm，均悬空保持 2 s；冻结面板选择对应为 55.800 mm、
55.854 mm，也均完成。由此“V5 更大离线禁碰余量会在这两个位置端点产生更大动态成功范围”的
预测被否定，位置端点路线停止，不再增加位移或角度。

因此当前证据只允许写：

\[
\text{H1 在 TE 的一个冻结组合点受到支持，但在当前型号同一点及两个位置端点不受支持。}
\]

这不是双型号一致优势，不是完整 (U_\delta) 比较，不是连续空间全局最优，也不是严格盲测或硬件
验证。若继续评价 H1，唯一合规路线是保持所有条件冻结，补齐当前型号 V4 名义抓法尚未运行的
(U_\delta) 成员；不能依据本次混合结果新增权重、改阈值或扩大扰动。
