# kcgtest1 协作规则

## 唯一入口与优先级

- 本文件适用于仓库根目录及全部子目录；用户最新直接指令在不违反事实、证据和安全边界
  的前提下优先。
- 新对话先读 `CURRENT_CONTEXT_CN.md`，再按其中的最小路线读取当前任务文件。
- 当前任务、候选、允许动作和下一步只在 `CURRENT_CONTEXT_CN.md` 中声明。Git 历史、
  标签、旧产物和报告只用于追溯，不能自行恢复成当前指令。
- 状态冲突、正式 gate 变更或动态启动前必须回查原始配置、产物、Git、进程和时间戳；
  摘要文件不能替代正式证据。

## 判断与实施原则

- 不得因为用户施压、限定答案或要求确定性，就复述缺乏证据的结论。用户要求的措辞与
  已知事实冲突时，必须明确指出冲突并坚持可证实的答案；不得为了迎合用户改变判断。
- 所有非琐碎问题先从第一性原理拆解：明确目标物理结果、已验证事实、未知量、最短因果链
  和最早阻塞点。先解决阻断实际结果的物理问题，再考虑扩展工具、框架或认证。
- 实施默认采用最小充分改动并优先复用现有入口。能用现有代码、一个小改动或一次直接实验
  回答的问题，不得扩张成新脚本、模块、优化器、管理器、测试体系或报告体系。
- 新增代码、测试、gate 或结果产物前，必须说明它具体回答哪个尚未解决的物理问题，以及
  为什么现有入口不能回答；不能给出这两个答案时，不得新增。
- 代码量、测试数、候选数、结果文件数和提交数都不是进展。进展只按离当前用户目标更近的
  可观察物理结果衡量；一次失败不得自动扩张成新路线或新框架。

## 用户沟通

- 用户可见解释使用中文；文件名、接口和原始参数可保留英文。
- 先说明机器人、三指手、连接器或仿真物理发生了什么，再说明代码与状态。
- 优先按“传感器读数 → 误差 → 控制律 → 指令 → 实际运动 → 判据”组织控制问题。
- 首次出现的新抓取/PhysX术语用一句白话说明；`wrench` 写成“三个方向的力和三个方向
  的力矩”，不单独简称“扳手”。

## 证据边界

- 严格区分程序正常结束、静态检查、离线验证、研究动态、正式动态和真实硬件验证。
- 退出码 0、`PASS`、生成文件、候选数量、图片或离线几何均不能单独证明物理动作成功。
- 抓取结论必须同时检查真实接触、离桌、抬升、保持、滑移、桌面接触、穿透和安全限制；
  插入/锁紧还要检查位移、转角、轴向前进及三个方向的力和三个方向的力矩。
- 仿真真值只可按当前合同用于运行后评价，不得进入在线控制。
- 所有结论标明“已验证事实”“根据数据推断”或“尚未验证”，历史结果不得写成当前结果。

## 稳定安全边界

- 工程默认是 simulation-only；除非用户另行明确授权并关闭硬件门，
  `hardware_authorized=false`。
- 动态运行必须由当前上下文和原始控制面共同明确授权；清理、文档、静态测试和离线检查
  不得推导出动态授权。
- 冻结模型、连接器身份、质量/质心/材料、当前安全门限和证据产物不得因清理或重构修改。
- 禁止磁吸、隐藏固定、运行后写物体位姿、在线真值控制、无限增力、批量盲扫参数和伪造证据。

## 修改、清理与验证

- 每次编辑文件前，先用普通中文说明为什么改、服务哪个当前问题、不会改变哪些冻结边界。
- 只读查看、审计和清理不得顺带启动 Isaac、生成动态媒体或改变物理门限。
- 工作树现有修改和未跟踪文件均视为用户或并行任务资产；不覆盖、不暂存、不清理。
- 旧源码和旧文档只从 Git 标签按需读取，不恢复到活动树；清理优先使用可恢复标签。
- 状态只使用 `NOT_STARTED`、`IMPLEMENTING`、`STATIC_PASS`、`OFFLINE_PASS`、
  `DYNAMIC_PASS`、`PARKED`、`BLOCKED_EXTERNAL`。
- 只暂存本任务明确修改的路径；禁止 `git reset --hard`、`git checkout --`、
  强制推送和宽泛清理。未经用户明确要求不推送远端。
