# kcgtest1 当前协作规则

## 入口、优先级与适用范围

- 本文件适用于仓库根目录及全部子目录。用户最新直接指令优先；用户说“停下”“走错了”或要求只读时，立即停止对应动作。
- 新对话、上下文压缩恢复、普通源码工作和静态测试先读根目录 `CURRENT_CONTEXT_CN.md`，再按其中“最小读取路线”读取当前任务文件；禁止默认加载全部历史台账。
- `CURRENT_CONTEXT_CN.md` 是轻量路由，不是正式验收记录。它缺失、过期、与原始产物冲突，或准备启动动态仿真时，必须失败关闭并回查原始控制面。
- 已结束的 2026-08-18 动态覆盖、R11/R12、H1-H25、旧 `B_GOAL_MODE` 和八小时窗口规则属于历史合同，不再作为默认指令。原文由 Git 标签 `pre-contract-cleanup-20260821` 和 `artifacts/agent_control/` 保留。

## 用户能力与交流方式

- 所有用户可见解释使用中文；文件名、接口名和原始参数可保留英文，但首次出现的新仿真/抓取术语要用一句白话说明物理含义。
- 用户是有 STM32、ROS、三指手运动学、位置/刚度控制、BLDC、CAN/EtherCAT 和真实样机经验的应用型控制工程人员，不得按机器人初学者交流。详细画像和学习路线见 `docs/D38999_ASSEMBLY_PERSONAL_CAPABILITY_AND_LEARNING_ROADMAP_CN.md`。
- 先说机器人、三指手、连接器或仿真物理实际发生了什么，再说代码、状态码和路径。优先使用“传感器读数 → 误差 → 控制律 → 指令 → 实际运动 → 判据”的结构。
- 不单独用“扳手”指代 `wrench`；先写“三个方向的力和三个方向的力矩”。画面外形与仿真计算接触使用的形状必须分开表达。
- 用户提出反驳时，明确哪些成立、哪些仍缺条件，并给出传感器、运动学、物理或运行数据依据。

## 证据与结论边界

- 严格区分：程序正常结束、静态检查、离线验证、动态仿真、正式动态验收、真实硬件验证。退出码 0、`passed=true`、生成文件或日志里的 `PASS` 不能单独证明物理动作成功。
- 抓取结论至少同时检查真实接触、离桌高度与保持、滑移、桌面接触、穿透和安全限制；插入/锁紧结论还要结合位移、转角、轴向前进以及三个方向的力和三个方向的力矩。
- 仿真内部真实位姿、碰撞名称、接触点和接触法向只可按合同用于事后评价，不得进入正式在线控制。
- 每个结论明确标成“已验证事实”“根据数据推断”或“尚未验证”；历史结果不得写成当前运行结果。

## 当前任务与硬边界

- 当前主线以 `CURRENT_CONTEXT_CN.md` 指向的任务为准；截至本规则更新时为 `CARTS-GRASP-CROSS-OBJECT-V1`，状态 `IMPLEMENTING`，正式候选为空。
- 未同时具备正式根区间候选、完整手—环境连续防碰撞证书和经校准的非摩擦误差边界时，禁止选择正式候选或启动 Isaac。
- `dynamic_launch_allowed=false` 和 `hardware_authorized=false` 必须保持失败关闭；静态测试通过不改变这两个字段。
- 冻结模型、连接器身份、质量/质心/材料、8 N、0.30 N·m、50 µm、5 µm门限，以及 `D38999_VISUAL_COMPLETE_V1`、`D38999_LOCAL_CONTACT_REFERENCE_V1`、`MODEL_MAPPING.json` 均不得因清理、重构或测试而修改。
- 禁止磁吸、隐藏固定、运行后写物体位姿、在线真值控制、无限增力、批量盲扫参数和伪造动态证据。

## 读取、修改与运行纪律

- 每次编辑文件前，先用普通中文说明为什么改、服务哪个当前问题、不会改变哪些冻结边界；修改后先说明功能变化，再给文件、测试和原始状态码。
- 只读查看、审计、展示或状态查询不得顺带修改文件、启动 Isaac、改变门限或生成媒体。
- 普通静态工作只读 `CURRENT_CONTEXT_CN.md`、当前任务合同/最新快照和待改源码。仅在历史审计、状态冲突、正式 gate 变更或动态启动前，按任务 ID 定向读取 `PROJECT_CHARTER_CN.md`、`WORK_QUEUE.yaml`、`MASTER_STATE.json`、`TASK_GRAPH.yaml`、`BLOCKER_LEDGER.jsonl`、`GATE_LEDGER.csv`、`CURRENT_TASK.md` 和 `AUTONOMOUS_RESUME_CN.md`。
- 不得因上下文压缩凭记忆续作；以磁盘源码、配置、进程、日志时间和运行产物为准。控制状态可能滞后，动态前必须同时核对目标 PID/进程和最新产物时间。
- 诊断或动态运行前记录假设、唯一改变、预期结果和失败判据。单条命令最长 1500 秒；长运行至少每 60 秒报告存活。
- 状态只使用 `NOT_STARTED`、`IMPLEMENTING`、`STATIC_PASS`、`OFFLINE_PASS`、`DYNAMIC_PASS`、`PARKED`、`BLOCKED_EXTERNAL`；实现完成不得冒充物理通过。

## 版本控制与历史恢复

- 工作树中的既有修改和未跟踪文件均视为用户或并行任务资产；只暂存本任务明确修改的路径，不使用 `git reset --hard`、`git checkout --` 或宽泛清理命令。
- 清理前建立可读标签，清理后采用小而单一职责的本地提交；历史源码优先由 Git 标签/提交恢复，不在主分支重复保存解压树和大体积源码 ZIP。
- 未经用户明确要求不推送远端、不改写远端历史。提交前列出 staged 文件并复核未混入当前 CARTS/J599 在研文件。
- 外部交付归档必须包含入口、清单、大小、SHA-256 和独立完整性检查；归档通过不等于动态验收通过。
