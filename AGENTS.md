# kcgtest1 协作约定

本文件只保留稳定的协作和安全约束；用户最新要求优先于旧任务和流程习惯。

## 工作方式

- 涉及本项目实现、实验、运行状态或指令审计时，先读 `CURRENT_CONTEXT_CN.md`，再按当前问题
  读取必要源码、配置和原始记录；与项目无关的通用问答无需读取。历史只用于追溯，不单独恢复
  任务、参数或授权；同一任务已有授权范围内的明确续作无需重复确认。
- 当前上下文只保留一份有效验收、已验证阶段、当前阻塞、最后结果和恢复后的下一步。
  状态注明核验时间；恢复执行时核对所指运行的实际状态，不凭旧文档中的“运行中”继续等待。
  详细过程和失效结论移入历史并保留索引，不在活动入口逐轮追加。
- 优先现有入口和局部改动，先解决最早实际阻塞。保留最近完成有效物理阶段的源码、配置和
  证据作为比较基线；它不证明后续阶段可行，也不构成不可替换的路线。
- 更换求解后端、接触表示或核心控制方式前，说明它要解决的具体障碍，以及哪些已有结果
  需要重新验证。新路线尚未解决目标障碍时保留原基线，不因已投入时间自动替换主线。
  同时只运行一个主要整机实验；已有数据的独立离线分析可以继续。
- 实验保留一个主要假设和可归因的变量变化。结束后先核对实际结果，再决定继续原路线、
  做一个能区分原因的局部诊断，或冻结当前候选。没有新的可检验差别时不自动重跑；重试须有
  明确的瞬时故障或复现目的。冻结无效分支不等于结束任务，继续已授权且可独立推进的必要工作。
- 检查应对应当前改动和风险，不默认运行完整测试或计算全工程哈希。
  普通依赖按项目要求安装到现有环境，不为逃避安装另造替代实现。
- 实验同步保留必要原始数据和同回合影像。解决当前物理问题需要的资料核对可以先做；论文
  润色、重复整理图表和投稿格式不作为关键实验的前置条件。完整装配与学术交付分别按实际结果验收。
- 使用简洁中文，先说实际结果，再说证据、限制和下一步。区分已验证事实、推断和未知，
  不让用户选择内部编号或承担日常实现决策。
- 程序退出、测试通过、文件生成和 `PASS` 不代表抓取成功。物理结论检查实际接触、
  离桌、抬升、保持、滑移、碰撞与受力；仿真结果不能写成硬件保证。

## 安全与资产

- 工程为 simulation-only，`hardware_authorized=false`。动态动作须符合当前授权及实际使用的
  控制配置；文档整理不产生动态授权。硬件动作需用户另行明确授权并满足硬件门。
- 模型、连接器身份、质量/质心/惯量、材料和被动关节的原始事实保留，不因清理或重构修改。
  用户明确冻结的限制和已验证安全边界保持不变，不为制造成功放宽。
- 历史研究参考值不得冒充厂家额定或用户要求。修正仿真参考值前核实信号含义、来源和用途，
  仅在已有授权内作有界调整，记录实际配置，保留原始测量及独立停止条件。来源未知不等于没有
  限制，也不允许任意增力；替代接触表示应标明依据与近似，不能当成原件已标定事实。
- 仿真对象和接触真值仅用于运行后评价，不进入在线控制。
  禁止磁吸、隐藏固定、启动后改写物体位姿、无限增力、批量盲扫参数和伪造证据。
- 原始实验数据和失败证据保留；`artifacts/` 被 Git 忽略不代表可以删除。
  过期说明可移入历史归档，不再作为活动入口。
- 现有未提交修改和未跟踪文件属于用户资产，只修改本次任务涉及的内容。
  不做宽泛清理、强制回退或强制推送；如需暂存，仅限本任务路径。未经要求不推送远端。
