# 八小时航天电连接器装配自主执行规则

## D38999 自主动态装配 V2 人工覆盖（2026-08-18T07:53:44Z起）

- 当前总任务为 `D38999-AUTONOMOUS-DYNAMIC-CLOSEOUT-V2`，当前节点为 `DYN-A1-EVENT-ONSET-CALIBRATION-V2`；旧 A1、事件起始校准和 B 的 `PARKED` 状态只作为不可覆盖历史证据，不再阻止 V2 执行。
- V2 顺序为 A1事件校准、A2名义插入、B抓取抬升、C抓后RGB-D、D视觉力觉插入、E螺母旋紧、F完整HOME到DONE、G随机化验证；只有真实 Isaac 动态证据可标记 `DYNAMIC_PASS`。
- V2 普通技术状态使用 `INVESTIGATING`、`ROOTCAUSE_IDENTIFIED`、`TARGETED_FIX_IN_PROGRESS`、`VALIDATING`；不得因表层失败字符串重复而停车。每个具体假设最多一次区分性诊断、一次结构性修正、最多三次同配置复验，失败后必须转向因果机制不同的新假设。
- V2 唯一停车状态为 `PARKED_EXTERNAL`，仅限外部必需资源确实缺失、可重复基础设施故障且代码无法修复、冻结合同直接矛盾、或定量证明唯一剩余方案必须改变冻结合同/基本几何/质量/材料/正式门限。
- 永久冻结主合同、连接器身份、shell 25、61触点、N键位、质量、质心、材料类别、摩擦基本假设、七事件位置、8牛/0.30牛米/50微米/5微米门限；`D38999_VISUAL_COMPLETE_V1`、`D38999_LOCAL_CONTACT_REFERENCE_V1` 和 `MODEL_MAPPING.json` 必须保持字节不变。
- 允许从主合同、唯一 V2 授权覆盖文件和同一生成器一次性、确定性地产生 `D38999_ASSEMBLY_CONTROL_V1`；禁止手工补丁最终 USD、磁吸、隐藏固定、运行后对象位姿写入、在线真值位姿/碰撞名称/接触法向/事件真值、语义真值视觉、无限增力和参数网格搜索。
- 本任务只由当前主 Agent 执行，不得分派给并行子 Agent。单条命令仍最长1500秒；动态长运行每60秒记录存活，每个进程完成后立即更新状态，每30分钟更新动态进度。
- 每次修改、运行或任务切换前，除既有控制文件外，还必须读取 `artifacts/agent_control/AUTONOMOUS_RESUME_CN.md`；压缩后必须以磁盘状态和 V2 历史索引恢复，不得凭记忆续作。
- 本节是最新直接人工授权，覆盖下文旧任务的一诊断一修正即停车、禁止重访A1/B、窗口截止和 `next task=null` 限制；不覆盖安全、真值隔离和历史证据只读边界。

## 事件起始校准与真实螺母抓取人工覆盖（2026-08-18T04:41:34Z起）

- 新任务 `DYN-A1-EVENT-ONSET-CALIBRATION` 获准重新访问 A1；依据是上一轮修正后仍有键/键槽50组、连续壳64组、金属止挡57组，共171组不同的提前接触。
- 5.500毫米初始化时 `LoosePlug ↔ FixedReceptacle` 装配接触必须为0，`world.reset()` 后基准误差仍须不超过50微米。
- 只允许从权威七事件位置反算并一次性修正装配控制模型生成器的碰撞代理；允许重新生成 `D38999_ASSEMBLY_CONTROL_V1`。
- 禁止修改主合同、`D38999_VISUAL_COMPLETE_V1`、`D38999_LOCAL_CONTACT_REFERENCE_V1`、权威尺寸、质量、重心、材料来源、七事件位置和安全门限。
- 只允许一个小型事件起始探针；禁止参数扫描。一次事件起始检查后，若满足门限，再运行三个独立A1正式验证进程。
- 人工已授权锁紧螺母局部z=9–29毫米、外半径24毫米的既有后部机器人抓持带作为 `INITIAL_PICK_AND_TRANSPORT_GRASP_REGION`；不得增加新后壳。
- 允许装配控制模型生成真实的 `coupling_nut_grasp_collision`，仅供手指与螺母外部接触；必须正确过滤插头内部假自碰撞。
- A1本轮仍失败则停车并立即转真实B抓取；B至少三个独立Isaac进程完成2/10/40毫米抬升与2秒保持后才可称 `DYNAMIC_PASS`。
- 本节由新的人工直接证据授权，覆盖下文“A1修正预算已耗尽”和“锁紧螺母抓取未授权”的旧状态，但不放宽任何安全或真值边界。

## 动态红色门阶段最高优先级覆盖（2026-08-18起）

- 当前唯一主线是把真实 Isaac 动态链从 `HOME` 向 `DONE` 推进；禁止继续提前建设无关框架、监控器、报告器或离线占位模块。
- 有效工作时间至少 60% 用于真实 Isaac 物理步进、动态日志分析、由动态失败驱动的最小修改和真实复验。
- 当前任务 `DYN-A1-INIT-ROOTCAUSE` 最长 90 分钟；其他普通问题最长 60 分钟。每项仍只允许一次诊断、一次修正、一次验证。
- `contactOffset/restOffset` 遗漏假设已经实验否定，禁止重试；禁止预补偿 226 微米、reset 后写位姿或放宽 50 微米门限。
- A1 必须先记录 T0 至 T7，并定位偏差首次出现的相邻阶段；未定位前禁止修改模型或控制代码。
- A1 一次修正后仍失败即 `PARKED`，立即转 `DYN-B1-GRASP-SEMANTIC`；FoundationPose 不是当前关键路径。
- 每 30 分钟覆盖更新 `DYNAMIC_PROGRESS_CN.md`；连续两个窗口 Isaac 物理步进运行均为 0 时，当前任务自动停车并转可动态验证节点。
- 本节覆盖下方旧八小时窗口的 45 分钟预算、离线队列优先级和窗口结束停止规则；安全门限、真值防火墙、冻结资产及禁止新几何候选继续有效。

本文件对本仓库及所有子目录生效。若更深层目录存在 `AGENTS.md`，其规则只能进一步收紧，不能放宽本文件约束。

1. 所有解释使用中文；文件名、程序名和原始参数名除外。
2. 每次切换任务、修改或运行前，必须重新读取：
   - `artifacts/agent_control/PROJECT_CHARTER_CN.md`
   - `artifacts/agent_control/WORK_QUEUE.yaml`
   - `artifacts/agent_control/MASTER_STATE.json`
   - `artifacts/agent_control/BLOCKER_LEDGER.jsonl`
   - `artifacts/agent_control/GATE_LEDGER.csv`
   - 兼容历史控制面时还要读取 `TASK_GRAPH.yaml` 与 `CURRENT_TASK.md`。
3. 普通技术问题最多 45 分钟，只允许一次明确诊断、一次针对性修改和一次验证；同一失败再次出现即 `PARKED`，不得结束整个八小时任务。
4. 同一 `PARKED` 问题仅可因新直接证据重新访问一次；第二次仍失败则标记 `PARKED_FINAL_FOR_THIS_WINDOW`，本窗口禁止第三次处理。
5. 单条命令最长 25 分钟（1500 秒），不得绕过超时守卫。长运行每 60 秒记录存活状态，每 10 分钟覆盖更新中文状态。
6. 连续 20 分钟无新数值、代码、测试或根因排除，只允许 10 分钟收尾后 `PARKED` 并切换队列。
7. 禁止偏离 `WORK_QUEUE.yaml` 当前任务；局部任务停止后必须选择不依赖该阻塞的最高优先级节点继续。
8. 禁止因上下文压缩按记忆猜测项目状态；必须以磁盘状态、源码、配置和运行产物为准。
9. `CURRENT_STATUS_CN.md` 只保留当前状态且不超过 60 行；覆盖前把旧状态追加到 `STATUS_HISTORY.jsonl`。
10. 任何诊断或动态运行前必须落盘：假设编号、唯一改变、预期结果、失败判据。
11. 任务状态只使用 `NOT_STARTED`、`IMPLEMENTING`、`STATIC_PASS`、`OFFLINE_PASS`、`DYNAMIC_PASS`、`PARKED`、`BLOCKED_EXTERNAL`；实现完成不得冒充动态或物理通过。
12. `TASK-R12-005`、`TASK-R12-006B`、`TASK-R12-006C` 不得重跑；006D、006E 不得执行。高精细资产、局部候选和失败证据只读。
13. 禁止新连接器几何候选、4392 组全组合微碰撞、修改主合同、放宽门限、提高 8 N 或 0.30 N·m、磁吸、运行后位姿写入、真实位姿控制、碰撞名称或接触法向控制、假数据和批量盲扫参数。
14. 允许在上游动态阻塞时继续机械臂、三指抓取、视觉、插入、锁紧和总状态机的接口实现、静态测试与离线测试；正式动态运行必须满足各自依赖，不得伪造上游通过。
15. 每 30 分钟至少留下一个状态变化、程序/测试结果、动态试验、根因排除、PARKED 记录或下游接口成果；否则当前任务立即停车并切换。
16. 八小时窗口内只有不可恢复破坏、系统权限或硬件安全风险可停止具体动作；仍应 PARK 该节点并继续其他安全节点。窗口结束统一生成最终报告和审查包，状态为 `EIGHT_HOUR_WINDOW_COMPLETE`，不是 `BLOCKED`。
