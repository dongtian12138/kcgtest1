# kcgtest1 当前轻量上下文

> 快照时间：2026-08-25T09:11:20Z
> 当前分支：`carts-grasp-full-palm-search-20260825`
> 原始运行产物和哈希优先于本摘要。

## 一句话状态

新的完整掌面构型路线已经开始：保留旧五构型失败证据，先核对可见模型、碰撞模型和桌面几何是否一致，再在 `f1j1` 全合法闭区间的 91 个固定角度上做六维手掌位姿与三指预构型联合搜索；当前尚无任务合格候选，Isaac 尚未启动。

## 六行恢复摘要

- 最初目标：A/B 用同一方法和主要参数，由三块真实指腹接触后离桌、抬升至少 50 mm、保持至少 2 s，且无未授权穿透。
- 当前已完成：官方 GraspGenX/GPU 正对照、旧五构型双对象生成、完整控制步闭合扫掠，以及新路线分支和只读监督审查。
- 当前真实物理结果：旧路线仅有离线运动学与网格距离；A 无三指闭合幸存者，B 有 2 个但闭合扫桌；连接器未在 Isaac 中运动。
- 当前唯一主要阻塞：尚未证明旧 B 候选在可见网格与注册碰撞网格中指向同一物理碰撞，也尚未完成 91 角、精确指腹表面法向和预构型联合搜索。
- 最近用户指令：`METHOD_CORRECTION + FULL_PALM_CONFIGURATION_SEARCH + VISUAL_TRUTH_DIAGNOSIS + AUTONOMOUS_ISAAC_VALIDATION`；不是硬件授权，不降低碰撞、力量、50 mm、2 s 或真值边界。
- 下一步：完成旧 B 候选和掌面角动画的视觉真实性核对，然后修正精确接触几何并启动两对象同参数的级联搜索。

## 当前权威状态

- 状态：`IMPLEMENTING`
- 里程碑：`FULL_PALM_VISUAL_TRUTH_AND_CONFIGURATION_AUDIT`
- 窗口：`2026-08-25T09:11:20Z` 至 `2026-08-25T21:11:20Z`，不自动延长。
- `hardware_authorized=false`；旧正式动态门关闭；研究型与正式动态均为 false。
- 官方 GraspGenX：commit `b9429097728cb1c430dd78b92edf17ba318aad03`；checkpoint 树 SHA-256 `f301fbd…`。
- 运行时：Python `3.10.12`、PyTorch `2.7.0+cu128`、RTX 5070 Ti `sm_120`。

## 新方法固定事实

- 掌面构型变量：主动 `f1j1`；`f3j1` 以 `+1` mimic。因两关节局部轴在 handbase 中物理反向，同号角度才是正确的对向展开。
- 离散覆盖：`np.linspace(0, 1.57, 91)`；锚点索引 0/30/45/60/90 对应约 0°/30°/45°/60°/89.95°。
- 该口径是“完整 URDF 闭区间 91 点离散覆盖 + 有界局部精修”，不得声称穷尽连续构型空间。
- 每角 GraspGenX 原始目标 128、分数保留 64、精确闭合最多 8；预算按级联执行，禁止形成笛卡尔积。
- 三指预构型固定为每指 phase `{0.0,0.1,0.2}`，共 27 种；张开指节碰桌只有在 27 种均无法安全接近时才拒绝。
- 精修每对象跨角度最多 64 个种子；每种子 Stage A+B 合计最多 300 次评价。直接候选通过全部门时立即进入 Isaac，不等待精修。
- 指腹接触方向来自最近允许表面的真实三角面法向；允许三角面集合为空时显式失败关闭。
- 旧五描述器和旧轴对称结果只作基线，不覆盖、不改写。

## 动态入口

候选必须依次通过：可见/碰撞身份一致、张开/接近/完整逐指闭合安全、三块真实指腹允许接触、12 N 名义任务、机械臂 IK、接近与携带抬升路径，才允许研究型 Isaac。当前尚未产生新路线候选，所以门保持关闭。

## 冻结边界

- 两对象身份、完整网格、质量、质心、惯量、摩擦和误差范围不变。
- 12 N 是仿真研究操作上限；旧 8 N 只作消融，不扫描力量。
- `3 rad/s`、`0.18 rad/s`、`1/120 s`、`0.0015 rad/周期`、50 mm、2 s 不变。
- 不使用磁吸、隐藏固定、物体位姿写入或在线对象/接触/PhysX 真值。
- 不修改 H102、旧连续碰撞器或真实手 URDF。

## 固定恢复读取顺序

1. `AGENTS.md`
2. `docs/carts_v2/NORTH_STAR_CN.md`
3. `docs/carts_v2/FULL_PALM_SEARCH_PLAN_CN.md`
4. 本文件
5. `docs/carts_v2/DECISIONS_CN.md` 最新部分
6. `artifacts/carts_v2/STATE.json`
7. `artifacts/carts_v2/full_palm_search/SEARCH_MANIFEST.json`
8. `git status`
9. `git log -8`
10. 当前唯一阻塞相关源码与原始产物

## 动作纪律

- 结论按“物理现象 → 数据 → 证据边界 → 下一步”表达；图片、退出码、覆盖率和测试不冒充抓取。
- Isaac 前重新核对源码、配置、模型、候选、GPU、预检查、物理门和真值隔离。
- 只提交本任务文件；第三方仓库、权重、缓存和逐周期大 trace 不提交。
