# kcgtest1 当前轻量上下文

> 快照时间：2026-08-24T22:47:00Z
> 当前分支：`carts-grasp-graspgenx-route1-20260824`
> 本文件只做恢复路由；物理结论以原始运行产物为准。

## 一句话状态

官方 GraspGenX 与 5 个 KCG 手描述器均已真实运行；官方默认 A/B 各 128 个单一手型提案经真实张开整手—有限桌面粗筛后分别保留 29/23 个，但多描述器覆盖门仍失败，闭合、任务、IK 和 Isaac 均未运行。

## 六行恢复摘要

- 最初目标：两个 D38999/J599 对象同一方法与主要参数，三根真实指腹接触、离桌、抬升至少 50 mm、保持至少 2 s，无未授权穿透。
- 当前已完成：官方样例与官方手同对象正对照、5 个对象无关 KCG 描述器、A/B 官方默认六维提案，以及使用 9 个登记手部 link、2000 个手表面点和 8160 个有限桌面点的官方场景粗筛。
- 当前实际结果：没有动态动作；A/B 均从 128 个单一描述器提案开始，场景粗筛分别保留 29/23 个，但覆盖门仍关闭，闭合、任务、IK 和 Isaac 未运行。旧混合参数诊断仍单独保留为 A `256→3→0`、B `256→0`。
- 当前唯一主要阻塞：除 `kcg_3f_preshape_02` 外四个描述器有原始提案却没有 open/half 可见提案；必须区分描述器坐标、条件箱尺度或姿态分布原因，不能靠调对象位置或阈值绕过。
- 最近用户指令：`METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION`，不是硬件授权或降低成功标准。
- 下一步：对现有固定输入做一次不改参数的单描述器可见性根因诊断；若证实坐标/schema 接线错误则最小修复，否则诚实收口为模型—描述器适配阻塞。

## 当前权威字段

- 状态：`IMPLEMENTING`
- 当前里程碑：`GRASPGENX_ROUTE1_SINGLE_DESCRIPTOR_VISIBILITY_ROOT_CAUSE_REVIEW`
- 窗口：`GRASPGENX_ROUTE1_12H_UNATTENDED`
- 开始：`2026-08-24T14:46:08Z`
- 硬截止：`2026-08-25T02:46:08Z`
- 官方 GraspGenX：本地隔离克隆，commit `b9429097728cb1c430dd78b92edf17ba318aad03`
- 隔离运行时：Python `3.10.12`、PyTorch `2.7.0+cu128`、torchvision `0.22.0+cu128`、`sm_120` 实际 GPU 张量通过。
- 官方正对照：`robotiq_3f + banana` 返回 `20/20` 个有限位姿；模型加载 `1.596 s`、推理 `0.522 s`、分数 `0.18669–0.98670`；只证明官方推理链联通。
- KCG 描述器：5 个；清单 SHA-256 `694bc748…`；虚拟基座到允许 PAD 前缘深度 `0.11195–0.11647 m`。
- 描述器闭合上限：按手型为 `0.521459–0.690987`；共检查 2644 个同步闭合状态，最大独立关节步长 `0.0015 rad`，最后保留状态均无非相邻连杆自碰。
- 对象输入：A `88,078/145,588` 顶点/面；B `343,520/687,036` 顶点/面；均由登记网格原样导出为米制 NPZ。
- ROI 点云：A SHA-256 `1e2eec06…`，B `d2a29907…`；允许面身份分别为 `10390/7269` 个三角面，完整对象网格仍用于所有碰撞与闭合检查。
- 当前官方默认组合：A `547→295→128`、B `589→374→128`；两对象最终 128 个都来自 `kcg_3f_preshape_02`，多描述器覆盖失败。旧混合参数的 `17480→约15700→640→256` 保留为诊断对照。
- 官方场景粗筛：同一 `0.02 m / 2000 手表面点 / 8160 有限桌面点` 规则下，A `128→29`、B `128→23`；9 个登记手 link 均参与，目标连接器点云被排除。它只能提前拒绝靠桌提案，不是三角网格或全路径安全证明。
- 旧混合参数 A 诊断：`256→3` 个离线闭合预测存活；2 个预测路径中途扫桌、1 个预抓/端点穿桌；任务评价 `0`、可执行 `0`。
- 旧混合参数 B 诊断：`256→0` 个离线闭合预测存活；首因分布为第一指无允许接触预测 164、禁抓面先接触预测 51、第二指无接触预测 39、第三指无接触预测 2；桌面/FCL/任务均未到达。
- 旧混合参数同源重放：HEAD `db540b3`；A result SHA `3c94f650…`，B result SHA `844ab07b…`；两对象 task-evaluated 均为 0，因此 12 N 名义门和正式鲁棒门是未到达，不是评价后失败。
- 参考运行时复现：`/usr/bin/python3` 下 A/B 候选 CSV 和 coverage JSON 与冻结证据逐字节一致；删除仅含耗时的顶层 `timings_s` 后 result JSON 也逐字节一致。换项目 `.venv` 后部分数值字节变化，故运行时版本必须绑定。
- 坐标审计：对象/ROI 尺度均约 47–48 mm；最终 handbase 约 ±0.45 m 来自真实 PAD 距手腕约 406–409 mm，不是米/毫米或变换链错误。
- 已审计描述器尺度对照：KCG open 条件箱 `extents[1]` 为 `0.117526–0.305499 m`；官方 32 个程序化配置中 open/half-open 同分量最大 `0.067305 m`，26 个运行时描述器 open 最大 `0.060 m`。这是配置箱对照，不是整手点云跨度或完整训练分布，也不证明失败因果。
- 官方手同对象对照：`robotiq_3f` 对 A/B 完整点云各得到 `256/256` 个有限 diffusion 位姿；这排除了对象点云让官方学习分支整体失效，但不是 KCG 抓取或唯一根因证明。
- 旧 B 基线复核：`384→110` 个三指闭合→`6` 个桌面安全→`0` 个整手 FCL 安全；六项均在第一指闭合 step `269–272` 由 `f1Link3` 非指腹表面碰物。
- 研究动态门：`allowed=false`；当前覆盖门失败，29/23 个粗筛保留项尚未经过闭合、完整网格路径、任务或 IK，不能启动 Isaac。
- 未来动态评价边界：旧配置的 `maximum_table_penetration_m=0.002` 尚无来源且本路线未执行；未来任何 Isaac 启动前必须先审查分阶段容差，不能用它证明“无穿透”。
- 旧正式动态门：保持关闭
- 正式动态：`false`
- 研究型动态：`false`
- 真实硬件：`hardware_authorized=false`
- 主研究力量上限：`12 N` 仿真操作上限；旧 `8 N` 仅作消融；不得扫描增力
- 动态目标：三指允许指腹接触、离桌、50 mm、至少 2 s、无未授权穿透

## 冻结事实与复用边界

- 两个对象身份、登记网格、质量、质心、惯量、摩擦和误差范围不变。
- 3 rad/s 关节安全限速、0.18 rad/s 手指目标速度、1/120 s 周期、0.0015 rad/周期、50 mm 和 2 s 不变。
- 在线控制不得读取对象位姿、接触名称、精确接触点/法向或 PhysX 真值；这些只供事后评价。
- 旧轴对称生成器、旧 Top-3 和 `candidate_11/33` 只作历史消融，不是本轮生产候选。
- 旧 H102 与大型连续碰撞实现冻结；Top-3 只通过现有薄适配器调用。
- 复用 V2 真实手/PAD/对象模型、完整顺序闭合扫掠、任务力学、selector、bounded IK、Isaac 控制器和评价器。
- GraspGenX 隔离环境只输出版本化文件，不污染 Isaac Python；第三方仓库、权重和缓存不提交。

## 证据边界

- 官方样例推理只证明模型环境联通，不证明 KCG 抓取可行。
- 六维覆盖只证明搜索姿态族变宽，不证明不碰撞或抓得住。
- `research_executable_candidate` 必须通过完整几何路径、三指名义接触、IK/路径和 12 N 名义任务门，才可进入研究型 Isaac。
- `NOMINAL_RESEARCH_DYNAMIC_PASS` 必须由物理时间、三指允许接触、离桌、50 mm、2 s、滑移、桌面接触和穿透共同判定。
- 正式动态还需预登记误差、严格证据、重复、扰动和双对象同参数；本轮旧正式证据线保持失败关闭。

## 固定恢复读取顺序

1. `AGENTS.md`
2. `docs/carts_v2/NORTH_STAR_CN.md`
3. `docs/carts_v2/GRASPGENX_ROUTE1_PLAN_CN.md`
4. 本文件
5. `docs/carts_v2/DECISIONS_CN.md` 最新部分
6. `artifacts/carts_v2/STATE.json`
7. `artifacts/carts_v2/graspgenx/INTEGRATION_MANIFEST.json`
8. `git status`
9. `git log -8`
10. 当前唯一阻塞相关源码

## 当前直接路径

- 路线计划：`docs/carts_v2/GRASPGENX_ROUTE1_PLAN_CN.md`
- 事实报告：`docs/carts_v2/GRASPGENX_FINAL_REPORT_CN.md`
- 决策：`docs/carts_v2/DECISIONS_CN.md`
- 机器状态：`artifacts/carts_v2/STATE.json`
- 集成清单：`artifacts/carts_v2/graspgenx/INTEGRATION_MANIFEST.json`
- V2 抓取源码：`src/kcg_connector/kcg_connector/grasp/carts_v2/`
- V2 Isaac：`src/kcg_connector/isaac/carts_v2/`

## 动作前检查

- 超过 30 分钟的工作先在路线计划写物理问题、唯一改变量、观察量、失败判据和时间上限。
- Isaac 前核对候选、源码、配置、模型、GPU 资源、预检查与真值隔离；研究门和正式门分开。
- 每 45～60 分钟及大里程碑更新本文件、计划、状态和集成清单；只提交本任务路径。
