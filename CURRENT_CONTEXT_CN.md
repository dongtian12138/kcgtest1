# kcgtest1 当前上下文

## 当前真实物理结果

无指甲三指手在 Isaac Sim 中对当前公开规格连接器和
TE/DEUTSCH D38999/26FJ35PN 均完成了 V4 自动名义抓取：三块完整蓝色指腹合法接触，
连接器离桌、抬升超过 50 mm、保持 2 s，错误接触为 0。

| 型号与抓法 | 三块完整指腹 | 离桌 | 抬升 | 保持 | 滑移 | 姿态变化 | 峰值手指力矩 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 当前 baseline | 是 | 是 | 55.732 mm | 2.000 s | 0.407 mm | 4.422° | 0.656 N·m |
| 当前 V4 自动 | 是 | 是 | 55.585 mm | 2.000 s | 0.461 mm | 0.773° | 0.476 N·m |
| TE baseline | 是 | 是 | 55.379 mm | 2.000 s | 0.430 mm | 0.078° | 0.644 N·m |
| TE V4 自动 | 是 | 是 | 54.556 mm | 2.000 s | 0.515 mm | 0.172° | 0.729 N·m |

原始 V4 自动抓法评价：

- 当前：
  `artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/algorithm_nominal_comparison_v4/current_auto/grasp_lift_run02/evaluation.json`
- TE：
  `artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/algorithm_nominal_comparison_v4/te_auto/grasp_lift_run02/evaluation.json`

全部结果均为 simulation-only；`formal_dynamic_pass=false`，`hardware_authorized=false`。

## 已冻结的名义算法

名义算法为 `COMPLETE_AXIS_ALIGNED_FULL_PAD_GRID_V4`。它只从每个连接器自身 CAD、质量/质心、
完整无指甲手模型和共同物理上限生成抓法，不输入型号专用世界位姿、接触坐标或手指角度。

- 合法区域和碰撞使用整块蓝色指腹，不是两个三角片；
- 每块检测到的完整指腹接触斑在任务受力 LP 中贡献一个合力点，不允许独立扭转力矩；
- 手掌关节 17 点，轴向转角 24 点，CAD 轴向截面为当前 34 点、TE 33 点；
- 横向偏移和倾角在本版固定为 0；
- 摩擦锥使用对象合同下界 μ=0.45 的八边保守近似；
- 每块指腹法向力上限 8 N，闭合关节保护上限 0.90 N·m；
- 主分数是保持和竖直抬升任务承载倍率的较小值；
- 0.075 rad 是最大预紧行程，动态控制逐指达到 LP 所需力矩后停止闭合；
- 碰撞、关节限位、完整指腹、错误表面、力矩和固定路径均为硬约束。

当前 13,872 个成员和 TE 13,464 个成员都已得到声明的离散评价。允许的最优性结论只覆盖该
轴对齐 `G_delta`，不覆盖连续空间、横移、倾角或连续时间碰撞证明。TE 已参与方法修正，
所以结果是跨型号验证，不是严格盲测。

完整公式、文献、CAD 图、质量分布、动态图片和定量对比：

`artifacts/kcg_connector/isaac/cad_robust_grasp_goal_20260828/algorithm_nominal_comparison_v4/finite_gdelta_study_v4/README_CN.md`

## 当前科学问题与唯一下一动作

名义算法已经解决“能否自动产生双型号可执行抓法”。下一个未知量是：预先声明的位姿、摩擦、
质量、质心和关节执行误差中，哪个单变量最先破坏合法接触、离桌、50 mm 或 2 s。

算法冻结后，先把扰动来源明确分成两类：

- 物理合同：摩擦系数 [0.45, 0.55]，但只是共享仿真材料区间，不是硬件标定；
- 研究假设：横向位置 ±0.17 mm、轴向转角 ±2°、质量 ±5%、质心 x ±1 mm、
  第三指关节目标 ±0.004 rad；这些都不是已标定的真实传感器误差。

首个开发扰动只改变连接器横向位置到 +0.17 mm。它有此前直接敏感性线索，并且最靠近
“三根指腹还能否接触”这一动作链起点。若成功，再测 -0.17 mm；若失败，只根据最早物理失败
修改一个通用因素。不得扩大搜索、批量盲扫或把先导数据写成最终鲁棒性结论。

## 仍未完成

- 冻结算法后的单变量开发扰动与少量组合扰动；
- 一个由真实失败原因支持的通用鲁棒改进；
- 再次冻结后，用未参与调参的工况在两个型号上做最终评价；
- 优化前后在相同工况的动态对比、最坏工况、失败反例、图片、公式和复现命令。

在这些实际动态结果完成前不得标记目标完成。
