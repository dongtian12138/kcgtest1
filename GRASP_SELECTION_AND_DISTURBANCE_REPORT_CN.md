# 三指抓取参数选择与持续抗扰实验报告

> 日期：2026-09-03。范围：Isaac Sim、固定确定性场景、simulation-only。
> 本报告中的“通过”仅表示对应有限仿真条件通过，不是实物额定能力。

## 1. 这轮实际得到的结果

最终保留的名义抓取参数为：

- 三指只接触联接螺母；
- 手掌绕插头轴线 `0°`；
- 抓取位置为后部 `-19 mm`；
- 1、3号指共同掌面布局角 `70°`；
- 三指采用等法向目标；
- 静态承载安全系数 `4.0×`；
- 三根闭合关节目标力矩为
  `0.290010 / 0.307879 / 0.290018 N·m`。

该方案在独立重复中均完成三指合法接触、离桌、约 `55.94 mm` 抬升和
`2.0 s` 保持。名义加载阶段，插头本体相对手的最大平移约 `0.0387 mm`，
插合轴线最大变化约 `0.145°`，三指全程只接触联接螺母。

但完整绕轴姿态没有静止：名义保持最后 `1 s` 中，本体键位仍以约
`0.205°/s` 单向漂移。这个方案可称为“位置与插合轴线稳定的抓取”，不能称为
“本体六维姿态完全冻结的抓取”。

## 2. 力矩安全系数比较

控制条件固定为真实参考质量 `62.4597 g`、摩擦系数 `0.45`、空载螺母等效阻力
`0.020 N·m`、`75°`布局、后部 `-19 mm`和手掌绕轴 `0°`，只改变静态承载倍数。

| 倍数 | 实际结果 | 本体最大平移 | 轴线变化 | 螺母—本体绕轴范围 |
|---:|---|---:|---:|---:|
| 2.0 | 掉落，只抬起 5.68 mm | 56.49 mm | 8.57° | 1.99° |
| 2.5 | 抓取成功 | 0.853 mm | 1.916° | 2.68° |
| 3.0 | 抓取成功 | 0.625 mm | 1.340° | 3.25° |
| 3.5 | 抓取成功 | 0.583 mm | 1.235° | 6.36° |
| 4.0 | 抓取成功 | 0.664 mm | 1.425° | 5.91° |
| 4.5 | 抓取成功 | 0.031 mm | 0.138° | 10.71° |
| 5.0 | 抓取成功 | 0.025 mm | 0.144° | 12.06° |
| 6.0 | 抓取成功 | 0.019 mm | 0.148° | 14.85° |
| 8.0 | 抓取成功 | 0.022 mm | 0.139° | 20.48° |
| 10.0 | 2号指无法达到目标力矩，未抬升 | — | — | — |

结论不是“力越大越好”。`4.5×`是75°布局首次进入低平移、低轴线变化区的档位；
继续增力几乎不再改善轴线，却持续增大螺母—本体绕轴转动，并压缩执行器余量。

![力矩倍数对比](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_force_factor_sweep_v1/force_factor_comparison.png)

![力矩倍数最终状态](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_force_factor_sweep_v1/force_factor_final_state_montage.png)

## 3. 掌面布局角比较

固定 `4.5×`物理夹力；每个布局根据自身CAD力臂重新换算关节力矩，因而比较的不是
“同一关节力矩”，而是“同一理论法向夹力”。插头、桌面和抓取高度没有自动移动。

| 布局角 | 本体最大平移 | 轴线变化 | 螺母—本体绕轴范围 |
|---:|---:|---:|---:|
| 30° | 0.142 mm | 0.151° | 8.27° |
| 45° | 0.142 mm | 0.170° | 11.13° |
| 60° | 0.076 mm | 0.279° | 4.39° |
| **70°** | **0.036 mm** | **0.141°** | **5.00°** |
| 75° | 0.031 mm | 0.138° | 10.71° |
| 80° | 1.002 mm | 2.277° | 1.55° |
| 85.94° | 1.817 mm | 3.291° | 6.30° |

`70°`相对`75°`只损失约 `0.005 mm / 0.003°`，却把内部绕轴转动减少一半以上；
它也在三项上同时优于30°和45°，所以选70°作为后续方案。

![布局角对比](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_layout_sweep_v1/layout_comparison.png)

## 4. 70°布局的局部夹力复核

70°布局下，`4.0× / 4.5× / 5.0×`均成功：

| 倍数 | 本体最大平移 | 轴线变化 | 螺母—本体绕轴范围 |
|---:|---:|---:|---:|
| **4.0** | **0.039 mm** | **0.145°** | **4.55°** |
| 4.5 | 0.036 mm | 0.141° | 5.00° |
| 5.0 | 0.034 mm | 0.138° | 5.57° |

三档处于同一稳定区。`4.0×`与更高档的位置/轴线差别很小，内部绕轴转动更小，
且执行器余量更大，因此保留最低的`4.0×`。

## 5. 三指力重新分配的否定结果

等法向基线为每指约 `1.815 N`。CAD静态配平在保持总法向力不变的前提下，将其改为
约 `2.069 / 1.368 / 2.007 N`，对应关节目标
`0.331 / 0.232 / 0.321 N·m`。

动态结果反而全面变差：

| 方法 | 本体最大平移 | 轴线变化 | 螺母—本体绕轴范围 |
|---|---:|---:|---:|
| **等法向** | **0.0387 mm** | **0.145°** | **4.55°** |
| CAD静态配平 | 0.0464 mm | 0.186° | 5.92° |

原因是点接触静态模型没有包含真实接触斑块、指腹滚动和摩擦力的动态自分配。运行后接触
数据也显示，目标法向力并不等于PhysX接触斑块的实际合力。因此不保留该配平方案，也不继续
围绕它调参。

![力分配对比](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_force_distribution_v1/force_distribution_comparison.png)

## 6. 持续扰动如何施加

本轮重新实现了测试输入：

- 力施加到分体插头**本体的CAD质心**，不再施加到刚体原点；
- 扰动开始时读取一次本体姿态，建立并冻结插头任务坐标方向；
- 每个物理步只用本体位姿把已知局部质心换算到世界坐标，不将该真值返回抓取控制器；
- 时序为 `0.2 s` 最小加加速度上升、`1.0 s` 恒定平台、`0.2 s`下降和`2.0 s`恢复；
- 每个方向独立reset；
- 使用同样时序的零输入运行逐帧扣除自然漂移；
- 力上限仍为 `3.0400615 N`，力矩上限仍为 `0.0540690202 N·m`，没有提高安全门。

共分析65个有载荷运行；全部输入轮廓验证通过，逐步质心施力点换算误差为0，全部数值有限。

分体螺母当前不能提供旧评价器要求的“对象材料表面投影点”，所以这些扰动评价中的旧综合字段
`core_condition_pass`会统一为`false`。本报告没有把该字段改写成通过；三指接触极限直接依据
每一物理步的三指接触计数、接触对象路径，以及全程“三指只接触CouplingNut、没有接触Body”
的分体接触审计。因而应使用本报告的精度/接触两类结果，不能把旧综合字段解释成测试输入失败。

## 7. 抗扰极限结果

精度通过定义为：相对零输入的峰值平移 `<0.5 mm`、轴线变化 `<0.5°`且键位变化
`<0.5°`。接触通过定义为：三指在所有扰动采样中保持合法螺母接触、没有安全中止且数值有限。
下表给的是“最后一个实测通过值—第一个实测失败值”，不是区间内连续认证。

| 任务方向 | 精度通过—失败区间 | 三指接触通过—失败区间 |
|---|---:|---:|
| +X 横向 | 0.766—0.842 N | 2.144—2.450 N |
| -X 横向 | 1.378—1.531 N | 2.297—2.450 N |
| +Y 横向 | 1.838—1.991 N | 2.297—2.450 N |
| **-Y 横向** | **0.498—0.536 N** | 1.531—1.838 N |
| +Z 拔出 | 1.838—1.991 N | **至少3.040 N，未测到失触** |
| -Z 插入 | 0.613—0.689 N | 0.689—0.766 N |
| +Z 绕轴力矩 | 0.000449—0.000674 N·m | **至少0.054069 N·m，未测到失触** |
| -Z 绕轴力矩 | 0.000449—0.000674 N·m | **至少0.054069 N·m，未测到失触** |

最弱纯力精度方向是 `-Y`，0.536 N时三指仍抓住，但轴线已变化约0.95°；这说明最早问题是
手内姿态变化，不是掉落。`-Z`插入方向则在约0.69 N先超出精度，约0.77 N前已经掉落。

绕轴方向最能说明“接触保持”和“姿态稳定”的区别：约 `0.00045 N·m`仍满足0.5°增量精度，
约 `0.00067 N·m`已经失败；但即使达到`0.054069 N·m`仿真上限，三指仍夹着螺母，只是本体
发生大角度旋转，键位早已不可用。

![抗扰极限区间](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_disturbance_limits_v1/disturbance_limit_intervals.png)

![代表性响应曲线](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_disturbance_limits_v1/representative_response_histories.png)

![各方向载荷响应](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_disturbance_limits_v1/force_direction_response.png)

![代表性仿真画面](artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_disturbance_limits_v1/disturbance_visual_montage.png)

## 8. 对最终任务意味着什么

当前抓法对“抓起、保持位置和保持插合轴线”是可行的，并且比之前75°/高夹力方案使用更低力矩。
它不能被当作被动保持插头键位的方案，因为手只抓可转动螺母，本体绕轴自由度仍存在，且
`0.020 N·m`本身只是未标定假设。

因此后续插入链路必须在抓起后重新观察本体键位，并在预对准/插入过程中继续跟踪键位；不能把
“螺母在手里没滑”当成“插头本体键位已固定”。最值得继续研究的物理问题不是继续提高三指总力，
而是70°布局在 `-Y` 和 `-Z`方向的接触状态突变，以及如何利用抓后视觉闭环处理本体绕轴漂移。

## 9. 材料与证据说明（Material Passport）

- 对象：TE `D38999/26FJ35PN`供应商CAD分体模型；本体与联接螺母只允许同轴相对转动。
- 质量：同MIL斜线号公开参考质量 `62.459669349 g`；分体质量按CAD闭合体积比例分配，
  不是当前实物逐件称重。
- 物理假设：接触摩擦系数 `0.45`；未插合空载螺母等效阻力 `0.020 N·m`，后者没有公开实测值。
- 软件：Isaac Sim 6.0项目运行入口 `run_grasp_lift.py`和评价器`evaluate_run.py`；
  `240 Hz`物理步；没有真实硬件指令。
- 随机性：没有随机候选采样；每个条件从同一确定性reset独立运行。没有进行多随机种子统计。
- 在线信息边界：仿真对象真值没有返回抓取控制器；扰动装置只读取本体姿态以冻结测试坐标并把
  CAD质心转换为世界施力点。
- 主要原始数据：
  `artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_force_factor_sweep_v1/`、
  `artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_layout_sweep_v1/`、
  `artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_force_distribution_v1/`、
  `artifacts/kcg_connector/isaac/te_j35_real_mass_0p020_q70_disturbance_limits_v1/`。
- 证据限制：所有数字均为固定场景、固定摩擦、固定阻力、`n=1`有限档位的仿真结果；不能外推为
  实物额定载荷、跨场景成功率或毫米级硬件保证。
