# 2026-08-17 八小时自主窗口脚手架

本目录保存 2026-08-19 从 `src/kcg_connector/` 活动包移出的倒计时、截止前监控、
最终封包和旧阻塞重入脚手架，以及它们的自测。

归档依据：

- 11 个模块的外部引用审计为 0；引用只存在于该模块组及其测试内部。
- 当前 B-V3 运行器、抓取控制器、`tools/agent_control/` 和当前控制面均不导入它们。
- 其用途绑定已经结束的八小时/倒计时流程，不产生机械臂、三指手、视觉、力觉或连接器
  装配能力。
- 当前章程明确禁止继续建设无关监控器、报告器和离线占位框架。

`source/` 与 `tests/` 保留清理时的原内容。原路径和完整工作树可从本地提交
`c606865` 恢复。此归档不代表相关历史证据无效；`artifacts/agent_control/` 中的既有
报告和台账仍保持原路径只读。
