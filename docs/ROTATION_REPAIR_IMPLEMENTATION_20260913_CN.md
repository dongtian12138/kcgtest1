# 旋拧控制与实验效率修复

2026-09-13。用户已要求实施前述问题，并明确以受载控制和完整装配为重点，空手回转优化后置。模型几何、质量、原始接触和独立传动/腕力边界保留。以下软件修改已接入；物理效果需要本轮短验证，不能以软件测试代替装配成功。

## 已实施

1. 保留名义关节轨迹积分，末端位姿纠正改用实际编码器位姿。超出有限横向误差范围时停转进入可恢复处理；关节速度采用整体缩放，避免逐关节裁剪改变指令方向。
2. 将期望抓持关系与当前视觉观察分开。期望关系用于纠正实际手部偏差，当前图像用于连接器位置、轴线、导向/避碰与进度。不能用刚观察到的偏移重定义期望抓位，把误差自动消掉。
3. 轴向采用已知7.62 mm导程的名义运动加有界力修正，修正有恢复项与0.1 mm范围，合成速度仍受原0.3 mm/s上限限制。
4. 旋拧时重新读取当前RGB-D，识别观察到的进展、无进展、滑移/卡滞线索和到位候选；到位候选经连续确认后停止运动，最终装配仍由独立物理评价决定。
5. 夹紧使用现有末段0.25秒数据检查均值误差、波动和剩余传动余量。未满足就绪要求则明确退出，不自动延时加力。手指调节/卸载统一新版底座力矩补偿与换算来源。
6. 加入同回合有限恢复：可恢复偏载停转后，用当前视觉检查导向，真实卸载、张手、回转与重抓，只继续尚未发送的角度。原失败记录保留，硬故障不恢复，物体状态不回写。
7. 空手返回优先只改变第七关节目标，其他六个关节目标保持。全轨迹检查碰撞，末尾检查实际角度；90°、1 rad/s、2 rad/s²的规划时长约2.071秒。是否在现有场景可直接执行，以局部检查为准。
8. 接触路径解码缓存；每步仅更新变化的驱动量，复用同一物理时刻的机器人读数；数值记录使用保留NaN/Inf失败读数的C JSON编码，并减少归档时整条记录的复制和高压缩成本。原始物理记录不抽掉。新增分项计时区分原生步进、接触回调、机构更新、读取和归档。
9. 同回合视觉身份与相机资源复用，当前深度图重新拟合位置/轴线；区域或质量不满足时重新完整识别。三个已保存真实RGB-D帧的离线回放与原完整识别的位置结果一致到浮点精度，后两帧拟合约0.13秒；这不等于已验证所有新视角。
10. 动态入口加入现实时间限制，默认总上限300秒，预留30秒受控收尾，并由外部进程组截止保护。加载、计算、观察、保存均计入，不临时延长。诊断读取指定源时刻改用原始记录块索引。

JSON依赖采用[PyPI官方ujson 5.13.0发布](https://pypi.org/project/ujson/5.13.0/)，已安装到项目离线环境与Isaac环境，并写入依赖文件。此处选择的数值编码路径保留原JSON对非有限失败读数的表达；已验证浮点回读与原始数组格式。

## 验证顺序

- 已完成相关既有回归及新软件测试，覆盖带载仿真测试模型的纠偏、带视觉更新时不吞掉跟踪误差、正常到位提前停止、剩余角度恢复、硬故障拒绝、就绪条件、单关节轨迹及现实时间截止。
- 空手回转的两次检查已结束；运动约2.071仿真秒，但现实耗时约247至257秒，此优化支线已停止。
- 随后用短旋拧窗口检查当前控制。局部源状态明确属于独立诊断，不拼接成从桌面开始的成功回合。
- 完整回合只有在控制局部结果成立且现实耗时可控时才启动。当前没有新的完整装配成功声明。

本轮代码和短验证产物入口：`artifacts/rotation_repairs_20260913`。主因见[诊断](/home/noob/WorkPlace/kcgtest1/docs/ROTATION_DIAGNOSIS_20260913_CN.md)，其余缺口见[控制审计](/home/noob/WorkPlace/kcgtest1/docs/ROTATION_CONTROL_AUDIT_20260913_CN.md)。

## 受载控制的直接反馈

loaded_control_01在136.53秒结束、未发出转动：0.5秒窗口含第一条尚无因果惯性估计的样本。诊断入口已补一个前置物理样本；同时修复SimulationApp关闭覆盖诊断失败退出码的问题。原失败保留。

loaded_control_02在222.28秒完成0.25°受载窗口，实际Nut转0.25458°、推进5.573µm，导程预测5.389µm；Body推进5.603µm。实际抓持中心相对Nut横向变化峰2.217µm，编码器推算的受载中心漂移峰3.598µm；第三指弹性力矩2.972672→2.972408Nm，未继续增长。峰侧力0.441N、扭矩0.241Nm。原物理记录2344条连续，当前视觉在转动中重新观察一次。它验证局部响应，不能证明90°、后续换抓或整轮成功。

进一步修正视觉判据：相机观察Body、手指抓Nut，依据模型现有1mm工作轴向游隙，分开计算横向误差与超出游隙的轴向误差；绝不放宽物理后备端点或力矩限制。无进展检查使用整次抓持的观察起点，扣除工作游隙及原抓持误差容限，不再凭单个1秒窗口把正常游隙运动认作停滞。两项相应回归与原控制测试共20项通过。

为能在几分钟内检查更大受载角程，下一次仅在局部诊断中比较240Hz与960Hz，保持同一0.25°轨迹、模型、求解迭代、传动和力矩边界；原960Hz比较基线保留。新的时间离散方式尚未接受，必须看实际负载、推进和模型误差，不能自动沿用旧验证。

## 后续受载结果与低层驱动修正

03的240Hz小角程在145.84秒结束并通过键槽检查；04扩大到约10.29°后，尽管第三指不再增长，源键残差2.430µm超过原2µm，故未将240Hz用于正式装配。04在271.61秒依预算停止，没有延长。

05在原960Hz检查12°、峰5°/秒：233.40秒停止；真实Nut转11.7677°/推进246.73µm，键槽通过，但抓持横向变化207.47µm，第三指力矩增长0.08123Nm。当前视觉正确发现了真实偏移，不是误识别。

对05原生关节读数的离线分解表明：原生速度目标一直为0；K=2500、D=160时，移动轨迹引入D/K=0.064秒的阻尼滞后。匀速段测得关节跟踪差与(D/K)×实测速度的差范数约为原差的9%至15%。位置轨迹积分不断叠加补偿，仍积累抓持偏差。新增原生关节速度目标并与位置轨迹来自同一个IK速度；饱和预测用D(vref-v)，保持K、D和力矩边界，首个非零目标及归零原生读回验证；非旋拧阶段明确置零，避免泄漏。06用同一运动轨迹检验这一主要变量。

受载采样同时定位到摩擦报告在每步遍历大量空配对及无窗口模式仍刷新UI的开销，已分别改为全量范围检查后只处理非空配对、无窗口诊断跳过额外UI刷新；没有减少原始接触、传感器或视频数据。22项相关软件回归通过，原失败证据保留。

## 三指松紧与蜗杆自锁的直接验证

07仅跟随当前视觉后，峰侧力降低，但第三指弹性力矩仍增长0.09122Nm。08启用原目标的底座力矩反馈后，第三指位置参考由0.725239降到0.716183rad，电机输入力矩仍为正4.065Nm，仍在蜗杆自锁区间内；三指真实负向输入速度次数均为0。不能把参考数值减小当成实际开指。

新增命令映射直接对现有passive_split方程求逆：给定有界输入速度，使用原K、c、B和有限Motor PD求相应输入位置参考；不改变传动模型，不改写实际输出位置/速度。零速采用原自锁保持。40项蜗杆相关检查通过，包含正反向、负载变化及能量/原力矩边界。

09仅增加该自锁补偿，在244.62秒结束；实际Nut11.5795°、推进242.68µm，导程预测245.10µm；源键最大残差1.726µm，通过原2µm线。F3实际负向输入速度1044次、净输入角变化−0.0003477rad，证明主动开指发生。F3弹性力矩增长由08的0.09686降到0.05429Nm；末根力矩误差约[+0.0390,−0.0090,−0.0197]Nm。电机均未达到原输入边界。

仍不能称完整解决：局部抓持横向变化约0.1925mm，轴向修正达到0.1mm上限；尚未验证越过原末段或完成到位。完整装配成功回合仍为0。下一步优先从原停止附近验证新控制是否能继续推进，而不是再重复同一低载窗口。
