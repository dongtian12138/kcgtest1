# TASK-R12-MULTILAYER-MODEL 执行计划

状态以 artifacts/agent_control/MASTER_STATE.json 为准；本文件只描述冻结流程。

1. TASK-R12-MULTILAYER-001：冻结原高精细基线、已否决局部候选和 005/006B/006C 证据，验证摘要无漂移。
2. TASK-R12-MULTILAYER-002：从现有权威配置和生成程序提取唯一主模型合同；每个参数记录公开资料、项目测量、推算或等效假设来源。
3. TASK-R12-MULTILAYER-003：只用主合同和一套共享生成程序产生完整外观、装配控制、局部高精细参考三种表示。
4. TASK-R12-MULTILAYER-004：验证装配轴、主要尺寸、五键、61 组编号/中心、质量/重心、事件、导程、止挡和路径映射一致，并验证无跨编号作用、无全组合接触和真值隔离。
5. TASK-R12-MULTILAYER-005：只对插头与固定插座执行一次名义插入，严格检查七事件、原门限、力/矩、漂移、求解器和位姿写入。
6. TASK-R12-MULTILAYER-006：仅在 005 通过后用三个独立进程复验，记录事件、力/矩、漂移、物理帧率、渲染帧率、显存与三次差异，并生成里程碑包。

## 提前停止条件

- 高精细资产或历史证据摘要漂移。
- 需要新孔径候选、第二个几何候选、更大驱动力、降低物理作用或放宽门限。
- 需要第二套权威模型，或不能从既有权威文件追溯关键参数。
- 同一失败特征第二次出现，或连续 30 分钟无新数值结论。
- 单命令、节点或总自主窗口达到上限。

## 当前状态

- TASK-R12-006C 阻塞结论已由人工接受；006D、006E 永久不授权。
- 已通过节点：TASK-R12-MULTILAYER-001、002、003。
- 当前节点：TASK-R12-MULTILAYER-004，阻塞。
- 阻塞性质：静态检查器基础设施错误；模型静态一致性未判定。
- 用户动作：需要上传审查包。
