# CONTACTOPT-1488 六小时执行计划

> 窗口：`CONTACTOPT_1488_FAST6H`  
> 开始：`2026-08-26T14:16:36Z`  
> 截止：`2026-08-26T20:16:36Z`  
> 分支：`carts-grasp-contactopt-1488-fast6h-20260826`

## 北极星

使用同一无指甲手型、同一方法和主要参数，从严格的 1488 个三点—法向配准初值中，为对象 B 先找到具有三指安全接触区间的候选，再在 Isaac Sim 以关节侧信号逐指接触、离桌、抬升 50 mm 并保持至少 2 s；之后才接完整机械臂，并在剩余时间对对象 A 原样运行。`hardware_authorized=false`、`formal_dynamic_pass=false` 始终失败关闭。

## 固定方法

1. 全局组：`13×16×5×1=1040`；加密组：`7×8×4×2=448`；合计严格 1488 个规格。
2. 手侧三块 `TASK_GRIP_SURFACE` 参考点/法向与对象 `PRIMARY/SECONDARY` 三个目标区域通过 Kabsch/SVD 和一次小型 `least_squares` 求六维位姿；不随机生成姿态。
3. 全部 1488 只用缓存 FK、每指 64～128 个代表点和对象近邻结构做便宜排序；保留结构多样的 Top120。
4. Top120 求逐指 `q_expected` 与 `q_safe_max`，再取 Top48 做一条有界接触优化路线；Top12 才运行原始网格和 Isaac 复合凸的双几何精查。
5. 只有几何、三指语义、安全区间、12 N 名义任务和必要 IK/路径门都通过的 Top3 才能进入 Isaac。

## 监督采纳的三项简化

- 只保留一个接触状态数值核：前级为代理证据，Top12 才绑定双几何；`q_safe_max` 是首个失败边界前一个不超过 0.0015 rad 的控制步，在线力矩门仍独立生效。
- 优化器不轮试：Kabsch+法向小精修只生成初值；Top48 只走一条预登记有界局部路线。Top8 增强仅在 2:10 前有时间且局部停滞时启用。
- 冻结现有临界大文件；新增四个单职责数值模块和薄入口，动态复用现有控制器、事后真值评价和运行资产。

## 任务卡与时间门

| 时段 | 物理问题与最简单方案 | 观察量/失败判据 |
|---|---|---|
| 0:00–0:55 | 三指几何能否产生完整、非随机的六维初值；实现严格规格、三点配准和覆盖审计 | 必须 1040+448=1488；每项 `POSE_GENERATED` 或唯一 `SEED_GEOMETRY_REJECT` |
| 0:55–1:25 | 如何避免把 1488 项送入重型 FCL；缓存 FK 和对象近邻，全部便宜评价后多样性 Top120 | 90 分钟前完成 1488 分类、Top120 和耗时 |
| 1:25–2:10 | 每指是否存在“预计接触到最远安全角”的可执行区间；一维括区间后取 Top48 | `q_expected<=q_safe_max`；无括区间即拒绝 |
| 2:10–2:50 | 候选是否在两套实际仿真几何中均安全且能承受任务；Top12 双几何、12 N、Top3 | 两套几何不一致、语义/桌面/自碰失败或名义余量<1 均不得进 Isaac |
| 2:50–4:20 | 对象 B 是否真实三指接触并离桌；Top3 依次低速搜索接触区间 | 三指真实接触、离桌、50 mm、2 s；单候选失败自动换下一个 |
| 4:20–5:35 | 局部成功后接全臂；再冻结方法运行对象 A | 完整机械臂证据；A 不回调方法参数 |
| 5:35–6:00 | 保存事实和失败，定向回归、监督、提交推送 | 不新增功能；20:16:36Z 到点停止 |

## 2026-08-26T17:30Z B0 局部纠正任务卡

- 要改变的物理结果：让三指在不把对象部位优选当硬禁区的前提下，先完成名义拾取。
- 当前唯一改变量：对象侧只保护明确功能面；完整外部实体面可承载，PRIMARY/SECONDARY仅诊断。
- 最简单方案：先重评旧候选，再固定重跑1488；对生成姿态在Top-K前作确定性纯z桌面投影。
- 观察量：生成拒绝分类、三指安全区间、967个闭合状态、1 mm桌面余量、12 N名义平衡、bounded IK和Isaac接触。
- 失败判据：未授权穿透、自碰、非任务手面承载、无三指区间、12 N名义平衡失败、IK失败或动态安全急停。
- 停止时间：仍为`2026-08-26T20:16:36Z`；不建立新窗口。
- 下一动作：候选专属局部Isaac初始穿透，然后第一指接触诊断；不再修改生成器分箱或运行第二轮COBYLA。
- 17:41 实际：监督要求的桌面操作余量门已失败关闭；重算候选闭合最小间隙为 0.991278 mm，满足 1 mm 减 10 µm 数值容差，Isaac 输入哈希绑定通过。

## 当前证据边界

- 上一 Surface V2 离线负结果是旧枚举设计的对照，不证明完整连续空间或三指手机械结构无解。
- 1488 完整、优化收敛、FCL/IK通过、图片和退出码都不是动态抓取成功。
- 研究动态只有在物理时间推进、三指真实允许面接触、离桌、50 mm、2 s 且无未授权穿透时才成立；正式动态与硬件均保持否。
