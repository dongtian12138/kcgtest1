# CONTACTOPT-1488 当前决策摘要

完整历史决策保存在 Git 标签 `pre-clean-project-20260826`。本文件只保留当前方法仍然
有效的决定，不作为动态授权。

## 方法身份

- 候选由真实手侧三点/法向与对象登记表面经 Kabsch/SVD 和小型法向精修得到；不使用随机
  姿态、Sobol 或手填对象坐标。
- 1488 个规格固定为全局 1040 和加密对向组 448；每项必须留下生成或唯一拒绝原因。
- 前级是代理证据，Top12 才绑定原始无指甲网格与 Isaac 复合凸双几何。

## B0 语义

- 对象 PRIMARY/SECONDARY 只作诊断排序；B0 使用登记外部实体承载面，明确功能保护面仍
  失败关闭。
- 桌面操作余量、逐指闭合、自碰、非任务手面和 12 N 名义任务门保持独立，不能用综合分数
  互相抵消。

## 当前候选事实

- `contactopt_g_q08_a03_z1_p0` 的采样闭合最小桌面间隙为 0.991278 mm，满足已登记的
  1 mm 减 10 µm 数值容差。
- 名义重力/抬升平衡通过，但额外六方向与误差场景未通过鲁棒门；bounded IK 通过不等于
  整臂路径安全。
- 初始 Isaac 只推进 0.5 s 且未发送闭合/抬升指令；没有真实手—物接触，不能称为抓取成功。

## 当前停止边界

用户已要求优先清理工程。CONTACTOPT 动态状态为 `PARKED`；清理、静态测试或文档更新
都不能把它升级为 `DYNAMIC_PASS`，也不能自动恢复运行。
