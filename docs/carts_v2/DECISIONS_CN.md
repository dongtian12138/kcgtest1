# CARTS-Grasp V2 决策记录

## 2026-08-23T14:41:38Z — NORTH_STAR_CHANGE

- 用户明确把当前阶段改为 CARTS-Grasp V2：真实表面候选 → 任务载荷鲁棒 Top-3 → Isaac 研究型动态。
- 物理成功改为离桌后抬升至少 50 mm、保持至少 2 s；旧 40 mm 口径退出当前主线。
- 旧 H102 路线冻结为 reference，严禁继续连续编号式局部修补。
- `hardware_authorized=false`、在线真值隔离、禁磁吸/隐藏固定/物体位姿写入保持不变。

## 2026-08-23T14:44:00Z — 证据线分离

- 旧正式控制面的 `dynamic_launch_allowed=false`、正式候选为空和 `NOT_CERTIFIABLE` 保持不变。
- 新 V2 只建立独立 `research_dynamic_gate`；它满足有限速度/力量、初始无明显穿透和在线真值隔离后，才允许研究运行。
- 研究运行无论成功与否都不回写为正式动态或硬件结论。

## 2026-08-23T14:52:11Z — ARCHITECTURE_CHANGE

- 问题：旧高层优化器对全部候选绑定严格 clearance，旧路线碰撞器还写死 40 mm，与 V2 顺序及 50 mm 冲突。
- 证据：两名监督分别核对公开力学、排序、碰撞和 Isaac 入口后均只作有条件批准。
- 替代：只复用公开模型、表面采样、Sobol、LP 与尾部统计；V2 自己做一次七项字典序调度。
- 允许面：`models.py` 维护唯一逐面语义掩码；映射未闭环则不生成候选，不能把全部 external 面当允许面。
- 碰撞：唯一薄适配器无法直接验证 50 mm 时返回 `UNRESOLVED_INTERFACE_MISMATCH`，正式线失败关闭，研究线按独立门推进。
- 力语义：另求 `lambda=1` 的最小单指负担；旧极限 lambda 处峰值力不得写成规定任务所需力。
- 时间影响：减少旧证书依赖，预计节省超过 60 分钟；不改变 50 mm、2 s、真值隔离和双对象同参数。
- 监督结论：Academic 与 Complexity 均 `APPROVE_WITH_REQUIRED_SIMPLIFICATIONS`。

## 2026-08-23T15:55:41Z — LOCAL_CORRECTION

- 高维 Sobol 不能把 `bits` 设成场景数的最小指数；该组合在新 SciPy 中会退化，旧离线报告已撤回并重算。
- 冻结数值环境为项目 `.venv`，26×16 设计完整写入结果 JSON，SHA-256 为 `6aea2d6e5bc384445e1accf7636435142356eaf96a425481d17299a5cdcd93c1`。
- “对象位姿误差”收窄为实际实现的共同接触点平移和重力方向误差，不声称完整手—物体刚体位姿误差。
- URDF `100 N·m` 归类为 `UNKNOWN_UNCALIBRATED_MODEL_VALUE`；只报告实际广义关节力矩，不声称关节或腕部硬件利用率。
- B 的 `candidate_46` 只通过离线任务门；IK、路径碰撞、初始穿透和控制器未闭合前，研究动态门保持 false。

## 2026-08-23T16:51:24Z — LOCAL_CORRECTION

- run04 在 Kp=400 时第二关节误差 0.12044 rad 对应 48.19 N·m；再等待 1 s 后仍为 0.11947 rad / 47.55 N·m，确认是重力下的刚度稳态误差。
- Academic 与 Complexity Supervisor 一致批准仅把 `arm_stiffness` 改为 2500；数值来自 48.19/0.020=2409.5 后向上圆整，不是盲扫参数。
- Kd=40、100 N·m 限力、速度/跟踪门、轨迹、100 mm 上方净空及 A/B 共用配置全部冻结。
- 2500 只归类为 `SIM_OPERATION_CAP`，仍是带阻尼和限力的位置/刚度控制，不声称重力补偿、硬件刚度或学术创新。
- 本次只允许一次 A 预飞；失败即停止 Kp/Kd 调参并转入公开重力前馈重设计。

## 2026-08-23T16:57:35Z — MILESTONE_REORDER

- run05 已让 A 到达上方稳定门：最大臂误差 0.01768 rad、最大臂力矩 45.38 N·m，未触及 100 N·m 上限。
- 下降第 146/240 步出现 f2Link2 凸包—桌面接触，随后 f2j1 速度 4.628 rad/s 触发安全急停；当前根因从控制稳态误差转为 A Top-1 路径几何冲突。
- A Top-2/3 的任务载荷余量为空，不能因为更高就替代任务质量 Top-1；该试验后置为路径消融。
- 两位监督者一致批准冻结 A 推导的所有主要参数，先集成唯一通过离线门的 B Top-1 自由 STEP 同源资产。
- B 只先做预飞；通过不等于抓取成功，失败不得回调高度、增益、门限或候选编号。
### 2026-08-23：PARK 三点刚体对齐候选族

- 类型：`LOCAL_CORRECTION`
- 证据：同一 384 样本/48 候选只读原型中，A/B 闭合幸存分别 5/4，但桌面快筛幸存均为 0；三点刚体拟合残差约 9.0--20.4 mm。
- 决定：保留高度条件化 yaw-only 生成器作为当前失败基线；停止倾角、预抓相位与三点匹配补丁，不做第三次候选姿态局部修改。
- 研究动态边界：当前 A/B Top-1 均可平衡名义重力与抬升峰值，只允许无扰动研究观察；完整误差/扰动任务门仍失败关闭。

## 2026-08-23T18:22:00Z — LOCAL_CORRECTION

- 用户插话要求澄清“是否只是碰撞检测没做好”；本次不改变总目标或成功标准。
- 旧 A `candidate_07` / B `candidate_46` 确实因快筛未覆盖真实手部网格—有限桌面路径而漏检；该缺口已由真实碰撞 STL 的接近、预抓和顺序停靠快筛补上。
- 当前 A `candidate_11` 规划净空约 `+0.705 mm`，但 Isaac 中约 `0.015 rad` 的重力稳态跟踪误差使手基实际低约 `9 mm`；5 个笛卡尔高度 IK 路点仍在同一下降末端碰桌。
- 当前根因因此是“已规划净空小于控制跟踪误差带”，不再是单纯的路径插值或快筛漏检。
- 下一唯一改变为公开姿态相关重力补偿；候选、路径、速度、力量上限、真值隔离和 A/B 同参数均保持。

## 2026-08-23T18:25:00Z — LOCAL_CORRECTION

- 用户询问 MoveIt 现成运动规划与碰撞检测能否解决当前困境；本次不改变总目标。
- 仓库已有 `kcg_moveit1` 的 ROS 2 / MoveIt 2 配置，但 V2 尚未把真实连接器、有限桌面、夹具和最新手部碰撞语义接入其规划场景。
- MoveIt 可作为后续 Home—预抓的全机器人避障路径后端，但不解决重力稳态跟踪误差、三指有效接触身份或任务承载排序。
- 当前先完成同参数重力补偿区分实验；若跟踪误差闭合后仍是路径中段冲突，再以真实 V2 规划场景接入 MoveIt，不复用旧圆柱场景。

## 2026-08-23T18:47:00Z — ARCHITECTURE_CHANGE

- 问题：公开 Newton `ArticulationActuators` 的两次运动前接线尝试分别失败于 DOF 顺序严格校验和 CPU 参数—`cuda:0` 物理视图设备不匹配；两次均未发送轨迹、未发生碰撞。
- 两次规则：停止 Newton 接线补丁，不修第三次设备配置。
- 最简单替代：删除 Newton actuator 路径，只保留一个公开 experimental `Articulation` 视图和原生有限力位置驱动；七轴命令为 `q_drive = q_nominal + g(q) / Kp`。
- 物理边界：只能写“控制律代数等价”，Isaac 离散实现待证；跟踪误差仍对比未偏置的 `q_nominal`，偏置、预测总力矩和关节限位分开记录。
- 冻结不变：前馈比例固定 1.0，A/B 同参数；Kp=2500、Kd=40、100 N·m、3 rad/s、轨迹、候选、场景、dt、seed 和真值隔离不变。
- 停止条件：只允许一次 A 有界预飞；若视图/shape/设备不一致、偏置越限、总力矩饱和、稳态误差不降或仍碰桌，立即 `PARK`，不再换 API 或调参。
- 监督结论：Academic 与 Complexity 均 `REDESIGN_APPROVE_WITH_FAIL_CLOSED_CONDITIONS`。

## 2026-08-23T20:34:00Z — PARK 动态对象运行

- 唯一 A 原生重力预飞真实推进 2.5667 s；机器人—桌面、夹具、连接器接触均为 0，机械臂最大跟踪误差 0.00735 rad。
- `f2j1` 在上方接近第 68 步由平滑目标 0.01250 rad 附近突增至 4.4916 rad/s，触发预登记 3 rad/s 急停；未进入预抓、闭指或抬升。
- 重力项最大 1.527 N·m、等效偏置最大 0.000611 rad、预测总力矩最大 12.26 N·m；无 100 N·m 饱和或关节限位越界。
- 本次启动时 NVIDIA 驱动不可枚举，PhysX 退到 CPU；旧比较运行使用不同后端，因此不能把机械臂误差差异严格归因于重力偏置。
- 失败类登记为 `HAND_PRESHAPE_SPEED_ABORT_WITH_CPU_FALLBACK`；PARK 重力接口、Kp/Kd、候选补丁以及当前条件下继续 A/B 对象运行。
- MoveIt 后置：当前触发点是自由空间手指动力学，不是机械臂路径或环境碰撞。
- GPU 恢复后唯一允许的区分实验为无对象、无桌面的同轨迹 `compare_cpu_vs_gpu_hand_drive`；通过前不得再次运行 A/B 候选。
- Academic 与 Complexity Supervisor 均 `VETO_PREFLIGHT_PASS_AND_PARK_OBJECT_DYNAMICS`。

## 2026-08-23T20:48:13Z — 动态失败证据边界更正

- run05 trace 直接证明的失败类别是 `HAND_PRESHAPE_SPEED_ABORT`：`f2j1=4.4916 rad/s` 超过 3 rad/s 并急停，且没有进入闭指、抬升或保持。
- NVIDIA 不可用和 PhysX CPU fallback 来自启动环境观察，未绑定进 trace；改记为 `CPU_FALLBACK_CONTEXT`，不得把它写成已证根因。
- 速度异常根因保持 `UNRESOLVED_HAND_MODEL_OR_PHYSICS_BACKEND`；GPU 恢复后的同轨迹无对象/无桌面对照仍是唯一允许的后续动态区分实验。
- 未归档的单一名义场景排序不作为 P3 证据；P1--P4 当前均未验证。

## 2026-08-23T21:01:32Z — 根因范围与迁移对象更正

- B 是见过几何的迁移对象，不是前瞻盲测或严格留出集。
- 根因更正为 `UNRESOLVED_HAND_DRIVE_MODEL_OR_BACKEND`，覆盖驱动接线、手模型/mimic 约束和物理后端。

## ARCHITECTURE_CHANGE — 共享 IK 的源码总量目标例外

- 问题：V2 core 为 3371 NLOC；完整计入共享 IK 后为 3622，超过 3500 目标 122 行。
- 证据：`candidate_joint_route.py` 1160→1004；aggregate 872→819；roster 803→854；共享 IK 251，robust 支撑净增 93。
- 最简单替代：一个公开 bounded IK 同时供 legacy 与 V2 调用，不保留两套求解器，也不再拆 bridge/state 模块。
- 时间影响：已完成，无新增动态运行；controller 597 NLOC 冻结，后续加功能前必须先删减。
- 监督结论：Complexity `APPROVE_WITH_WRITTEN_EXCEPTION`；未降低物理门、跨对象复用或真值隔离。

## 2026-08-23T21:42:11Z — Git 只读环境的集成交付

- 原工作区执行 `git add` 时失败：`.git/index.lock` 位于只读文件系统；未修改或破坏原 Git 数据。
- 从同一 `698a2bb` HEAD 建立 `/tmp/kcgtest1-carts-v2-integration-20260823`，以原工作区作为内容源精确暂存本轮路径。
- 代码与离线数据提交为 `4d90a52`；原工作区 HEAD 仍为 `698a2bb`，因此 IDE 会继续显示工作树修改。
- 远端推送结果在文档提交后记录；禁止把临时克隆机制解释成原工作区已同步。

## 2026-08-23T21:44:03Z — 远端失败与可恢复交付

- 文档提交为 `58c9503`；推送 `carts-grasp-v2-rebuild-20260823` 失败，原因为 `Couldn't connect to server`，未发生远端写入。
- 本地保留 `/tmp/kcgtest1-carts-v2-integration-20260823`；另生成增量 bundle，要求起点 `698a2bb`，包含到 `58c9503`。
- bundle 为 802,864 B，SHA-256 `b22d65dd19d7b468ac7a1d0cfe2a8c46f9b784d1473f884b93124a9f44c3a9d1`，已通过 `git bundle verify`。
- 网络恢复后只允许普通 push 同名 V2 分支，禁止 force push；原工作区同步仍需恢复 `.git` 写权限。

## 2026-08-24T06:42:18Z — STOP：12 小时硬截止

- 本轮硬截止为 `2026-08-24T02:41:38Z`；复核时已经超时 4 h 00 min 40 s，因此停止新增功能、Isaac 运行和 GPU/CPU 区分实验。
- 截止时只达到双对象离线排序和真实 Isaac 预飞失败证据；三指闭合、离桌、抬升 50 mm、保持 2 s 和扰动均未完成，不能写 `RESEARCH_DYNAMIC_PASS`。
- 截止后唯一执行的是交付修复：只读确认原工作区内容与临时集成 tip 完全一致，保留 `698a2bb` 恢复分支，再无历史改写快进至 `57efc16`。
- `57efc16` 已用普通 push 同步远端 `carts-grasp-v2-rebuild-20260823`；没有 force push，没有启动仿真，没有改变对象、参数、成功门或 `hardware_authorized=false`。

## 2026-08-24T07:05:10Z — MILESTONE_REORDER + EXECUTION_WINDOW_REOPEN

- 用户明确重开补足窗口 `V2_RECOVERY_1`，最长 5 小时，硬截止为 `2026-08-24T12:05:10Z`；这不是新的 12 小时窗口，也不是 `NORTH_STAR_CHANGE`。
- 北极星、两个对象身份、50 mm、2 s、真实指腹接触、3 rad/s 安全限速、在线真值隔离和禁止未授权穿透全部保持。
- `hardware_authorized=false`、`legacy_formal_dynamic_launch_allowed=false`、`formal_dynamic_pass=false` 保持失败关闭。
- 研究门先只开放 `ISOLATED_HAND_DIAGNOSTIC`：无对象、无桌面、同模型/初值/轨迹/dt/drive 的手部区分实验；在 `f2j1` 根因关闭前禁止对象 A/B 运行和候选算法修改。

## 2026-08-24T08:13:09Z — LOCAL_CORRECTION + EXECUTION_CONTINUATION

- 用户明确纠正：GPU found/lost aggregate-pair 与 total aggregate-pair 容量属于 Isaac/PhysX 运行资源，不是抓取算法、物理参数、控制增益或成功标准；`V2_RECOVERY_1` 因此属于过度保守停止。
- 开启补足窗口 `V2_RECOVERY_2`，有效预算 4 小时 15 分钟，硬截止 `2026-08-24T12:28:09Z`，不自动延长，也不重开 12 小时计划。
- 先修复预检查失败关闭漏洞，再审计场景结构与两类容量峰值；没有重复碰撞体或错误过滤时，容量按 `next_power_of_two(ceil(2 * observed_peak))` 分别确定。
- `candidate_11`、`candidate_33`、算法排序、物性、摩擦、增益、力量、3 rad/s、50 mm、2 s、指腹标准、真值防火墙及旧 H102 全部冻结。
- `hardware_authorized=false`、旧正式动态门、`formal_dynamic_pass=false` 和 `research_dynamic_pass=false` 保持；只有新评价的 `accepted_preflight_pass=true` 才能启动对象 A 抓取。

## 2026-08-24T09:36:16Z — V2_RECOVERY_2 失败收口

- run17 在第二次有证据修复后，仍与 run15 同一步、同阶段触发 `f1j3` 超过 3 rad/s；该根因的两次尝试已用尽。
- 决定：`status=PARKED`、V2 研究动态门关闭；禁止第三次直接补丁、调增益/阈值或提高限速。
- 对象 A 未形成三指真实 PAD 接触，抬升和保持均为 0；对象 B 不满足启动前提，因此本窗口不运行。
- GPU 容量已验证健康，不再列为当前阻塞；正式动态、研究动态和硬件通过均保持 false。

## 2026-08-24T10:16:54Z — SUPERVISION_RULE_CORRECTION + ROOT_CAUSE_REVIEW

- 用户纠正：同一根因两次实现失败只触发根因复盘，不自动触发项目 `PARKED`；恢复 V2_RECOVERY_2 剩余 10313 s，不创建新窗口。
- `03594b8` 只影响接触确认后的后续保持周期，不能影响 run17 接触确认当周期的目标阶跃，因此不计作消除该阶跃的独立因果修复。
- 当前改为 `IMPLEMENTING`，里程碑为接触状态切换根因复盘；先使用 run15/run17 原 trace，连续性测试通过前不启动 Isaac。
- 3 rad/s、50 mm、2 s、物性、候选、控制增益和真值隔离均冻结；硬件、研究动态和正式动态通过继续为 false。

## 2026-08-24T10:36:33Z — 接触切换采用关节力代理的有界位置修正

- 删除所有接触阶段的 `target=measured_position`；确认周期输出保持上一命令。
- CONTACT_SETTLE/HOLD 只复用既有检测阈值与手部刚度，把去 tare 关节力代理误差换成每周期受限的位置修正，不新增增益或力量门限。
- 该控制量包含惯性、阻尼和约束反力，只称“关节力代理”，不声称是真实指腹法向力或完整力控。
- 只有新源码绑定预检查和第一指 0.5 s 事后接触评价通过，才允许继续完整三指运行；整末端凸包路径不能升级为 PAD patch 身份。

## ARCHITECTURE_CHANGE — 完整场景接触见证的临时规模例外

- 问题：对象场景超速而无对象同目标回放稳定，旧步后轮询无法区分真实碰撞、未分类路径和 GPU 接触缓冲时机。
- 最简单替代：在现有 evaluator 内并行记录事件回调与步后轮询，未分类接触和通道分歧失败关闭；不建新文件或框架。
- 规模：控制修复仍约 119 NLOC；见证约增 67 NLOC，Recovery 2 四个生产文件累计约 186 NLOC，低于 250 行窗口额度。
- 影响：三个动态文件暂超原 600 行目标；动态前重构会改变已绑定路径，故仅批准一次预检查和第一指诊断，取得证据后禁止继续叠加功能。
- 监督结论：Academic 与 Complexity Supervisor 均批准；在线真值隔离、3 rad/s、对象/候选/物性和正式门均未改变。

## 2026-08-24T11:46:11Z — ROOT_CAUSE_REVIEW 关闭冻结动态路径

- 注册网格重放确认 `candidate_11` 第一指闭合中段由 `f1Link3` 先扫入桌面；代理触发时 PAD 距连接器仍约 41.873 mm。
- 快筛只检查预抓、接近和接触停止端点，漏掉最深约 1.838 mm 的非单调中间扫掠。
- 两位监督者一致认为继续运行会重复已知未授权穿透；filtered-pairs 会隐藏真实冲突，明确禁止。
- 由于候选、快筛和路径在本窗口冻结，研究动态门关闭；这不是预算耗尽，也不改变 NORTH_STAR、50 mm、2 s、正式门或硬件授权。

## 2026-08-24T12:11:41Z — METHOD_CORRECTION + ROLLBACK_TO_OFFLINE_SELECTION

- 用户纠正：`candidate_11` 的第一指中途扫桌只否定该候选和旧端点快筛，不应令整个项目 `PARKED`；状态恢复为 `IMPLEMENTING`，回退到离线候选筛选。
- `candidate_11` 永久标记为 `INVALIDATED_BY_INTERMEDIATE_HAND_TABLE_SWEEP`，不得再次运行；旧 `candidate_33` 和旧 Top-3 因方法版本变化必须重算，不能继承旧排名。
- 快筛改为按真实三指控制顺序覆盖每一指从预抓到预测接触停止的完整离散扫掠；相邻状态任一独立关节变化不超过 `0.18 × 1/120 = 0.0015 rad`，复用注册手部碰撞网格、正运动学和有限桌面距离。
- 固定处理顺序改为：384 个确定性原始表面种子 → 闭合预测 → 完整顺序闭合扫掠 → 路径安全种子多样性选择最多 48 个 → 任务载荷评价。
- “第一名”只来自同时满足闭合、完整扫掠、快筛和 `TASK_SURVIVE` 的 `executable_candidates`；失败但有诊断价值的候选只能写 `DIAGNOSTIC_ONLY_NOT_EXECUTABLE`。
- 本阶段最多 4 小时，从第一条实际检查命令 `2026-08-24T12:11:41Z` 起计；不是 `V2_RECOVERY_3`，也不改变两个对象、物性、8 N 基线、3 rad/s、50 mm、2 s、真值隔离、正式门或 `hardware_authorized=false`。
- 用户提供硬件事实：实体单指能力至少 15 N。若 8 N 基线在完整 384 种子池后仍无任务合格候选，只允许一次独立只读 12 N 敏感性计算；它不得升级为 Isaac 参数或硬件上限。

## 2026-08-24T12:55:10Z — 可执行候选术语的证据边界

- 按用户固定定义保留 `executable_candidates`：闭合、完整顺序扫掠、`FAST_SURVIVE` 和 `TASK_SURVIVE` 必须同时成立；任务失败候选只进入 `DIAGNOSTIC_ONLY_NOT_EXECUTABLE`。
- `executable` 在本阶段只表示“离线满足进入绑定预检查的资格”，不是已获动态启动授权，更不是研究动态、正式动态或硬件通过；报告固定写 `OFFLINE_ELIGIBLE_FOR_BOUND_PREFLIGHT_NOT_DYNAMIC_PASS`。
- Isaac 入口还会失败关闭检查新 schema、任务门、快筛、完整扫掠、三指接触预测和 rank；缺字段、空列表或诊断候选均拒绝。
- A/B 当前运行只作实现烟雾；A 产物早于最终报告接口，必须以最终同源码同配置重跑后才能形成配对证据。

## 2026-08-24T13:08:26Z — 完整候选池后按停止条件 6 收口

- 最终同源码重算取代 12:55 的实现烟雾：A 的 384 个原始种子没有完整顺序扫掠幸存者；B 有 6 个路径幸存者，但 8 N 下没有 `TASK_SURVIVE`。
- 按用户允许的唯一诊断，对 B 原 6 个路径幸存者做固定 12 N 只读敏感性；最佳最差任务余量为 `0.511753`，仍小于 1，任务通过仍为 0。
- 该实验只证明把 8 N 提到 12 N 会改善余量但仍不足；没有约束活跃性或更高力量对照，不能区分力量上限、接触几何和任务模型谁是最终限制，也不能升级成硬件力量结论。
- 两对象 `executable_candidates` 均为空，`selected_executable_candidate=null`；诊断候选不得进入 Isaac 或被称为最优抓法。
- 依照用户永久停止规则第 6 条，将状态置为 `PARKED` 并关闭研究动态门；未启动 Isaac，不改变 3 rad/s、0.18 rad/s、0.0015 rad/周期、物性、摩擦、50 mm、2 s、真值隔离或硬件授权。

## 2026-08-24T14:46:08Z — METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION

- 用户明确开启 `GRASPGENX_ROUTE1_12H_UNATTENDED`，截止 `2026-08-25T02:46:08Z`；这不是硬件授权，也不降低碰撞、力量、50 mm、2 s 或真值防火墙。
- 旧 A/B 结果只否定轴对称窄候选族；旧生成器及 `candidate_11/33` 保留为消融证据，不再约束新完整六维生产候选。
- 监督建议 1（采纳）：先用单个真实描述器回放并检查 `+Z approach/+X closing → handbase_link`，再冻结最多 5 个描述器；GraspGenX 始终是隔离的离线位姿提案器。
- 监督建议 2（采纳）：只增加一个薄适配层，复用真实手 FK、PAD、完整闭合、12 N 任务评价、bounded IK 和 Isaac；快筛结果不能冒充完整全手/对象/桌面路径证据。
- 监督建议 3（采纳）：首次研究型 Isaac 提前到第 6 小时前；此前后置正式误差场景、MoveIt 场景接入、cuRobo、完整消融和文档美化。
- Lead 选择官方单进程文件输出而非常驻 ZMQ 服务：当前没有跨进程在线请求需求，直接复用官方 Python API 能保持模型只加载一次并减少服务生命周期；不改变官方模型或输出语义。
- `12 N` 作为两对象一致的仿真研究操作上限，旧 `8 N` 保留为消融；不得在 8～15 N 间扫描或将 12 N 声称为硬件极限。
- 研究动态门与旧正式门分开；`hardware_authorized=false`、正式动态和研究动态通过均保持 false，直到实际证据满足。

## 2026-08-24T15:18:00Z — 描述器闭合范围以空手自碰证据冻结

- 旧 V2 的 `maximum_closure_phase=0.75` 不能直接继承为 GraspGenX 描述器动画终点：登记全手网格重放在 9 个固定 `f1j1` 样本上均出现非相邻连杆自碰，最早约在闭合相位 `0.522532`。
- 描述器库采用对象无关的固定闭合相位 `0.50`；按 `0.0015 rad` 最大独立关节步长检查 9 个样本、共 4203 个状态，全部无空手自碰。
- 该数值只定义生成模型看到的 open/half/close 手形，不替代候选的逐指首次接触预测，也不放宽后续完整手—对象—桌面扫掠。
- 选择过程未读取对象 A/B 的抓取结果；两对象使用同一 5 个描述器，连接器物性、12 N 操作上限、速度、穿透容差、50 mm、2 s 和真值隔离均未改变。

## 2026-08-24T16:21:43Z — ARCHITECTURE_CHANGE：SM120 兼容运行时

- 问题：官方锁文件选出的 PyTorch `2.6.0+cu124` 不含 RTX 5070 Ti 的 `sm_120` 内核，真实 CUDA 张量在模型加载前失败。
- 证据：PyTorch `2.7.0+cu128` 的架构列表含 `sm_120/compute_120`，同卡真实 GPU 张量和矩阵计算成功；官方 `pyproject.toml` 明示代码已验证 PyTorch 2.7。
- 最简单替代：保留官方 commit、checkpoint、推理代码和参数，只在独立 `.venv` 覆盖为 PyTorch `2.7.0+cu128`/torchvision `0.22.0+cu128`。
- 时间影响：一次约 29 分钟的大轮子下载；不污染 Isaac Python，不修改系统驱动或 HAMER 环境。
- 监督结论：Academic+Complexity 条件批准；必须再由官方内置手、官方样例和非空有限位姿正对照验收，不能把 GPU 张量通过写成抓取成功。

## 2026-08-24T17:19:05Z — 描述器虚拟基座与官方工作区语义修正

- 只读官方语义审计确认：GraspGenX 返回 canonical gripper base 位姿，现有 `T_object_handbase = T_object_graspgenx @ T_graspgenx_handbase` 和 `+Z` 接近方向正确，不得反转或重复平移。
- 原 KCG 描述器把虚拟原点留在 `handbase_link`，使指尖深度约 `0.39 m`，超过官方常见描述器尺度并触发约 `±0.3058 m` 的生成平移裁剪；这否定旧提案文件，不否定模型或三指手。
- 对象无关最小修正：保持原三轴，把虚拟原点移到手掌接近轴与三根近端关节平均轴向平面的交点；重建后五个描述器指尖深度为 `0.09723–0.10153 m`。
- 旧位姿不得事后平移；已用同一 checkpoint、种子和预算重新推理 A/B。
- 官方点云工作区占用检查必须在每描述器分数保留前执行；它只说明连接器表面进入张开手指内部区域，不替代真实 PAD 接触、碰撞、IK 或任务承载。
- 修正后 A 每描述器 `145–180/472`、B 每描述器 `144–180/472` 通过工作区语义，各保留 128 条；A 的 256 条物理筛选仍为 0 个三指闭合存活，B 的 256 条覆盖诊断仍失败，当前继续做 640 条只读因果区分而不调物性或安全门。

## 2026-08-24T16:39:00Z — SM120 运行时例外通过官方正对照验收

- 固定官方源码、模型和参数未变；`robotiq_3f + banana` 在隔离 PyTorch `2.7.0+cu128` 环境返回 20 个有限六维位姿，关闭上一条架构变更的验收条件。
- 官方 checkpoint 与描述器 revision、文件树、单文件权重和运行输出均绑定哈希；完整第三方仓库和权重不进入主仓。
- 该结论只允许继续 KCG 描述器和两个连接器的离线提案；研究动态门、正式动态门和 `hardware_authorized=false` 均不改变。

## 2026-08-24T18:21:11Z — ARCHITECTURE_CHANGE：跨描述器分层合并

- 问题：每描述器已保留的 d02 顶向分数第 3/7 名安全候选，在 `640→256` 的纯六维最远点步骤被删，而保留下来的 A/B 三指接触项全部扫桌。
- 证据：A 的 d02 顶向 22 条进入适配器，旧合并只留下名次 4/5/6/17/21；该层未读取碰撞，却破坏了前一层固定方向覆盖。
- 最简单替代：只在同一描述器内做六维去重；随后按 `descriptor_id × 官方世界接近方向` 每层取相同整数配额，全部缺额和余量用同一六维最远点补齐到 256。
- 时间影响：一个现有函数内的小修改和一个定向回归；不重新推理、不增加候选预算、不建立框架。
- 边界：A/B 同规则，不读取碰撞、接触、任务或对象 B 成败；GraspGenX 分数仍只在层内保留代表，不替代物理排序。
- 监督结论：Academic+Complexity 复核认为该规则不读取碰撞/接触/桌面真值且比额外手型首选与字典序余数更简单；Lead 采纳，仍须 A/B 同参数结果验收。

## 2026-08-24T18:32:00Z — 固定对象点云抽样身份

- 问题：`trimesh.sample_surface` 未收到 seed，会从系统熵建立生成器；配置里的 `20260824` 因此没有绑定模型实际看到的 8192 个表面点。
- 证据：同一 d02/raw index 跨未绑定点云重生成时，手掌平移差约 `2.95–15.49 mm`、旋转差约 `71.7–75.7°`；旧候选编号和位姿不能复现。此前把矩阵元素差误写成厘米的口径已撤回。
- 最简单替代：把既有固定种子显式传给官方输入前的表面采样，并在每个 proposal 文件记录“中心化点云字节 + 中心”的 SHA-256。
- 影响：必须重生成 A/B；不改官方模型、对象网格、预算、物性、碰撞或动态门，旧未绑定点云结果只作无效诊断保留。

## 2026-08-24T18:49:36Z — 完整控制步加入薄 FCL 失败关闭门

- 问题：旧快筛只查手—桌，手部自碰和非指腹—对象仍为 `UNRESOLVED`，不能把幸存项称为可执行。
- 最简单替代：在现有 `fast_filter.py` 复用公开 python-fcl；9 个注册碰撞 link 检查 28 组非相邻 pair，末端从 authored STL 精确排除 PAD 源面后检查对象。
- 证据：三个末端各 `14192−2479=11713` 个禁碰面；candidate_11 在 `FINGER_1_CLOSURE_0248` 检出 `f1Link3` 非 PAD 碰撞；2 项真实回归通过。
- 复杂度：生产净增约 119 个非空行，文件总 364 个非空行；不新增碰撞引擎、manager 或 H102 路线。
- 边界：FCL 不可用即失败关闭；仍保留“控制步采样而非连续证明”，不改变桌面容差、力量、50 mm、2 s、真值隔离或动态授权。

## 2026-08-24T20:08:58Z — 冻结允许表面 ROI 提案变体

- 用户指令分类仍为 `METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION`；没有改变北极星、硬件授权或动态成功标准。
- 同一固定 ROI 方法的结果为：A `256→3` 个三指接触预测→`0` 个桌面路径安全；B `256→0` 个三指接触预测，未到达桌面、FCL或任务受力。
- 独立尺度复核确认对象与 ROI 都约 47–48 mm，坐标链正反误差最大 `1.61e-7`；约 ±0.45 m 的 handbase 范围由真实 PAD 距手腕约 406–409 mm 解释，不是单位或变换错误。
- 监督结论：命中预登记停止判据后，不再修改面标签、工作区箱、候选偏移或描述器；否则会变成看结果调生成器。
- 当前只可结论为 KCG 长指描述器在已审计尺度特征上超出官方描述器范围、域外推是受支持但未证实的限制解释，且本轮没有研究可执行候选；不能结论为三指手物理上不可抓。
- 研究动态门保持关闭；12 N、摩擦、对象、桌面、3 rad/s、50 mm、2 s和在线真值隔离均未改变。

## ARCHITECTURE_CHANGE — 生产源码超过 1500 目标但不越 2200 硬限

- 监督按非空非注释差异口径核算：物理新增 2145、删除 76、净新增 2069 NLOC；超过 1500 目标，但仍低于 2200 硬限。
- 新文件 1642 NLOC 主要承担可复现描述器构建、隔离官方推理、文件适配与失败关闭；为凑目标删除它们会破坏用户硬要求。
- `controller.py` 当前 693 NLOC 已是既有超限文件，本轮只加 17 NLOC 六维接近方向接线，动态前不做无关重构。
- 其余最大生产文件按 NLOC 为 `graspgenx_adapter.py=525`、`reporting.py=521`、`models.py=492`；适配器经机械压缩为 548 个物理行，均不超过 550 硬限。
- 决定：接受目标例外，不增加新生产功能；只允许真实回归修正、已有代码删除、证据与文档收口，硬限不得越过。

## 2026-08-24T20:43:00Z — 官方手同对象对照只作域区分

- 唯一改变量：固定官方 `robotiq_3f`，对 A/B 同一完整对象点云、种子和 256 预算各运行一次 diffusion；不把结果接入 KCG 候选链。
- A/B 各得到 `256/256` 个有限刚体位姿，证明对象点云本身不会令官方学习分支整体失效。
- 因跨手分数不可直接比较，该结果只提高“KCG 描述器/手几何适配受训练域外推限制”的可信度；不能证明唯一根因，也不能升级碰撞、承重、Isaac 或抓取证据。
- 决定：生成器、描述器、允许面与偏移继续冻结；后续只做当前源码绑定的 A/B 重放和证据收口。

## 2026-08-24T20:43:00Z — 桌面扫掠与整手 FCL 统计分离

- 旧对象 B 的 6 个候选确实通过完整控制步手—桌检查，但随后均由 `f1Link3` 非指腹表面在第一指闭合 step `269–272` 碰到对象。
- `sequential_closure_sweep_pass` 只表示手—桌扫掠，不得被后续自碰或非指腹—对象理由改写；总体 `FAST_REJECT` 仍由任一硬理由决定。
- 这是报告/数据语义修正，不删除碰撞、容差或失败关闭门，也不把这 6 个候选升级为可执行。

## 2026-08-24T20:48:00Z — 生成点云与下游碰撞几何分开命名

- GraspGenX 提案和官方 open/half 工作区可见性实际只读取登记允许面 ROI 点云；完整对象网格未进入这两个官方调用。
- 完整对象网格用于后续 V2 顺序闭合与控制步 FCL，因此原 `collision_geometry_scope` 改写为 `downstream_collision_geometry_scope`，不得冒充 GraspGenX 内部碰撞范围。
- 旧 proposal v1 文件不改写；适配器只把其旧字段失败关闭地归一为“下游范围”，新生成文件使用不含歧义的新字段。
- 同时把所有尚无机械臂 IK/路径证据的 task-eligible 候选固定为 `offline_task_gate_passed=false`，防止未来误接动态入口。

## 2026-08-24T21:49:19Z — 描述器配置箱统计与训练分布分开命名

- `0.06730544 m` 来自当前官方仓库 32 个程序化配置中的 `revolute_3f_v1_1004`，是 open/half-open `sweep_volume.extents[1]` 的全尺寸最大值；不是 `points.json` 整手点云跨度。
- 26 个运行时 `x_grippers` 配置的 open 同分量最大为 `0.060 m`；KCG 五个描述器为 `0.117526–0.305499 m`。
- checkpoint 声明 `train_gripper_split=proc_v1_train_32`，官方 README 说明 released model 使用 32 个程序化手训练，但本地没有完整训练数据分布；因此只写“已审计配置资产尺度差异”，不写完整训练分布上界或唯一因果。
- 该澄清不改变描述器、候选、碰撞门或离线结果；域外推仍只是受支持但未证实的解释。
- 本条取代 `2026-08-24T20:08:58Z` 和 `2026-08-24T20:43:00Z` 中“官方描述器范围/训练域可信度提高”的旧措辞；旧记录保留但不得再作为当前结论引用。

## 2026-08-24T21:49:19Z — 旧动态 2 mm 接受阈值不得证明无穿透

- `maximum_table_penetration_m=0.002` 只由运行后评价器检查非 settle 阶段对象最低登记点，并非控制器或 PhysX 参数；当前 GraspGenX 路线从未执行该门。
- 该值没有登记来源，且远大于离线手—桌 `1e-5 m` 数值容差；未来动态启动前必须完成分阶段容差和来源审查。
- 本轮不在没有动态候选时改动冻结配置或重算离线证据，只把它登记为 `LEGACY_UNVALIDATED_FUTURE_DYNAMIC_ACCEPTANCE_BOUND`；它不能支持“无未授权穿透”。

## 2026-08-24T22:10:00Z — 官方默认组合口径纠正

- 监督审计确认旧运行把 `run_planner_on_object()` 低层默认与 object-PC 示例默认混合使用，不能称为“一套官方默认组合”。
- 采用当前官方 `demo_object_pc.py` 的默认：阈值 `0.7`、Z 偏移 `[-2, 0] cm`、`dense-topandside`；其余保持官方示例默认，只有用户预登记的 `256` 生成预算例外。
- 该修改不是按 A/B 结果调参：参数来源在重跑前固定，两个对象同值，旧结果保留，不修改描述器、网格、允许面、碰撞、力量或安全门。
- 重跑后若仍无路径安全候选，不再尝试第二套数值组合；官方 scene-PC 碰撞过滤另作一次薄接法合规检查。

## 2026-08-24T22:47:00Z — 官方场景过滤只作提前拒绝

- 采用官方 object-PC 示例默认 `0.02 m / 2000 手表面点 / 最多 8192 场景点`，实际有限桌面网格为 8160 点；A/B 同参数。
- 输入手几何来自 9 个登记 link 的真实张开构型与 FK，不使用 `from_sweep_volume()` 内部 dummy box；目标连接器点云必须排除，避免误杀预期指腹接触。
- 输出只能称 `OFFICIAL_COARSE_OPEN_HAND_SCENE_PC_REJECT_ONLY`：它不是有符号网格距离、闭合扫掠或完整安全证明，后续 V2 门继续失败关闭。
- 复杂度监督否决新增独立模块；Lead 在现有离线 runner 内作薄接线，并删除重复哈希实现，使生产源码物理新增仍低于 2200 硬限。

## 2026-08-24T23:09:42Z — 点云可见性降为审计，不再替代真实物理门

- 固定重放证明 A/B 各描述器的刚体变换、米制单位、open/half 字段与官方函数逐项一致；旧硬门仍把 A 的 `12/31/387/16/101`、B 的 `4/2/486/0/97` 个原始提案压成仅一个描述器。
- 官方 object-PC 示例不调用该函数；它位于 dataset utility，用于点云视角自遮挡过滤，不是手指闭合、碰撞或承重门。用户预登记顺序则明确要求每描述器先按最高分保留，再进入项目真实快筛。
- 双监督批准删除生成器和适配器中的两道重复箱体占用硬门：visibility 只保留计数审计；每描述器按 `(-score, raw_index)` 稳定保留最多 128，之后仍经过 scene-PC 粗筛、完整闭合、登记全手 FCL、12 N、IK 和路径门。
- 这是方法接线纠正，不是调对象、阈值、箱体、摩擦或力量；旧单描述器产物原样保留并降为错误硬门诊断，新结果写入独立目录。
- 该纠正不能证明新候选会三指接触、不碰撞、承重、有逆解或在 Isaac 抬升；只有后续真实物理门的数据能回答这些问题。

## 2026-08-24T23:45:19Z — 闭合预测按真实控制步长采样

- 旧 `phase_sample_count=25` 令五个手型的单步独立关节变化约为 `0.030–0.040 rad`，是冻结动态步长 `0.0015 rad` 的 20–27 倍，可能跨过短暂的首次接触。
- 只对 GraspGenX 路线启用 `DYNAMIC_CONTROL_STEP_BOUNDED`：实际区间数取旧 24 个区间与 `ceil(独立关节总行程/0.0015)` 的较大值；旧 V2 基线仍保持固定点数，历史证据不改写。
- A/B 用当前提案全池和新采样重放后，三指闭合幸存数仍均为 0；因此当前零候选不是 25 点粗采样或 scene-PC 粗筛造成。
- 该结果只否定当前固定提案池内的三指允许接触，不证明完整六维空间无解，也未到达桌面/FCL、12 N、IK 或 Isaac。

## 2026-08-24T23:45:19Z — 坐标链通过，登记 `base_rotation` 潜在语义债务

- 独立复核确认 `object_from_handbase = object_from_graspgenx @ graspgenx_from_handbase` 与官方基座输出语义一致；A/B 全部 487 条候选的变换闭合误差最大约 `55/63 nm`。
- 当前 `base_rotation` 字段含约 `289 mm` 平移，而官方 wizard 将它作为纯旋转；当前 sweep-only 推理接口不读取该字段，因此它没有影响已生成提案或闭合结果。
- 本窗口不为未触发的 viewer/mesh-loader 路径重建描述器和全部证据；若未来使用这些官方入口，必须把纯旋转字段与完整 `graspgenx_from_handbase` 变换分开并重新绑定。

## 2026-08-25T00:03:41Z — 生产生成输入恢复为完整对象网格

- 当前 runner 实际只从允许面 `faces[allowed]` 采样，令 GraspGenX 把允许面 ROI 当成整个对象；这会改变点云中心、外形和 OBB，不符合本路线“真实完整对象网格提出六维抓法”的分工。
- 双监督批准替换式修正：GraspGenX 从完整登记网格按面积固定采样 8192 点；允许面索引、面积和哈希继续保留，只供后续 PAD 首次接触和任务语义门。
- A/B 使用同一改法，模型、描述器、随机种子、预算、官方推理参数、碰撞、12 N、50 mm、2 s 和真值隔离均不变；这不是参数扫描。
- 旧 ROI 提案和 `444ff16` 原样保留为消融；生产 runner 不保留 ROI/FULL 双分支，避免两套当前方法。

## 2026-08-25T00:32:00Z — 模型置信度退出物理检查前的硬门

- 完整对象固定池在 scene-PC 粗筛前后均为 A `207→0`、B `200→0` 个三指闭合幸存者，因此桌面粗筛不是零候选原因。
- 复核官方底层接口确认 `run_planner_on_object()` 与 `run_graspmoe()` 默认 `grasp_threshold=-1.0`；当前 `0.7` 来自 object-PC 演示程序，不是规划接口默认。
- 本路线不允许 GraspGenX 置信度充当物理可行性门；因此取消 `0.7` 绝对阈值。分数仍按预登记规则为每描述器固定 128 项排序，物理合格后的最终选择中只作后置并列信息。
- 生产值一次性改为 `-1.0`，含义是保留有限提案后仍按每描述器最高 128、合并最多 256 的固定预算裁剪；不是阈值扫描，也不改变模型、种子、描述器、碰撞、12 N 或动态标准。
- `0.7` 全物体结果原样保留为分数硬门消融；若无硬门的固定预算仍无三指允许接触候选，不再尝试第二个分数阈值。

## 2026-08-25T00:58:00Z — 不用候选后处理掩盖固定池接触缺口

- A/B 粗筛前固定 256 池仍均为 0 个三指允许接触；网络实际收到的五组工作箱张量与 descriptor manifest 完全一致且彼此不同。
- 工作箱含有对象点不等于三指真实可接触：A/B 分别有 69/56 个工作箱占据姿态，但最多只有一块允许指腹接触。
- 不在看到 B 的毫米级残差后临时加入对象专用平移、六维局部投影、信赖域或新优化器；这会改变“全局提案→物理筛选”方法并需要新的预登记边界。
- 继续只允许检查描述器规范或固定原始提案池中的系统性接口遗漏；没有三指接触候选就保持 Isaac 研究门关闭。

## 2026-08-25T01:25:00Z — 修正 OBB 提案与非居中工作区的接口

- 官方 GraspMoE 的 OBB 放置逻辑使用工作区宽度和指尖深度，但没有把 KCG open sweep box 的横向 x/y 偏移带入输出位姿；五个 KCG 描述器的 x 偏移为约 `-22.7～-38.0 mm`，因此这是可由描述器本身推导的系统性接口遗漏。
- 统一规则为 `O_T_H' = O_T_G · Trans_G(-offset_x,-offset_y,0) · G_T_H`；只作用于 `obb` 分支，diffusion 和 z 不变，A/B 使用完全相同规则。z 已由官方 fingertip depth 处理，不能重复补偿。
- 修正后 A 仍为三指闭合 0；B 从 0 变为 2 个三指允许接触，但两项均在第一指 `FINGER_1_CLOSURE_0164` 由 `f1Link3` 首次越过容差，第三指阶段由 `f3Link3` 达到最差约 `-4.46 mm`，所以仍无路径安全、任务合格或动态可执行候选。
- 该结果证明横向接口遗漏确实影响了闭合结果，但不能把它写成当前全部失败的唯一根因，也不能把“两指/三指接触”升级为抓取或承重成功。
- 监督否决在剩余窗口临时加入六维局部投影：它会把“全局提案后物理筛选”改成新的非光滑优化方法，需要预先冻结搜索边界、停止规则和独立评价，不是本接口修复的自然延伸。

## 2026-08-25T09:11:20Z — 开启完整掌面构型联合搜索

- 用户指令分类为 `METHOD_CORRECTION + FULL_PALM_CONFIGURATION_SEARCH + VISUAL_TRUTH_DIAGNOSIS + AUTONOMOUS_ISAAC_VALIDATION`；它不改变三指允许接触、无未授权穿透、50 mm、2 s、在线真值隔离和仿真研究边界。
- 旧五构型与旧轴对称搜索的零可执行结果只否定其有限搜索族，继续保留为消融基线；不再据此 PARK 整个项目。
- 新路线在 `f1j1∈[0,1.57] rad` 上取 91 个固定等距点，并对每个角度运行同一 GraspGenX 与同一物理筛选；学术口径是“完整合法区间离散覆盖与有界局部精修”，不是连续空间穷尽。
- `f3j1` 的 `+1` mimic 保持不变：其局部旋转轴在 handbase 中与 `f1j1` 物理反向，同号角度产生正确的对向展开。
- 先做旧 B 候选 131/133 的可见网格、碰撞网格、桌面和掌面角同帧核对；图片只能证明几何身份与可见一致性，不能证明动态或承重。

## 2026-08-25T09:18:00Z — 双监督对搜索预算的三项修正

- 接受级联预算：91 角各 `128→64→最多8`；27 种预构型在每个六维种子内共用逐级拒绝，跨角度最多 64 个精修种子/对象，每种子 Stage A+B 合计最多 300 次评价。禁止把这些上限相乘成笛卡尔积。
- 接受碰桌语义修正：掌座碰桌可立即拒绝；张开指节碰桌只有在 27 种预构型的远端接近与剩余闭合均失败后才能拒绝。真实指腹接触用最近允许三角面法向，非指腹/自碰复用已有 FCL。
- 接受救援式精修：原始候选若已通过完整几何、12 N 名义任务、IK 和抬升路径，立即进入 Isaac；只有直接候选不足时才启用一次有界局部精修。
- 不新增 manager/contract/ledger 类系统；优先增加一个小型数值搜索模块，避免继续扩张已接近规模上限的 GraspGenX 适配器。

## 2026-08-25T09:47:00Z — 旧 B 候选属于真实扫桌而非碰撞代理误杀

- 131/133 在全部重放状态保持 `q_p=0.157 rad`；排除掌面构型在候选链丢失这一解释。
- 原始可见网格与注册碰撞网格对第一次越界和最深穿透给出相同连杆、见证点和间隙；两候选永久归类为 `REAL_INTERMEDIATE_HAND_TABLE_SWEEP`，不调整桌高、容差或候选坐标。
- scene 配置与 authoring 定义中的三条桌面坐标链一致，尚未实时查询 Isaac；不为本问题修改 Isaac 场景。
- 接触预测的稀疏 PAD、径向法向和空 eligible 缺口仍单独修正，因为它们影响新斜向候选；不得把这项方法修正写成能够救回 131/133。
- 视觉脚本为单次可复现实验入口，虽达到 550 行硬上限但不进入生产数值链；收口时优先删除重复渲染代码，动态之前不为美化扩张。

## 2026-08-25T10:29:22Z — 精确指腹材料外法向与 `q_p` 失败关闭

- 决策分类：`LOCAL_IMPLEMENTATION_DECISION`；不改变北极星、物理标准或硬件授权。
- 91 个描述器采用 schema v2，并把 `palm_configuration_rad` 作为候选身份的一部分；v1/v2 backend 与 schema 一一绑定，禁止静默回退。
- 真实接触查询复用完整 PAD/对象 FCL；对象面法向必须乘已绑定 material-boundary orientation certificate 的逐面符号，不能继续使用径向代理。
- 只有全局最近见证已在接触距离内但运动方向不兼容时，才使用 1024 个确定性 PAD 表面样本的完整对象最近点后备；该口径不是连续 PAD 曲面的数学证明。
- FCL 相交、证书/哈希不符、`q_p` 缺失或篡改、允许集合为空均失败关闭，不得升级成有效接触。
- 学术与复杂度监督结论：APPROVE；尚未批准真实 v2 提案、完整级联、任务力学、IK、路径或 Isaac 结论。

## 2026-08-25T10:59:26Z — 预构型路径必须先于接触预测独立检查

- 决策分类：`LOCAL_CORRECTION`；不改变完整掌面方法、物理标准或动态成功定义。
- 否决 runner 内只按最终桌面间隙选 27 组合的实现：它没有验证远端预闭合、完整接近、自碰、非指腹碰撞和 PAD 提前接触，因果上会产生错误淘汰或错误保留。
- 最小替代是在现有 `fast_filter.py` 增加一个公开批处理入口，内部复用唯一 FCL/FK/桌面内核；runner 不允许导入下划线私有函数或复制碰撞器。
- descriptor 的 open/half sweep AABB 是 GraspGenX 条件输入，未覆盖完整后续闭合；AABB/对象球分离保留为 `DIAGNOSTIC_ONLY`，不得称无接触证明或用于硬拒绝。

## 2026-08-25T11:38:50Z — 64→8是预算分配，直接级联与救援精修分账

- 每角64条提案中的最多8条进入27种预构型路径和精确闭合；其余只标记预算未评估，不得归类为碰撞或不可抓。
- 混合生成分支至少各保留一个预算席位；官方scene-PC、手可达球和GraspGenX分数只参与角内预算排序，不构成物理通过或拒绝。
- 直接级联按每种子27次预构型加1次精确闭合记账；控制步是同一次路径评价的内部状态。每种子300次几何评价只保留给直接候选不足时的预登记救援精修。
- 该决定不改变两对象、12 N、碰撞、0.0015 rad、50 mm、2 s或在线真值隔离。

## 2026-08-25T11:57:12Z — 全池前修正三项物理排序语义

- 决策分类：`LOCAL_CORRECTION`；不改变完整掌面路线、冻结物理边界或动态成功标准。
- 远端路径从仿真实际初始 `q_p=0` 开始，以每周期不超过 0.0015 rad 到候选掌面角，再设置三指预构型；接近仍明确为有限采样快筛。
- 预构型先最小化三块 PAD 中最远一块的对象距离，再比较桌面余量；scene-PC 和 AABB 只作预算偏好，不作物理拒绝。
- 每个生成分支只保留一个代表席位，剩余席位按全局物理诊断填充；所有精确闭合幸存者进入 12 N，不在任务评价前截成每角一个。
- 一个已选预构型精确失败只否定该相位；其他路径安全相位记录为预算未评估，不冒充整个六维位姿不可抓。
- 为满足 120 分钟全池上限，同一候选完全相同的远端掌面角段只查询一次，27 个分支仍各自检查预构型与采样接近；这是等价去重，不减少物理状态。
- 5824 条候选的 phase-0 预算诊断只检查真实预抓快照，不检查路径；完整远端掌面角路径只对每角进入 Top-8 的候选执行。
- 离线检查的 handbase 固定在远端，不能冒充 Isaac 当前机械臂与手同步 home→far 轨迹已经验证；动态前必须对齐控制顺序。

## 2026-08-25T12:40:26Z — 长级联改为逐角原子恢复

- 决策分类：`LOCAL_CORRECTION`；首条对象 A 命令因 1490 s 执行上限中断，不是候选或物理失败。
- 不改 91 角、每角 64→8、27 预构型、0.0015 rad、碰撞、接触或 12 N；只在每个完整掌面角后原子保存累计事实。
- 恢复时重新绑定对象 ID、proposal、完整候选物理姿态序列、选中相位的关节值，以及配置、runner、级联、快筛、闭合预测、表面接触、模型和搜索 manifest 哈希；任何变化均失败关闭。
- 未完成角不落完成标记；检查点不携带全量预算延后明细，恢复时从同一候选和诊断重新确定性计算。

## 2026-08-25T09:58:00Z — 旧对象 A 描述器只作接触缺口对照

- 五个旧描述器各取 3 个近接触候选，其中只有两条候选各形成 1 个允许指腹接触，没有三指接触候选。
- 该结果只说明旧五描述器候选族覆盖不足；不得外推为完整掌面角无解，也不得称这些诊断候选为可执行抓法。
- 后续生产选择只接受 91 掌面角新提案经过真实面法向、完整顺序闭合、任务载荷和 IK/路径门后的候选。

## 2026-08-25T15:15:34Z — 用户修正为无指甲、内侧抓持面和高度先行方法

- 指令分类：`TASK_MODEL_CORRECTION`；新窗口为 `NAILFREE_HEIGHT_PROJECTED_12H`。它不改变 50 mm、2 s、碰撞、安全速度、力量和在线真值隔离，也不授权硬件。
- 生产手型改为用户确认的最终实物配置 `CONNECTOR_GRASP_NO_NAIL`；旧带指甲模型、旧蓝色 PAD 语义和 89/91 角离线结果只读保留为消融。
- 生产接触语义改为 `TASK_GRIP_SURFACE`：允许经审计的无指甲内侧抓持表面，继续禁止外侧、背面、关节壳体、拆卸安装面、尖锐边缘和原指甲。
- 高度可行域必须在每角 Top-8 前：由完整路径和有限桌面求 `H_table`，由三指真实接触求一个或多个 `H_contact` 闭区间，交集非空后投影并重新计算全部接触与闭合事实。
- A/B 使用相同无指甲手型、91 角、27 预构型、高度算法、碰撞/受力规则和控制参数；不得根据 B 回调。

## 2026-08-25T15:27:00Z — 三监督审查后的三项简化

- 学术与复杂度监督结论：`APPROVE_WITH_CHANGES`。只采纳三项能减少复杂度并前移物理证据的修正。
- 无指甲资产采用一次性可追溯转换：绑定原 STL 哈希和唯一 TIP 连通分量，保留源面身份；不建立通用分割、修网格或证明框架。
- 三末节质量、质心和惯量按用户允许的来源优先级 2 计算：由旧质量和原网格体积导出同材料有效密度，再扣除 TIP 的体积质量属性和用平行轴关系重算。结论只称仿真模型估计，不冒充真实硬件标定。
- 路径状态相对 handbase 生成一次，有限桌面最低安全高度由投影运动包络解析计算；每个六维种子最多两种预构型进入精确高度搜索，其余为 `BUDGET_NOT_EVALUATED`。
- 无对象 Isaac 资产与关节烟雾测试前移到无指甲资产完成后；第一次候选动态前不做可选完整消融，也不引入第二套规划器。
- 明确否决通用网格修复器、`5824×27×多高度×全路径` 笛卡尔积、新连续碰撞证明器和动态前的论文图美化。

## 2026-08-25T16:17:00Z — 凸分解不得填回已拆卸指甲凹槽

- 决策分类：`LOCAL_CORRECTION`；不改变无指甲任务模型、物理成功标准或硬件授权。
- 三末节精确可见网格各删除唯一 TIP 的 1076 面；同密度仿真质量估计约 0.057130736 kg，不能冒充硬件标定。
- CoACD 64 块覆盖 365–377 个距保留原网格超过 10 µm 的 TIP-only 样本，双向采样表面偏差也超过 1 mm，因此不得进入生产 Xacro/Isaac。
- 首轮 Isaac VHACD 请求的 128 顶点超出后端 8–64 合法范围；该次输出只作诊断，不把 API 接线错误算成高保真方法已失败。
- 只再执行一组由 1 mm 审计界反推的合法高保真 VHACD 参数，不做参数扫描；未通过则继续失败关闭动态碰撞资产。

## 2026-08-25T17:36:00Z — 指甲身份、数值闭合体和惯性结论纠正

- 决策分类：`LOCAL_CORRECTION`；纠正 16:17 记录，不改变任务模型、安全门或硬件授权。
- 可拆卸组件是指甲壳体 155 加两个安装柱 156/157；每末节共删除 2356 面并精确保留 11836 个源面。仅删除 1076 面的旧数字不再代表完整无指甲模型。
- 精确保留可见网格用于接触语义和离线原始三角网格检查；0.25 mm 体素闭合体只用于数值体积估计，不是精确或保守动态碰撞体。
- 闭合体 `raw→closed` 最大偏差为 1.702–1.716 mm，且仍含 332–351 个位于保留面 0.262 mm 内的拆卸面采样点；不得声称删除区完全不回填。
- 质量按相同闭合算子的体积比估计；质心/惯量以旧 URDF 为基线扣除拆卸体质量矩。全部数值仍是 `PROVISIONAL_NOT_RUNTIME_BOUND`，不冒充硬件标定。
- `TASK_GRIP_SURFACE` v2 只允许末端体组件 0 和旧蓝色 PAD 组件 75；关节壳体 152/153/154 允许面为 0。静态语义通过不证明对象接触或动态抓取。
- 高保真凸分解仍未接受；停止继续调分解参数，先用精确原网格推进高度可行性和双对象离线链，Isaac 门保持关闭。

## 2026-08-25T18:40:00Z — ARCHITECTURE_CHANGE：解析高度外包区间加投影点精确复核

- 问题：`5824×最多2×33×完整逐指闭合` 实测约 `26.49 s/种子`，会超过 12 小时窗口；解析闭区间再用 33 点采样还会漏掉窄区间。
- 替代：逐控制步扫描三指 `TASK_GRIP_SURFACE` 支撑盒，解析得到必要外包 `H_reach_outer`；与保守 `H_table` 直接求交，不再重新采样解析区间。
- 投影点必须重新运行真实任务抓持面接触、完整远端预构型、接近和逐指闭合原始网格 FCL；只有该点通过才称离线高度可行。
- 影响：正向结果继续失败关闭；外包和保守桌面盒可能误拒，投影点失败也不能证明整个高度区间或六维种子无解。
- 最新用户固定的 Top-8 物理顺序不含旧 OBB/diffusion 席位配额；生成分支只保留诊断统计，不覆盖高度/接触优先顺序。
- 学术/复杂度监督：`APPROVE_WITH_EVIDENCE_LIMITS`；不得称完整 `H_contact`、连续碰撞证明、可执行候选或动态成功。

## 2026-08-25T22:17:59Z — 60°对向抓取族与研究型复合凸路线重开

- 指令分类：`ROOT_CAUSE_METHOD_CORRECTION + OPPOSITION_GRASP_FAMILY + MULTI_WITNESS_CONTACT + RESEARCH_COLLISION_ASSET + AUTONOMOUS_ISAAC_EXECUTION`；北极星物理成功标准不变，开启 `OPPOSITION60_ISAAC_12H`。
- 上一轮 `PARKED` 只否决“精确开放 STL 直接做 SDF/无依据封口”；用户现明确授权经误差与删除区审计的复合凸碰撞体用于研究动态，因此该停止原因重分类为 `RESEARCH_COLLISION_ASSET_ROUTE_REOPENED`，正式碰撞线仍失败关闭。
- 约 4°的 `graspgenx_659` 仅保留为旧算法对照；本轮优先对象 B，重点搜索 `q_p∈[45°,75°]` 的一指对两指布局，不能据旧结果宣称机械规律。
- 对向族每个六维种子完整精查 27 种预构型；高度使用首次接触即停条件化路径，桌面硬门使用原始网格精确距离，接触使用多见证与对象抓持中心。
- 研究动态先做局部手—连接器，再做完整机械臂；复合凸负责 PhysX 接触，运行后以精确无指甲网格重放审计。`hardware_authorized=false`、`formal_dynamic_pass=false` 保持。
- 初始学术/复杂度监督结论：`APPROVE_WITH_3_CHANGES`。先在固定 `q_p=60°` 下逐项比较同一组 27 种预构型的旧门与新门；一旦出现精确几何正对照就优先进入局部 Isaac，再按同规则扩展 45°～75°，不删减最终覆盖。
- 多见证接触直接复用每指已经登记的 33 个 `patch_indices`，确定性选 16 个表面片；不重写分片器。七变量精修先用直接锚点和有界局部优化，只有直接锚点无三指接触时才启用固定小预算全局优化。
- 旧高保真 VHACD 与组件凸仍覆盖已拆除指甲区域，不能直接绑定研究动态。新研究复合凸只允许一次基于 11836 面无指甲源网格的确定性构建，并以删除区占据、任务抓持面/桌面朝向误差、源哈希、初始穿透和 PhysX 健康为接受条件。

## 2026-08-25T23:03:52Z — 60°生产正对照前关闭三项剩余假拒绝

- 分类：`LOCAL_IMPLEMENTATION_DECISION`；不改变北极星、12 N、安全速度、50 mm、2 s或硬件授权。
- 多见证遍历每指全部33个已登记面片，再按距离保留16个；完整对象最近距离独立守卫禁抓面先接触，不能用较远允许面覆盖真实首次接触。
- 接触高度使用33个粗样本与最多32次、0.1 mm状态边界二分；没有找到见证只能写采样未解析，不能冒充空可行域证明。
- 12个周向锚点按低/中/高轴向层各保留4个；候选ID不再编码随后会被重绑定的预构型。
- CoACD、固定PhysX shrink-wrap及确定性幽灵凸块删除均未通过预登记无指甲保真门，`runtime_binding_accepted=false`；这些失败不等于60°抓法失败。

## 2026-08-25T23:42:56Z — 60°静态检查先关闭重复查询成本

- 分类：`LOCAL_IMPLEMENTATION_DECISION`；不改变对向抓取方法、接触语义、碰撞门或动态目标。
- 完整27预构型在1500秒命令上限中断且没有检查点，因此不能写0幸存；单预构型对照完整结束后才获得物理拒绝分布。
- 单组129个高度探针产生52745个闭合相位查询；全部面片查询占127.16秒，是当前主要规划开销。
- 允许的最小优化只是在完整对象距离仍大于既定接触阈值时早退；进入阈值后仍必须查询全部33面片、禁止面和真实法向。
- 优化前后若接触分类不一致立即回退；不得以减少采样、放宽距离或跳过禁止面换取速度。
- 实测优化前后完整高度搜索JSON一致，单组耗时缩短36.2%，监督批准；该结论只适用于当前离散控制步检查，不升级为连续碰撞证明。
- 27种预构型先按原确定性规则完整排序，再用半开区间分片执行；三个9项分片已由测试证明无重复、无遗漏。静态入口为415行，超过400行目标但低于550行硬限；监督批准在动态前保留同一入口，因为拆分只会增加一次性接线而不减少物理复杂度。

## 2026-08-26T00:37:58Z — 首锚点27预构型与对象禁抓面复核

- 分类：`ROOT_CAUSE_REVIEW`；不改变60°路线、对象接触语义、12 N、安全速度、50 mm、2 s或硬件授权。
- 周向0°/低轴向首锚点的27种预构型已由4个确定性分片无重叠完成：3477个高度探针中2808个无接触、669个只有第一指接触，未观察到二指或三指同时接触；全部结果仍按离散采样未解析记为 `UNRESOLVED`，不得称该锚点连续空间无解。
- 失败阶段为：1782次第一指先碰禁止面、1026次第一指无有效接触、669次第一指接触后第二指先碰禁止面。face21232是第二指子集最高频面，其几何是外圈凸齿/槽口交界的窄切向侧壁；外法向与局部径向绝对点积0.136，邻接真实外圆允许面，且不是方向证书或outer-envelope计算错误。厂商特征名称未由无面名STL确认，按当前径向抓持带规则继续失败关闭。
- 禁止按对象B白名单加入face21232、降低径向法向阈值或修改对象物理参数。第二指子集显示该锚点会指向真实禁止特征，但总体首先由第一指禁面/无接触主导；整个60°族未被证明无解。
- 下一最小区分实验固定为保留锚点index 1（周向30°/中层）与同一 `[0.2,0.2,0.2]` 预构型；它只检查错开外圈重复凸齿后第二指能否进入允许圆柱带，不根据结果选候选。

## 2026-08-26T01:13:00Z — 60°三指接触成立但与桌面安全高度冲突

- 分类：`ROOT_CAUSE_REVIEW + MILESTONE_PROGRESSION`；不改变接触语义、1 mm桌面余量、12 N、安全速度、50 mm、2 s或硬件授权。
- anchor1在世界handbase高度 `0.628937548 m` 出现三指允许接触，三块均为旧蓝色PAD和对象允许径向面；这证明60°一指对两指手—物几何存在离线接触见证。
- 接触条件化完整路径需要handbase至少 `0.631656067 m` 才满足1 mm桌面余量；单独上投 `2.718520 mm` 后，接触丢失并先触及下侧斜肩face19702。因此当前阻塞是桌面与接触的联合位姿可行性，不是三指行程无解。
- 按用户路线进入一次固定anchor/固定预构型的有界七变量局部精修；分阶段比较米和弧度，不加权混合，总评价不超过300、墙时不超过30分钟。

## 2026-08-26T02:15:00Z — anchor1有界搜索未闭合桌面余量，回到预登记锚点覆盖

- 分类：`ROOT_CAUSE_REVIEW + ROLLBACK_TO_PREVIOUS_VALID_STAGE`；不改变三指接触、1 mm桌面余量、12 N、安全速度、50 mm、2 s或硬件授权。
- anchor1固定 `[0.2,0.2,0.2]` 的300次有界评价中，最好点仍只有 `0.441636 mm` 完整路径桌面间隙，距硬门差 `0.558364 mm`；它保留三指允许接触、预抓取和快筛。
- 该结果只是有界未找到完整见证，不是连续无解证明；不否定60°抓取族、其他11个锚点或其他26种预构型。
- 不继续增加anchor1的优化预算；回到预登记锚点覆盖，先检查index 2的高轴向/60°周向组合和同一预构型。它是高信息联合探针，不把结果单独归因于轴向高度。

## 2026-08-26T02:32:00Z — anchor2成为60°桌面安全三接触离线正对照

- 分类：`MILESTONE_PROGRESSION`；不改变接触语义、1 mm桌面余量、12 N、安全速度、50 mm、2 s或硬件授权。
- anchor2在同一 `[0.2,0.2,0.2]` 预构型下投影到handbase世界高度 `0.640675321 m`，三指首次接触停止相位约 `0.497559/0.403027/0.497559`。
- 完整逐指路径最小桌面间隙 `10.019253 mm`，三块接触为登记内侧面与对象允许面，没有禁抓面先接触或快筛碰撞原因。
- 该结果使60°手—物几何正对照成立，但它尚不是12 N任务候选、IK路径、Isaac接触或动态抓取成功。
- 因anchor1到anchor2同时改变周向与轴向，只能说预登记组合消除了冲突，不把原因单独归于轴向高度。

## 2026-08-26T02:42:00Z — 名义研究任务与完整扰动门分层

- 分类：`EVIDENCE_SEMANTICS_CORRECTION + MILESTONE_PROGRESSION`；不改变任务集、12 N或研究/正式成功标准。
- `nominal_gravity_lift_balance_pass=true` 表示12 N下名义重力和抬升本身有可行接触力分配；`nominal_parameter_task_margin=0.339826` 表示在此基础上只能承受0.340倍完整六方向登记扰动。
- 因此anchor2可进入名义研究动态的后续准备，但`TASK_REJECT`、鲁棒候选与正式动态仍保持失败关闭。
- bounded IK通过只证明冻结对象位姿下的handbase目标有数值逆解，不证明接近、全臂碰撞、携物抬升或Isaac动态。

## 2026-08-26T03:03:44Z — 新鲜PhysX身份纠正与残差vertex64静态资产

- 分类：`ROOT_CAUSE_REVIEW + EVIDENCE_IDENTITY_CORRECTION + MILESTONE_PROGRESSION`；不改变碰撞误差门、删除区、12 N、安全速度、50 mm、2 s或硬件授权。
- 历史与新鲜固定参数PhysX运行逐块复现相同192个凸块SHA；命中拆卸区的真实身份是f1 `[24,31]`、f2 `[49]`、f3 `[39,56]`。旧 `2/1/3` 中的3没有测量依据，纠正为两次独立观测的 `2/1/2`，不是安全门放宽。
- 每删除一个命中凸块，只从它对应的保留无指甲真实表面补一个最大面积安全连通残差凸包；PhysX固定64顶点限制统一由quadric简化实现，不逐指调参。
- 最终三末节各64块，删除区占据均为0；所有凸块凸、闭合、有限、正体积且不超过64顶点；全局、TASK_GRIP_SURFACE和60°桌面侧的9个p95门全部通过。
- 该结果只升级为静态研究碰撞资产候选；运行时导入、初始穿透、60°重放和PhysX健康尚未执行，`runtime_binding_accepted=false`、动态与正式门继续关闭。

## 2026-08-26T03:41:35Z — 复合凸资产只关闭Isaac运行时导入门

- 分类：`MILESTONE_PROGRESSION + EVIDENCE_SCOPE_GUARD`；不改变无指甲几何、碰撞误差门、12 N、安全速度、50 mm、2 s或硬件授权。
- 完整无指甲手臂USD在Isaac 6.0.1 GPU物理中推进3步；15个运行时DOF、4组USD mimic、三末节`64/64/64`个凸碰撞块和审计质量/质心/惯量均与绑定一致。
- PhysX场景读回1个articulation、17个刚体和206个碰撞shape；found/lost与total容量为`8192/16384`，观测峰值`128/0`，容量警告和PhysX错误都为0。
- 这只足以使`ISAAC_IMPORT=true`。对象B尚未加载，因此`INITIAL_PENETRATION=false`、`OPPOSITION60_REPLAY=false`、含对象`PHYSX_HEALTH=false`、`runtime_binding_accepted=false`；不宣称实际接触或抓举。
- 采纳监督建议的最简单路线：从同一已替换碰撞/惯性的URDF树生成hand-only articulation，先固定handbase做初始穿透和60°逐指闭合，通过后才进行受控handbase抓举；这不证明完整机械臂路径。

## 2026-08-26T03:53:00Z — hand-only运行时组合必须单独读回

- 分类：`EVIDENCE_IDENTITY_GUARD + MILESTONE_PROGRESSION`；不改变手指几何、对象位姿、驱动参数或成功标准。
- 完整手臂run05不自动为hand-only组合关闭导入门；因为局部手是一个新的USD组合，必须重新读回它自己的关节、耦合、碰撞和质量属性。
- hand-only读回通过：8个DOF、4组mimic、三末节`64/64/64`个凸块、全手198个碰撞shape；GPU峰值`120/0`，警告/错误为0。
- 这仍只是无对象的导入门；对象B的初始穿透、对向60°轨迹和含对象PhysX健康必须在后续原子实验中单独关闭。

## 2026-08-26T04:32:54Z — 对象初始门采用复合凸运行与原始网格事后双证据

- 分类：`MILESTONE_PROGRESSION + EVIDENCE_SCOPE_GUARD`；不改变候选、12 N、驱动增益、3 rad/s、50 mm、2 s或在线真值隔离。
- Isaac 6 GPU pipeline必须从`World`创建时统一使用Torch/CUDA；run01～run04的数组/API失败均在物理步前失败关闭并原样保留，不属于碰撞或抓取失败。
- USD对象矩阵读回存在`1.91e-8`量级浮点差；采用仓库资产读回既有`1e-7`数值容差，不修改桌面/碰撞穿透容差。对象0.5 s刚体表面运动仍受已登记`1 mm`动态位姿容差约束。
- PhysX复合凸零手部接触不足以单独关闭初始门；必须把全部实际关节和对象姿态用无指甲原始三角网格事后重放。实际mimic残差保留进正运动学并单独报告，不用理想1:1角覆盖实测值。
- run06的60个状态双重通过后只关闭`INITIAL_PENETRATION`；有效接触为0，故不能宣称接触、离桌、抬升或研究动态成功。下一门仍是有界60°与逐指闭合重放。

## 2026-08-26T05:12:22Z — 预构型门不复用接触代理阈值

- 分类：`LOCAL_CORRECTION + EVIDENCE_SEMANTICS_GUARD`；不改变0.0015 rad/周期、0.18 rad/s目标速度、3 rad/s急停、0.9 N·m受力门或接触判据。
- run01错误地要求无对象预构型末态位置误差小于`contact_position_error_rad=0.004`；该值服务“受阻后是否可能接触”的关节侧代理，不是无接触位置控制的到位精度。
- canonical run03改为同时检查每周期目标连续、整个0.5 s保持段8关节速度不超过0.18 rad/s，以及四个主动关节的位置误差在保持段均减小；run01/run02原样保留。
- run03只证明手能有界到达第一指诊断的起点；没有对象、桌面或接触，不能宣称抓取。

## 2026-08-26T05:24:00Z — 局部原子入口484行规模例外

- `run_opposition60_local_contact.py`为单一hand-only原子证据入口，484行低于550硬限；超出400行目标的内容主要是显式SHA绑定、逐周期安全trace和失败关闭字段。
- 预构型与第一指共用约200行资产身份、drive和PhysX接线；拆成两个脚本会制造重复，抽runtime层则会形成当前没有第二个调用者的新框架。
- 场景、控制器与PhysX健康均复用公开实现；第一指门结束后冻结该入口，不向其中增加第二/三指或抬升功能。

## 2026-08-26T05:33:00Z — run04绑定发送前门与完整任务面穿透检查

- 目标增量与有限性改为在`apply_action`前检查；违规目标不会先发送一个物理周期。3 rad/s和0.9 N·m仍按本周期实测值事后急停。
- 快速原始网格重放新增三块完整TASK_GRIP_SURFACE—对象相交门，避免把“允许接触面”误解为“允许深穿面”；20项定向检查通过。
- 因源码哈希变化，run03保留但不再授权第一指；同物理参数run04重放结果一致，并新增hand资产、runtime资源和源码完整哈希绑定。

## 2026-08-26T06:18:57Z — anchor2按允许面优先语义淘汰并回退候选阶段

- 分类：`ROOT_CAUSE_REVIEW + ROLLBACK_TO_PREVIOUS_VALID_STAGE`；不是北极星变化，不修改12 N、3 rad/s、0.18 rad/s、0.0015 rad/周期、50 mm、2 s或真值隔离。
- run01的正向自由运动drive负担被错误取绝对值；改为沿闭合方向的有符号受阻分量后，同阈值下run02不再假确认，历史受阻负力矩仍能触发。
- 冻结候选从预测邻近点按0.0015 rad离散推进时，0.696241 rad是最后允许对象面明确优先的未相交状态；0.697741 rad起允许/禁抓面并列，0.700741 rad出现原始TASK面相交。运行目标只允许前者。
- run03实际最大f1j2为0.696636 rad，未越过预登记语义边界；397步内无PhysX手—物接触，末周期事后精确查询已出现禁抓面并列优先，故anchor2永久淘汰，不启动第二指、预紧或抬升。
- `run_opposition60_local_contact.py`现523行，仍低于550硬限；新增内容仅为哈希绑定端点与运行前/运行中失败关闭。该入口冻结，不在其中接入第二/三指或抬升。
- 下一步继续剩余60°预登记锚点/预构型，优先要求允许面相对禁抓面有可解释间隔；不得用当前候选的运行真值反推新端点。

## 2026-08-26T07:38:00Z — anchor7边界接触不升级，继续完整预构型搜索

- 分类：`ROOT_CAUSE_REVIEW + ROLLBACK_TO_PREVIOUS_VALID_STAGE`；不改变12 N、3 rad/s、0.18 rad/s、0.0015 rad/周期、50 mm、2 s、对象物性或真值隔离。
- anchor5全部27种预构型都在桌面安全投影后丢失三指接触；随后anchor7的对称预构型形成3指路径安全接触，并通过12 N任务与bounded IK。
- anchor7候选专属初始门、精确重放和预构型回放通过；第一指397步运行无超速、超力或未授权碰撞，但PhysX手—物接触为0。
- 事后原始网格末8周期有运动相容TASK近接，不过允许面与禁抓对象面从0.697741 rad起距离并列；不通过扩大凸体或继续硬压制造接触。
- 下一步按固定顺序检查anchor7其余26种预构型，寻找接触点远离语义边界的候选；单个预构型失败不否定anchor7或60°对向族。

## 2026-08-26T08:28:00Z — anchor7完整预构型族关闭并回退预登记方位

- 分类：`ROLLBACK_TO_PREVIOUS_VALID_STAGE`；不是抓取算法总失败，不改变对象、手型、12 N、桌面、接触语义或控制安全门。
- anchor7完整27种预构型中6种通过高度投影、三指接触和逐指闭合路径；其余21种被几何硬门淘汰。
- 6种幸存者的第一指允许面领先禁抓面的最小距离只分为0.00556 µm或1.90214 µm，和已无PhysX接触的动态候选属于同一对象面边界族。
- 因此不把其余5种重复边界候选送入Isaac；下一原子实验选择同轴向层但不同预登记方位的anchor10对称预构型，先区分问题是否来自anchor7接触扇区。

## 2026-08-26T09:13:32Z — 旧二分类路线检查点与Surface V2切换

- 分类：`SURFACE_SEMANTICS_METHOD_CORRECTION + FEATURE_AWARE_FAST_SEARCH + REGION_CONTACT + SIX_HOUR_ISAAC_CLOSURE`；这是方法修正，不是硬件授权或安全标准降低。
- 旧路线最后原子任务为59°/anchor2/p222：离线三指几何、12 N名义任务和bounded IK通过，第一指Isaac运行396步但PhysX接触为0；第二/三指和抬升未启动。
- 用户提供的表面审计指出外包络切向侧壁不应继续被旧“非PRIMARY即禁抓”二分类视为硬禁抓；旧结果保留为`LEGACY_RADIAL_ONLY_BASELINE`。
- 本分支在该原子任务后停止继续anchor扫描；提交后新建`carts-grasp-surface-v2-fast6h-20260826`，新窗口从新分支第一条实际命令计时。
