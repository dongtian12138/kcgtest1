# CONTACTOPT-1488 当前决策摘要

完整历史决策保存在 Git 标签 `pre-clean-project-20260826`。本文件只保留当前方法仍然
有效的决定，不作为动态授权。

## 方法身份

- 候选由真实手侧三点/法向与对象登记表面经 Kabsch/SVD 和小型法向精修得到；不使用随机
  姿态、Sobol 或手填对象坐标。
- 1488 个规格固定为全局 1040 和加密对向组 448；每项必须留下生成或唯一拒绝原因。
- 前级是代理证据，Top12 才绑定原始无指甲网格与 Isaac 复合凸双几何。

## B0 语义

- 对象 PRIMARY/SECONDARY 只作诊断排序；B0 使用登记外部实体承载面，明确功能保护面仍
  失败关闭。
- 桌面操作余量、逐指闭合、自碰、非任务手面和 12 N 名义任务门保持独立，不能用综合分数
  互相抵消。

## 当前候选事实

- `contactopt_g_q08_a03_z1_p0` 的采样闭合最小桌面间隙为 0.991278 mm，满足已登记的
  1 mm 减 10 µm 数值容差。
- 名义重力/抬升平衡通过，但额外六方向与误差场景未通过鲁棒门；bounded IK 通过不等于
  整臂路径安全。
- 初始 Isaac 只推进 0.5 s 且未发送闭合/抬升指令；没有真实手—物接触，不能称为抓取成功。

## 2026-08-26 漏斗回退与生成修正

- 用户最新纠正归类为 `LOCAL_METHOD_CORRECTION + ROLLBACK_TO_GENERATOR_FUNNEL`，不改变
  北极星、无指甲手型、1488 规格、12 N、3 rad/s、50 mm、2 s 或真值隔离。
- 停止旧候选的局部位姿、终点、接触阈值和控制增益补救；将其记录为“任务指腹未形成
  PhysX 接触前，`f1Link3` 非任务面先发生原始网格干涉”。
- 72×24 外轮廓代表面只用于旧基线/初始化诊断，不再作为 B0 连续外表面三点目标搜索域。
- 三指相对轴向错位保持不变，允许共同中心在既有对象抓持带内做有界投影；真实手形跨度
  超出抓持带仍失败关闭。
- 对三边相容候选先按边长残差选固定 Top-64，再最小化最大局部法向残差，最后最小化最大
  点位残差；没有改变任何碰撞、接触、受力或运行安全阈值。
- 新 Top-3 已按固定规则尝试：两个在端点规划失败，一个第一指动态无 PhysX 接触；不得
  称为三指抓取、离桌或研究动态通过。

## 2026-08-26 近接带语义纠正

- 用户纠正归类为 `LOCAL_CORRECTION`：0.75 mm 是几何近接/接触搜索带，不是本轮开始前
  冻结的手侧净空硬标准。
- 非任务手面进入该邻域只记录风险；B0 手侧硬失败仍以真实网格相交、PhysX 实际接触、
  穿透或承载为准。不得事后把近接距离升级成硬门。
