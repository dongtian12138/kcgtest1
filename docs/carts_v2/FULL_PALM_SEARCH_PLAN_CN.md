# 完整掌面构型与六维手掌联合搜索 12 小时计划

## 任务边界

- 执行窗口：`FULL_PALM_6D_SEARCH_12H`
- 开始：`2026-08-25T09:11:20Z`
- 硬截止：`2026-08-25T21:11:20Z`
- 分支：`carts-grasp-full-palm-search-20260825`
- 结论范围：仅仿真研究；`hardware_authorized=false`、正式动态门关闭。

## 双监督采纳的三项简化

1. 全部预算按级联上限执行：91 角各生成 128、保留 64、最多 8 个进入精确闭合；每对象只汇总最多 64 个精修种子，300 次是每种子两阶段合计上限。
2. 掌座与桌面碰撞可立即拒绝；张开手指连杆碰桌必须让 27 种预构型都失败后才能拒绝。指腹用真实最近三角面法向，非指腹/自碰继续复用 FCL。
3. 局部精修仅作救援：直接候选一旦通过几何、12 N、IK 和抬升路径就立即进入 Isaac，不等待全体精修。

## 计划与实际

| 阶段 | 计划时间 | 实际开始 | 物理输出 | 状态 |
|---|---:|---:|---|---|
| 冻结分支、状态与监督审查 | 0:00–0:30 | 09:11Z | 新路线身份、预算与停止规则 | 09:24Z 完成 |
| 旧候选视觉真实性与掌面角审计 | 0:30–1:30 | 09:24Z | 可见/碰撞/桌面一致性、0–上界角动画 | 09:58Z 完成，提前约43分钟 |
| 91 描述器与精确接触几何 | 1:30–3:00 | 09:32Z | q_p 全链、材料外法向、相交/空集合失败关闭 | 10:29Z 完成，提前约1小时42分 |
| A/B 同参数生成与级联筛选 | 3:00–5:30 | 10:29Z | 每角计数、27 预构型、路径安全候选 | 公共门与0°小样本完成，A全池待运行 |
| 12 N、IK、路径与 Top-3 | 5:30–7:00 | 待记录 | research executable 候选 | 未开始 |
| 对象 A Isaac Top-3 | 7:00–9:30 | 待记录 | 三指接触、离桌、50 mm、2 s 或明确物理失败 | 未开始 |
| 对象 B 同参数与收口 | 9:30–12:00 | 待记录 | 跨对象结果、监督、提交和事实报告 | 未开始 |

落后超过 30 分钟时依次删除美化、可选消融与非必要精修；不降低碰撞、力量、速度、50 mm、2 s 或真值防火墙。

## 已完成任务卡：27 预构型真实路径门与级联入口

- 要解决的物理问题：每个六维位姿的 27 种三指预闭合组合，能否在远离连接器时平滑完成，再沿接近路径到达而不碰桌、不自碰、不让非指腹先碰，也不让 PAD 提前接触。
- 服务目标：直接改变路径安全和真实三指接触候选数，服务第一次新路线 Isaac 与“预构型避免扫桌”的方法假设。
- 最简单方案：在现有 `fast_filter.py` 暴露一个薄的批处理入口，FCL 场景只建一次，内部复用已存在的 FK、有限桌面和全手碰撞内核；runner 只消费结果。
- 唯一主要改变量：补齐“无接触预测之前”的预构型与接近路径检查；不改候选、网格、容差、速度、力量或排序。
- 预期数据：每角原始数、明显初始错误数、27 组合各拒绝原因、每角 Top-8、精确三指接触与完整闭合安全数。
- 失败判据：接口跳过远端设置/接近任一阶段、跨模块调用私有内核、复制碰撞实现，或把 descriptor AABB 当完整可达体硬证据。
- 实际结果：公共门已接线；0°的 `8×27=216` 条路径在 63.8 s 内检查 30645 个控制步，84 条快筛存活、132 条真实扫桌。
- 失败后：保留 AABB 分离为诊断，缩小到成熟 FCL 的最小公开状态/路径检查；不调容差、不进入 H102。

## 当前 120 分钟任务卡：对象 A 全 91 角固定级联

- 要解决的物理问题：固定预算选出的六维位姿中，哪些能先安全预闭合、通过采样接近快筛，再由三块真实允许指腹逐指接触且整手不扫桌。
- 服务目标：直接产生任务力学入口，服务三指接触与第一次新路线 Isaac；不做报告美化。
- 最简单方案：使用当前 hash 绑定 runner，一次构建 FCL 场景；每角 64 条先用 phase 0 真实网格状态作预算诊断，最多 8 条检查 27 预构型和一次精确闭合。
- 唯一主要改变量：从0°小样本扩大到对象 A 的91角固定池；候选、阈值、对象、手模型和控制步均不变。
- 预期数据：每角预算保留数、路径存活数、三指接触数、完整闭合安全数、失败原因和总时间。
- 失败判据：哈希/身份不符、运行超过120分钟、或0条精确闭合幸存；失败时进入已预登记的有界几何精修/真因分析，不放宽标准。
- 时间上限：120分钟；每60秒检查存活，完成后立即汇报并决定是否进入12 N。
- 更简单方法：若计算超时，只优化重复查询或缩小昂贵验证的预算，不能减少91角生成证据或降低碰撞标准。
- 等价性能修正：同一候选完全相同的远端掌面角控制状态只作一次 FCL/FK 查询，27 个预构型复用该事实；每个预构型自己的手指设置和 5 点接近仍独立检查。

## 固定搜索预算

- `q_p_grid_count=91`，闭区间 `[0,1.57] rad`，种子 `20260825`。
- 每角 `raw=128`、`score_keep=64`、`precise_closure<=8`。
- 每指预构型 phase 为 `{0.0,0.1,0.2}`，共 27 组合。
- 直接级联每种子最多 `27+1=28` 次候选级调用，内部控制状态不计为精修评价。
- 救援精修每对象最多64个种子、每种子最多300次几何评价；只在直接候选不足时启用。
- 两对象完全同序同参；禁止根据 B 回调参数。

## 停止条件

只在用户明确停止、必须真实硬件、必须降低冻结标准、必须使用在线真值/磁吸/物体位姿写入、外部 GPU/Isaac 不可恢复、数据有不可逆风险，或硬截止到点时停止。单候选、单命令、单测试或单阶段失败均回退到最近有效阶段继续。
