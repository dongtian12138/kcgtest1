# 完整掌面构型与六维手掌联合搜索 12 小时计划

## 任务边界

- 执行窗口：`FULL_PALM_6D_SEARCH_12H`
- 开始：`2026-08-25T09:11:20Z`
- 硬截止：`2026-08-25T21:11:20Z`
- 分支：`carts-grasp-full-palm-search-20260825`
- 结论范围：仅仿真研究；`hardware_authorized=false`、正式动态门关闭。

## 双监督采纳的三项简化

1. 全部预算按级联上限执行：91 角各生成 128、保留 64、最多 8 个进入精确闭合；每对象只汇总最多 64 个精修种子，300 次是每种子两阶段合计上限。
2. 掌座与桌面碰撞可立即拒绝；张开手指连杆碰桌必须让 27 种预构型都失败后才能拒绝。指腹用真实最近三角面法向，非指腹/自碰继续复用 FCL。
3. 局部精修仅作救援：直接候选一旦通过几何、12 N、IK 和抬升路径就立即进入 Isaac，不等待全体精修。

## 计划与实际

| 阶段 | 计划时间 | 实际开始 | 物理输出 | 状态 |
|---|---:|---:|---|---|
| 冻结分支、状态与监督审查 | 0:00–0:30 | 09:11Z | 新路线身份、预算与停止规则 | 09:24Z 完成 |
| 旧候选视觉真实性与掌面角审计 | 0:30–1:30 | 09:24Z | 可见/碰撞/桌面一致性、0–上界角动画 | 09:58Z 完成，提前约43分钟 |
| 91 描述器与精确接触几何 | 1:30–3:00 | 09:32Z | q_p 全链、材料外法向、相交/空集合失败关闭 | 10:29Z 完成，提前约1小时42分 |
| A/B 同参数生成与级联筛选 | 3:00–5:30 | 10:29Z | 每角计数、27 预构型、路径安全候选 | 进行中，提前开始 |
| 12 N、IK、路径与 Top-3 | 5:30–7:00 | 待记录 | research executable 候选 | 未开始 |
| 对象 A Isaac Top-3 | 7:00–9:30 | 待记录 | 三指接触、离桌、50 mm、2 s 或明确物理失败 | 未开始 |
| 对象 B 同参数与收口 | 9:30–12:00 | 待记录 | 跨对象结果、监督、提交和事实报告 | 未开始 |

落后超过 30 分钟时依次删除美化、可选消融与非必要精修；不降低碰撞、力量、速度、50 mm、2 s 或真值防火墙。

## 当前 90 分钟任务卡：A/B 六维生成与级联入口

- 要解决的物理问题：91 个掌面角能否让官方生成器提出旧五构型没有覆盖到的斜向、侧向六维抓法，并完整保留到物理筛选。
- 服务目标：直接改变候选覆盖和路径安全候选数量，属于论文方法和第一次新路线 Isaac 的必要输入。
- 最简单方案：在官方隔离环境中只加载模型一次，按固定 91 角顺序处理两个完整对象网格；输出 v2 proposal 后复用现有 V2 物理门。
- 唯一主要改变量：掌面构型覆盖从旧 5 个增加到预登记 91 个；A/B 的模型、预算、种子和物理门相同。
- 预期数据：每角有限提案数、六维覆盖、`q_p` 完整性，以及 27 预构型级联各拒绝阶段计数。
- 失败判据：官方推理不能产生有限位姿、候选丢失/篡改 `q_p`、或真实级联未按预算接入。
- 时间上限：90 分钟完成推理与产物审计；级联物理运行继续占用本阶段剩余预算。
- 失败后：区分模型环境、描述器 schema、坐标链或级联接线中的单一原因；不改物理标准、不做参数扫描。

## 固定搜索预算

- `q_p_grid_count=91`，闭区间 `[0,1.57] rad`，种子 `20260825`。
- 每角 `raw=128`、`score_keep=64`、`precise_closure<=8`。
- 每指预构型 phase 为 `{0.0,0.1,0.2}`，共 27 组合。
- 每对象跨角度 `refinement_seed<=64`；每种子 Stage A+B 总评价 `<=300`。
- 两对象完全同序同参；禁止根据 B 回调参数。

## 停止条件

只在用户明确停止、必须真实硬件、必须降低冻结标准、必须使用在线真值/磁吸/物体位姿写入、外部 GPU/Isaac 不可恢复、数据有不可逆风险，或硬截止到点时停止。单候选、单命令、单测试或单阶段失败均回退到最近有效阶段继续。
