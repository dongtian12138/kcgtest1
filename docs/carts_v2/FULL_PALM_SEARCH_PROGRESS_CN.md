# 完整掌面构型搜索进度

> 这是简短进度记录；原始 JSON、CSV、图像、视频和运行日志优先。

## 2026-08-25T09:11:20Z — 开窗

- 最初目标：两个连接器同方法同参数，在 Isaac 中由三块真实指腹抓住、离桌、抬升 50 mm 并保持 2 s。
- 已完成：从旧五构型路线的已推送提交创建新分支；无残留 Isaac 或 GraspGenX 运行进程。
- 当前真实结果：只有旧离线证据；对象 A 无三指闭合幸存者，对象 B 两个闭合幸存者均扫桌；新路线尚未运行。
- 当前阻塞：需要先核对可见/碰撞模型真实性，再完成 91 掌面角和 27 预构型的级联搜索。
- 指令分类：`METHOD_CORRECTION + FULL_PALM_CONFIGURATION_SEARCH + VISUAL_TRUTH_DIAGNOSIS + AUTONOMOUS_ISAAC_VALIDATION`。
- 下一步：复现旧 B 候选 131/133 的可见/碰撞叠加，并生成掌面构型锚点与连续动画。

## 证据边界

- 当前等级：代码与旧离线算法证据。
- 研究型动态：false。
- 正式动态：false。
- 硬件：未授权。

## 2026-08-25T09:47:00Z — 旧候选视觉真因已闭环

- 对象 B 旧候选 131/133 的掌面角在全部 1216/1212 个重放状态中保持 `f1j1=f3j1=0.157 rad`，候选转换和 FK 未丢失该变量。
- 两候选都在第一指阶段由 `f1Link3` 首次越过桌面约 `0.03245 mm`，第三指阶段由 `f3Link3` 最深穿入约 `4.45995 mm`。
- 原始可见网格与注册碰撞网格在两类关键状态的连杆、最低见证点和间隙一致，差值为 0；因此不是单凸包假阳性。
- 可见、离线和 Isaac 配置中的有限桌面顶面均为 `z=0.200 m`，x/y 范围一致；这里只审计了配置与 authoring 代码，没有实时启动 Isaac。
- 旧 96 点接触近似确有毫米以下偏差，旧径向方向与真实面法向最大相差 `8.682°`；它们不是 131/133 扫桌主因，但对未来斜向候选存在误判风险，必须用真实面法向复核。
- 当前证据等级：离线几何重放与媒体审计；两候选永久保留为真实中途扫桌反例，不是动态失败。

## 2026-08-25T09:58:00Z — 对象 A 旧描述器近接触对照补齐

- 对旧五个掌面描述器各保留 3 个最接近允许指腹接触的诊断候选，共 15 个；排序只用于解释旧候选族，不进入执行名单。
- `kcg_3f_preshape_02` 的 `graspgenx_100` 和 `graspgenx_098` 各形成 1 个允许指腹接触，其余 13 个为 0；没有任何候选形成三指允许接触。
- 多数候选没有“下一运动兼容允许接触距离”，含义是旧运动方向下 eligible 面集合为空，不是距离为零。
- 桌面间隙只比较预抓和失败端点，不声明完整闭合路径安全；这组结果只能证明旧五描述器候选族接触不足。
- 视觉诊断门至此完成；下一步是使用 91 个完整掌面角、真实三角面法向和显式空集合拒绝重新生成候选。
