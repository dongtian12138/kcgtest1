# 完整掌面构型搜索进度

> 这是简短进度记录；原始 JSON、CSV、图像、视频和运行日志优先。

## 2026-08-25T09:11:20Z — 开窗

- 最初目标：两个连接器同方法同参数，在 Isaac 中由三块真实指腹抓住、离桌、抬升 50 mm 并保持 2 s。
- 已完成：从旧五构型路线的已推送提交创建新分支；无残留 Isaac 或 GraspGenX 运行进程。
- 当前真实结果：只有旧离线证据；对象 A 无三指闭合幸存者，对象 B 两个闭合幸存者均扫桌；新路线尚未运行。
- 当前阻塞：需要先核对可见/碰撞模型真实性，再完成 91 掌面角和 27 预构型的级联搜索。
- 指令分类：`METHOD_CORRECTION + FULL_PALM_CONFIGURATION_SEARCH + VISUAL_TRUTH_DIAGNOSIS + AUTONOMOUS_ISAAC_VALIDATION`。
- 下一步：复现旧 B 候选 131/133 的可见/碰撞叠加，并生成掌面构型锚点与连续动画。

## 证据边界

- 当前等级：代码与旧离线算法证据。
- 研究型动态：false。
- 正式动态：false。
- 硬件：未授权。

## 2026-08-25T09:47:00Z — 旧候选视觉真因已闭环

- 对象 B 旧候选 131/133 的掌面角在全部 1216/1212 个重放状态中保持 `f1j1=f3j1=0.157 rad`，候选转换和 FK 未丢失该变量。
- 两候选都在第一指阶段由 `f1Link3` 首次越过桌面约 `0.03245 mm`，第三指阶段由 `f3Link3` 最深穿入约 `4.45995 mm`。
- 原始可见网格与注册碰撞网格在两类关键状态的连杆、最低见证点和间隙一致，差值为 0；因此不是单凸包假阳性。
- 可见、离线和 Isaac 配置中的有限桌面顶面均为 `z=0.200 m`，x/y 范围一致；这里只审计了配置与 authoring 代码，没有实时启动 Isaac。
- 旧 96 点接触近似确有毫米以下偏差，旧径向方向与真实面法向最大相差 `8.682°`；它们不是 131/133 扫桌主因，但对未来斜向候选存在误判风险，必须用真实面法向复核。
- 当前证据等级：离线几何重放与媒体审计；两候选永久保留为真实中途扫桌反例，不是动态失败。

## 2026-08-25T09:58:00Z — 对象 A 旧描述器近接触对照补齐

- 对旧五个掌面描述器各保留 3 个最接近允许指腹接触的诊断候选，共 15 个；排序只用于解释旧候选族，不进入执行名单。
- `kcg_3f_preshape_02` 的 `graspgenx_100` 和 `graspgenx_098` 各形成 1 个允许指腹接触，其余 13 个为 0；没有任何候选形成三指允许接触。
- 多数候选没有“下一运动兼容允许接触距离”，含义是旧运动方向下 eligible 面集合为空，不是距离为零。
- 桌面间隙只比较预抓和失败端点，不声明完整闭合路径安全；这组结果只能证明旧五描述器候选族接触不足。
- 视觉诊断门至此完成；下一步是使用 91 个完整掌面角、真实三角面法向和显式空集合拒绝重新生成候选。

## 2026-08-25T10:29:22Z — 91 掌面构型与精确指腹接触接口闭环

- 从 `f1j1=0` 到 `1.57 rad` 生成 91 个固定等距描述器，`f3j1` 按真实 mimic 同步；每个描述器通过 open/half/close、网格、自碰撞和有限数值检查。
- v2 候选必须同时携带 descriptor identity 和有限 `q_p`；adapter、候选数据、闭合预测、CSV/JSON 和控制计划之间缺失或篡改都会失败关闭。
- 指腹接触先使用完整 PAD 与完整对象的 FCL 最近见证；材料外法向由已绑定的逐面方向证书修正。全局最近点运动不兼容且已进入接触距离时，才在 1024 个确定性 PAD 表面点上找兼容见证。
- FCL 报告相交时不会被当成零距离有效接触；允许集合为空显式拒绝。A/B 实际登记网格的材料方向证书、哈希和查询构造均已在项目 FCL 环境核对。
- 监督结论为 APPROVE，但证据只覆盖描述器、精确接触查询和 `q_p` 接口；真实 v2 提案、完整逐指闭合、12 N、IK、路径和 Isaac 均尚未完成。

## 2026-08-25T10:34:27Z — A/B 同参数 91 角官方提案生成完成

- 官方模型在同一进程中加载 1 次，顺序处理两个真实登记对象；A/B 各运行 91 个掌面角，每角固定保留 64 条，共各 5824 条。
- A/B 各自的 109928 条官方原始输出均为有限刚体位姿；逐条 `q_p` 与描述器一致，位置覆盖四个方位，roll/pitch/yaw 均非固定。
- A 提案文件 SHA-256 为 `aed02e49…`，B 为 `0086d1be…`；完整文件保存在忽略目录，不把约 8.4 MB 原始 JSON 提交进 Git。
- 这证明完整掌面角和六维提案真实运行，不证明张开手、闭合路径、三指接触、12 N、IK 或 Isaac 可行；下一步只做真实物理级联。

## 2026-08-25T10:59:26Z — 否决 table-only 预构型接线

- 只读重放发现：对象 A 一个真实 OBB 提案有 12/27 个桌面安全预构型，但仅按桌间隙挑出的组合在精确闭合时第一指没有有效接触；因此“桌面安全”不足以选择预构型。
- 当前唯一公开 `fast_filter_predictions()` 要求闭合预测先成功，无法检查尚未接触的远端预闭合和接近路径；若 runner 复制或导入其私有 FCL 内核，会形成第二套碰撞逻辑。
- 决定在现有快筛模块增加一个最小公开批处理入口，补齐远端预闭合、接近、桌面、自碰、非指腹和 PAD 提前接触；之后再由精确闭合和原快筛复核。
- A/B 有 79.22%/80.94% 提案的 descriptor open/half AABB 与对象包围球分离，但该 AABB 明确不是完整物理闭合可达体；这项数据只作生成器输出诊断，不作硬拒绝。

## 2026-08-25T11:38:50Z — 预构型路径门接线并通过0°真实小样本

- 每个候选先在远端从仿真初始 `q_p=0` 以不超过 `0.0015 rad/周期` 到候选掌面角，再按同一步长完成三指预构型，随后保持该构型走 5 点采样接近；逐状态检查有限桌面、非相邻自碰、非指腹—对象和指腹提前相交，最终预抓还要求 1 mm 指腹净空。
- 0°掌面角取8个六维候选，共运行216种预构型、检查30645个控制步状态；84条快筛存活，132条因真实手部网格穿桌拒绝，耗时63.8 s。
- 64→8只分配昂贵闭合预算；未运行的56条写 `DEFERRED_BY_PER_ANGLE_PRECISE_BUDGET`，不能称物理失败。官方scene-PC和手可达球仅作排序诊断，不作硬拒绝。
- 直接级联的上限是每种子27次预构型加1次精确闭合；300次属于以后救援精修，闭合控制步不计入该预算。
- 证据仍是离线有限采样快筛；84条没有经过精确三指接触、任务受力、IK或Isaac。下一步运行对象A全91角。

## 2026-08-25T11:57:12Z — 全池运行前关闭预算排序歧义

- 每个生成分支只预留一个角内精查席位，其余席位按 phase 0 真实预抓快照诊断全局排序；scene-PC 只写“未优先”，不再写“拒绝”。
- 27 种路径安全预构型先最小化三块指腹中距离最远的一块，再比较有限桌面间隙；一块指腹很近不能掩盖另两块离对象很远。
- 每角最多 8 条只是精确闭合预算；所有精确闭合幸存者都进入 12 N 任务评价，不在任务力学之前只保留一个。
- 已选预构型精确失败时，其他路径安全预构型明确写预算未评估，不把整个六维手掌位姿宣判为物理失败。
- 这些修正通过 34 项直接相关测试；仍未运行对象 A 全池、任务受力、IK 或 Isaac。
- 全池前性能复核发现同一候选的 `q_p` 远端段会重复 27 次；现只计算一次公共段，phase-0 全池排序只看一个真实预抓快照。后者明确不证明掌面角或接近路径安全。
- 离线轨迹把 handbase 固定在远端抓取前位置，并从 Isaac 零关节向量先调掌面角再调三指；当前 Isaac 控制器的 home→far 同步轨迹必须在动态入口前另行对齐或验证。

## 2026-08-25T12:40:26Z — 对象 A 首次全池命令有界中断

- 5824 条 phase-0 真实预抓快照全部完成：1709 条快照未发现冲突，4115 条快照穿桌；其中掌座 2147、f2Link2 859、f3Link3 609、f1Link3 463、其他连杆 37。
- 官方 scene-PC 仅给 2695 条“优先”和 3129 条“未优先”，没有据此硬拒绝。
- 后续 91 角级联在 1490.24 s 达到单命令上限而中断，完整 cascade audit 未落盘；这只证明执行方式不可恢复，不证明无三指接触候选。
- 最小修正为逐角原子检查点；恢复必须绑定全部候选 ID、预构型关节、配置和相关源码哈希，未完成角不得跳过。
