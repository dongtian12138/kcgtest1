# GraspGenX-CARTS 无人值守进展

## 2026-08-24T15:18:00Z — 上下文恢复六句话

- 最初目标：两个 D38999/J599 对象使用同一生成器、描述器、物理筛选和控制参数，由三根真实指腹抓住允许表面并在 Isaac 中离桌、抬升至少 50 mm、保持至少 2 s。
- 当前已完成：路线分支已建立并推送，官方源码固定到 `b942909`，5 个跨对象一致的 KCG 三指手预构型描述器已经生成。
- 当前真实物理结果：9 个预构型在描述器闭合相位 `0.50` 下共检查 4203 个空手开合状态且没有自碰；新路线尚未在 Isaac 接触或抬起连接器。
- 当前唯一主要阻塞：官方 `uv sync` 正在构建大体积 CUDA/TensorRT 依赖，官方三指手正对照尚未产出六维抓取位姿。
- 最近用户指令属于：`METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION`，不是硬件授权或降低物理标准。
- 下一步：完成官方正对照，同时把两个真实登记网格转换为隔离推理输入，随后检查新候选是否真正覆盖斜向和侧向姿态。

## 当前证据边界

- 已验证：描述器基于登记手部网格、真实 mimic 关系与合法关节范围；固定相邻关节检查步长不超过 `0.0015 rad`。
- 尚未验证：GraspGenX 对 KCG 手的推理输出、两对象路径安全候选、任务承载、IK、Isaac 接触、离桌、抬升和保持。
- 结论上限：离线手部几何验证；`hardware_authorized=false`、研究动态和正式动态通过均保持 false。

## 2026-08-24T16:21:43Z — RTX 5070 Ti 隔离运行时联通

- 官方 `uv sync` 的 PyTorch 2.6+CUDA 12.4 轮子不包含 `sm_120`，实际 GPU 张量在模型加载前失败；未修改 GraspGenX 源码或模型。
- 按官方源码已验证的 PyTorch 2.7 兼容范围，在同一隔离 `.venv` 内改用 PyTorch `2.7.0+cu128`、torchvision `0.22.0+cu128`；`trimesh` 保持官方锁定的 `4.5.3`。
- 模块均来自 GraspGenX `.venv`；架构列表包含 `sm_120/compute_120`，RTX 5070 Ti 实际矩阵和张量计算产生有限结果。
- 这只关闭运行时兼容问题，尚不证明 checkpoint 可加载、会输出抓取位姿或 KCG 手能抓起连接器。
- 下一步：固定官方 Hugging Face revision、下载真实 LFS 权重和样例，再运行内置三指手的 headless 正对照。

## 2026-08-24T16:39:00Z — 官方三指手正对照通过

- 官方 `robotiq_3f` 对官方 `banana.obj` 在 RTX 5070 Ti 上一次加载模型，返回 `20/20` 个有限、合法齐次变换的六维候选。
- 模型加载 `1.596 s`，推理 `0.522 s`；置信度范围 `0.186690–0.986697`，峰值分配显存约 `616 MB`。
- checkpoint、官方描述器、源码 commit、隔离 Python/PyTorch/CUDA 来源和小型输出均已哈希绑定；静态总览显示候选从多个方向包围样例物体。
- 这证明官方模型、权重、GPU 和文件输出链可用；不证明 KCG 手、连接器碰撞、任务承载、Isaac 抬升或硬件成功。
- 下一步：模型仍只加载一次，顺序处理 5 个固定 KCG 描述器和两个真实登记网格，再检查候选是否真正覆盖侧向与斜向。

## 2026-08-24T17:19:05Z — KCG 虚拟基座修正并完成 A/B 重生成

- 原描述器把生成基准放在腕部，指尖深度约 `0.39 m`；官方生成器因此出现大量平移裁剪。按注册 URDF/FK 把基准移到三根近端关节平面后，五个描述器指尖深度为 `97.2–101.5 mm`，旧提案没有被事后平移。
- 官方模型仍只加载一次，用同一 checkpoint、种子和预算为 A/B 各输出 640 条；先用官方点云工作区语义筛选，每描述器分别有 A `145–180/472`、B `144–180/472` 条可覆盖张开工作区，再保留最高 128 条。
- 对象 A 的 256 条六维分散候选中，三指闭合存活为 0：119 条先碰禁抓面、108 条第二指够不到、28 条第一指够不到、1 条初始过近；未进入任务承载。
- 对象 B 的 256 条保留项以同一 OBB 面姿态为主，pitch 和径向变化不足，六维覆盖诊断失败关闭；尚未进入闭合或承重。
- 当前证据等级仍为离线算法；没有连接器接触、离桌、抬升或保持结果。下一步只读检查截断前 640 条，区分是生成失败还是 640→256 压缩丢失物理可行候选。

## 2026-08-24T18:21:11Z — 恢复核对与六维合并缺口定位

- 最初目标：两个连接器同一方法和参数，三块真实指腹接触后离桌、抬升至少 50 mm、保持至少 2 s，且无未授权穿透。
- 当前已完成：官方正对照、5 个对象无关描述器、世界坐标推理、dense top/side 提案、每种手型的最后采样无自碰闭合上限，以及 A/B 各 640 条同预算提案。
- 当前真实物理结果：旧全局合并后的 A/B 各 256 条均有 5 个三指预测接触，但全部在第一指闭合时由 `f1Link3` 扫桌；任务评价未启动，Isaac 未启动。
- 当前唯一主要阻塞：每描述器阶段已保留的 d02 安全顶向分数第 3/7 名，在跨描述器 `640→256` 的纯六维最远点中被删；当前快筛还缺手部自碰与非指腹—对象闭合检查。
- 最近用户指令属于：`METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION`，不改变硬件、碰撞、力量、50 mm、2 s 或真值隔离。
- 下一步：按对象无关的描述器×方向固定配额重跑 A/B；若出现路径幸存者，失败关闭补齐自碰/非指腹检查后才进入 12 N 任务评价和 IK。
- 证据边界：16 个定向测试通过只证明接口和固定选择规则；`research_dynamic_pass=false`、`formal_dynamic_pass=false`、`hardware_authorized=false` 均保持。

## 2026-08-24T18:49:36Z — 固定原始点云并补齐离散整手碰撞门

- 对象表面抽样原先未把 `20260824` 传给 Trimesh；同一 raw index 跨重生成出现 `2.95–15.49 mm` 平移和约 `71.7–75.7°` 旋转差，旧未绑定候选只保留为无效诊断。
- 修正后 A/B 点云哈希分别为 `3fe94183…`、`6525139f…`；相同完整命令重跑的 640 条候选 ID、分数和位姿逐项完全一致。
- 固定高分分层池：A `256→2` 个三指接触、B `256→7`；两对象完整顺序扫掠均为 0，任务受力和 Isaac 均未启动。
- 同一固定池的 A 层内六维 FPS 消融产生 7 个三指接触但仍为 0 个路径安全，说明扩大几何覆盖改善接触数量但未改变桌面碰撞结论。
- 快筛已用公开 FCL 薄接法补入 28 组非相邻自碰和精确非 PAD—对象检查；旧 candidate_11 在第一指第 248 步由 `f1Link3` 非 PAD 面命中。
- 当前只检查 A 的全部 13179 条官方工作区可见原始提案是否存在桌面安全三指接触；它决定下一步应修有限预算选择还是回查生成/描述器几何。
- 证据边界：FCL 检查仍按 `≤0.0015 rad` 控制步离散采样，不是状态间连续证明；研究动态、正式动态和硬件均为 false。

## 2026-08-24T18:55:08Z — 上下文恢复六句话

- 最初目标：两个连接器使用同一 GraspGenX-CARTS 方法和主要参数，以三块真实指腹接触后离桌、抬升至少 50 mm、保持至少 2 s，并且无未授权穿透。
- 当前已完成：官方正对照、5 个对象无关描述器、世界坐标推理、固定点云种子与哈希、可复现 A/B 六维提案，以及离散整手自碰和非 PAD—对象 FCL 快筛。
- 当前真实物理结果：固定池 A/B 分别有 2/7 个三指预测接触，但完整闭合桌面安全数均为 0；任务评价和 Isaac 尚未启动。
- 当前唯一主要阻塞：对象 A 的 13179 条固定原始提案中是否存在三指允许接触且完整闭合不扫桌的姿态，当前全池检查只完成闭合预测的一部分。
- 最近用户指令属于：`METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION`，不是硬件授权，也未改变碰撞、力量、50 mm、2 s 或在线真值边界。
- 下一步：完成全池存在性检查；若存在则采用对象无关的有限保留规则，若不存在则直接定位描述器或提案几何根因，再用同一方法重跑 A/B。

## 2026-08-24T20:08:58Z — 允许表面 ROI 双对象离线区分完成

- 生成器只把登记允许面点云用于提案；闭合、桌面和全手碰撞仍使用完整登记对象网格，没有删除禁抓几何。
- 对象 A：256 个六维候选中 3 个形成三指接触预测；2 个在逐指闭合中扫桌、1 个预抓/端点穿桌，任务评价与可执行候选均为 0。
- 对象 B：256 个候选全部在闭合预测拒绝；第一指无允许接触 164、禁抓面先接触 51、第二指无接触 39、第三指无接触 2，桌面/FCL/任务均未到达。
- 尺度审计确认最终 handbase 的约 ±0.45 m 来自登记长指几何而不是单位/变换链错误；KCG 描述器工作区超出本轮已审计官方描述器清单的尺度范围，且每对象仅 26/640 条保留提案来自学习分支。该差异只是受支持的可疑原因，不是已证因果。
- 这证明官方程序和自研描述器接口真实运行、候选覆盖斜向/侧向，但没有产生可安全进入 Isaac 的候选；不证明三指手本身不能抓连接器。
- 生成器、描述器、面标签和候选偏移现已冻结；后续只做失败关闭、研究/正式门语义、证据清单、定向验证和学术局限收口。

## 2026-08-24T20:43:00Z — 官方手同对象对照与旧 B 碰撞口径复核

- 官方 `robotiq_3f` 在同一 A/B 完整对象点云上各输出 `256/256` 个有限 diffusion 位姿；手基座到点云中心距离中位数分别为 `174.83 mm` 和 `173.75 mm`，与该官方手约 `190 mm` 指尖深度量级一致。
- 这排除了“连接器完整点云让官方学习分支整体失效”，并提高了“KCG 长指描述器/手几何适配需要进一步验证”的可信度；它不能证明训练分布、域外推或唯一根因，更不是碰撞、承重或抓取成功。
- 旧对象 B 基线按当前完整门重放为 `384→110` 个三指闭合→`6` 个桌面安全→`0` 个整手 FCL 安全；六项均在第一指闭合 step `269–272` 由 `f1Link3` 非指腹表面碰到对象。
- 已把“完整顺序闭合不碰桌”和“后续非指腹/自碰 FCL”拆成独立统计；旧报告把后者混入桌面布尔值的语义错误已修正，但总体 `FAST_REJECT` 物理结论不变。
- 当前仍没有进入任务力学、机械臂 IK 或 Isaac 的候选；37 项接口/碰撞定向测试和 4 项旧双对象管线回归通过，研究动态、正式动态和硬件均为 false。
- 下一步从冻结提交对 A/B 做同源绑定重放，保存最终覆盖、阶段计数和哈希；不会再改生成器、描述器或物理门。

## 2026-08-24T21:06:00Z — `db540b3` 双对象同源重放完成

- 对象 A：`256→3` 个离线三指闭合预测→`0` 个桌面安全；2 个预测路径中途扫桌，1 个预抓/端点穿桌。墙时 `41.10 s`，result SHA `3c94f650…`。
- 对象 B：`256→0` 个离线三指闭合预测；墙时 `141.96 s`，result SHA `844ab07b…`。
- 两对象各有 26 个 diffusion 与 230 个 OBB 候选；A 的 3 个闭合预测幸存者都来自 OBB，26 个学习分支候选在两对象上均没有形成三指闭合预测。
- 两对象六维覆盖诊断均通过，但任务评价、机械臂 IK 和 Isaac 均未到达；不能把 task-survive=0 写成 12 N 评价失败。
- 当前源码、配置、描述器、提案和输出哈希已绑定；动态目录明确记录“门关闭、未运行”，不是动态失败轨迹。

## 2026-08-24T21:12:36Z — 事实报告进入终审

- 已把双对象离线结果写入独立事实报告：A 的 3 个三指允许接触预测提案全部碰桌，B 没有三指允许接触预测提案；任务、IK 和 Isaac 均明确为“未到达”。
- 报告将高度、保持、滑移和桌面接触写为“不适用”，没有用 0 伪装动态测量。
- 生产源码冻结；剩余窗口只做 Academic/Complexity Supervisor 只读审查和直接相关验证，不再新增候选变体或调整安全门。

## 2026-08-24T21:21:22Z — 双监督终审通过

- Academic Supervisor 确认所有接触均表述为离线预测，12 N 任务、IK、Isaac、50 mm 和 2 s 均为未到达；P1–P4 未被宣称验证。
- Complexity Supervisor 确认本次报告差异没有生产源码，路线生产源码净增 `2069 NLOC`，12 N、3 rad/s、50 mm、2 s、真值隔离和硬件未授权均未改变。
- 系统 Python 因缺少 `python-fcl` 得到 `26/27`；绑定项目现有 FCL site-packages 后同组为 `27/27`。前一次缺包失败保留，不把测试数量当作物理进展。
