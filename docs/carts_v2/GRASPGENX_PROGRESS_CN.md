# GraspGenX-CARTS 无人值守进展

## 2026-08-24T15:18:00Z — 上下文恢复六句话

- 最初目标：两个 D38999/J599 对象使用同一生成器、描述器、物理筛选和控制参数，由三根真实指腹抓住允许表面并在 Isaac 中离桌、抬升至少 50 mm、保持至少 2 s。
- 当前已完成：路线分支已建立并推送，官方源码固定到 `b942909`，5 个跨对象一致的 KCG 三指手预构型描述器已经生成。
- 当前真实物理结果：9 个预构型在描述器闭合相位 `0.50` 下共检查 4203 个空手开合状态且没有自碰；新路线尚未在 Isaac 接触或抬起连接器。
- 当前唯一主要阻塞：官方 `uv sync` 正在构建大体积 CUDA/TensorRT 依赖，官方三指手正对照尚未产出六维抓取位姿。
- 最近用户指令属于：`METHOD_REVISION + NEW_AUTONOMOUS_EXECUTION_WINDOW + UNATTENDED_EXECUTION`，不是硬件授权或降低物理标准。
- 下一步：完成官方正对照，同时把两个真实登记网格转换为隔离推理输入，随后检查新候选是否真正覆盖斜向和侧向姿态。

## 当前证据边界

- 已验证：描述器基于登记手部网格、真实 mimic 关系与合法关节范围；固定相邻关节检查步长不超过 `0.0015 rad`。
- 尚未验证：GraspGenX 对 KCG 手的推理输出、两对象路径安全候选、任务承载、IK、Isaac 接触、离桌、抬升和保持。
- 结论上限：离线手部几何验证；`hardware_authorized=false`、研究动态和正式动态通过均保持 false。

## 2026-08-24T16:21:43Z — RTX 5070 Ti 隔离运行时联通

- 官方 `uv sync` 的 PyTorch 2.6+CUDA 12.4 轮子不包含 `sm_120`，实际 GPU 张量在模型加载前失败；未修改 GraspGenX 源码或模型。
- 按官方源码已验证的 PyTorch 2.7 兼容范围，在同一隔离 `.venv` 内改用 PyTorch `2.7.0+cu128`、torchvision `0.22.0+cu128`；`trimesh` 保持官方锁定的 `4.5.3`。
- 模块均来自 GraspGenX `.venv`；架构列表包含 `sm_120/compute_120`，RTX 5070 Ti 实际矩阵和张量计算产生有限结果。
- 这只关闭运行时兼容问题，尚不证明 checkpoint 可加载、会输出抓取位姿或 KCG 手能抓起连接器。
- 下一步：固定官方 Hugging Face revision、下载真实 LFS 权重和样例，再运行内置三指手的 headless 正对照。

## 2026-08-24T16:39:00Z — 官方三指手正对照通过

- 官方 `robotiq_3f` 对官方 `banana.obj` 在 RTX 5070 Ti 上一次加载模型，返回 `20/20` 个有限、合法齐次变换的六维候选。
- 模型加载 `1.596 s`，推理 `0.522 s`；置信度范围 `0.186690–0.986697`，峰值分配显存约 `616 MB`。
- checkpoint、官方描述器、源码 commit、隔离 Python/PyTorch/CUDA 来源和小型输出均已哈希绑定；静态总览显示候选从多个方向包围样例物体。
- 这证明官方模型、权重、GPU 和文件输出链可用；不证明 KCG 手、连接器碰撞、任务承载、Isaac 抬升或硬件成功。
- 下一步：模型仍只加载一次，顺序处理 5 个固定 KCG 描述器和两个真实登记网格，再检查候选是否真正覆盖侧向与斜向。
