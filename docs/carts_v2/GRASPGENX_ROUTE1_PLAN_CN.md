# GraspGenX-CARTS 路线一 12 小时计划

## 时间与边界

- 窗口：`GRASPGENX_ROUTE1_12H_UNATTENDED`
- 实际开始：`2026-08-24T14:46:08Z`
- 硬截止：`2026-08-25T02:46:08Z`
- 起点：`bd710ee5941f963ee404d6ede236206560f215bf`
- 分支：`carts-grasp-graspgenx-route1-20260824`
- 硬件：`hardware_authorized=false`
- 结论上限：仿真研究；旧正式证据线继续失败关闭。

## 最小执行路线

| 窗口 | 物理问题 | 计划产物 | 失败后的有限动作 |
|---|---|---|---|
| 0:00–0:30 | 固定新方法身份和恢复边界 | 分支、计划、状态、manifest、监督审查 | 删除文档美化，不延迟官方正对照 |
| 0:30–1:30 | 官方模型是否能在本机 GPU 输出六维抓法 | 内置三指手样例的位姿、分数、耗时、显存 | 只区分依赖、权重、CUDA、输入、输出；两种官方隔离方式上限 |
| 1:30–3:00 | KCG 手掌基坐标与固定预构型是否物理一致 | 最多 5 个描述器、mimic/自碰撞/箱体/静态图 | 保留实际合法数量，不放宽自碰撞 |
| 3:00–4:30 | 六维搜索是否覆盖旧方法遗漏的斜向与侧向 | A/B 各最多 256 个去重提案与覆盖报告 | 查尺度、坐标链、描述器轴；不调对象位姿 |
| 4:30–5:30 | 哪些提案真实不碰桌且名义承重 | 完整闭合、PAD、12 N 门、任务字典序 Top-3 | 缩小昂贵 Top-K，不降低物理门 |
| 5:30–6:00 | 机械臂是否可达且接近/抬升路径安全 | 现有 bounded IK 和路径检查 | 只有全因求解能力失败时限时评估 cuRobo |
| 6:00–9:30 | A 能否三指接触、离桌、50 mm、2 s | A Top-3 动态与全部失败证据 | 单候选淘汰；共同根因只做一个最小因果修复 |
| 9:30–10:30 | 同参数能否迁移到 B | 参数冻结后的 B 动态 | 记录失败，不按 B 回调参数 |
| 10:30–11:30 | 结果是否可重复、旧/新生成器差异何在 | 优先重复；有余时最小消融 | 动态未闭环则取消可选消融 |
| 11:30–12:00 | 交付能否恢复且不夸大结论 | 定向验证、监督、报告、commit/push/bundle | 到点停止新增功能 |

## 当前任务卡：官方三指手正对照（最多 60 分钟）

- 要改变的物理/工程结果：确认官方预训练模型、GPU、权重和输出坐标格式确实可用，避免把项目适配错误误判为模型失败。
- 类型：必要实现，也是后续自研手推理的正对照。
- 成熟复用：官方 `demo_object_mesh.py`、内置 `robotiq_3f` 或 `barrett_hand`、官方 checkpoint。
- 唯一主要改变量：在隔离环境中运行官方原样 headless 推理；不改 kcgtest1 物理代码。
- 观察数据：`(K,4,4)` 位姿、置信度、运行时间、GPU/显存、随机种子、输出文件和版本哈希。
- 失败判据：依赖、checkpoint、CUDA、样例输入或输出保存仍未被区分。
- 最简单替代：`uv sync` 失败后仅尝试官方允许的 Python 3.10/3.11 隔离 venv；不重写模型。
- 对 Isaac 影响：这是进入六维候选的最短前置；60 分钟后无论成败都转入可并行的描述器/适配器工作。

## 当前任务卡：分层合并后的 A/B 物理筛选（最多 45 分钟）

- 要改变的物理结果：确认官方已生成的安全顶向/侧向抓法不会在 `640→256` 压缩时被全局最远点误删，并找出真正不扫桌的三指接触候选。
- 类型：必要方法修正，直接服务首次 Isaac 动态门。
- 成熟复用：现有 GraspGenX 文件、六维去重、闭合预测、完整顺序扫掠和任务载荷评价；不新增碰撞引擎。
- 唯一主要改变量：每个描述器和官方世界接近方向先保留固定份额，再用原六维最远点补齐到 256。
- 观察数据：A/B 的闭合存活、扫桌拒绝、任务通过、方向/描述器分布和候选受力余量。
- 失败判据：分层后仍无路径安全三指接触，或候选仅靠未解决的非指腹/自碰撞检查存活。
- 最简单下一步：若有路径幸存者，先补现有快筛缺失的手部自碰与非指腹—对象薄查询；若没有，只定位生成姿态与真实闭合几何，不改物性或安全门。

## 当前任务卡：指尖参考深度因果实验（最多 60 分钟）

- 要改变的物理结果：让官方 OBB 生成器使用真实允许 PAD 的最前端接触深度放置手基座，检验现有整手系统性靠桌是否来自参考点取在 PAD 质心。
- 类型：必要方法修正；直接服务完整接近/闭合路径门，而非报告美化或参数调优。
- 成熟复用：官方 `graspmoe` 的 `fingertip_depth` 语义、现有登记 PAD 源面、FK、同模型推理和完整离散扫掠；不新增生成器或碰撞后端。
- 唯一主要改变量：`fingertip_depth` 从三块 PAD 质心平均深度改为三块 PAD 各自最大 `+Z` 深度的平均；预计增加约 `14.7–15.0 mm`。
- 观察数据：新旧描述器深度差、A/B 候选覆盖、三指闭合数、桌面安全数、最小间隙及首次碰撞阶段/连杆。
- 失败判据：同模型、种子、预算重生成后仍没有桌面安全三指接触候选，或改善不足以改变主要碰撞模式。
- 最简单替代：失败后只做“允许接触表面高度与手工作区中心”的一次语义区分，不调桌面、对象位姿、物性、z-offset、力量或候选编号。
- 对 Isaac 影响：当前没有几何可执行候选，修正描述器参考深度是进入 Isaac 的最短必要动作；未通过前继续禁止动态。

## 当前任务卡：open/half 工作区联合可见性（最多 45 分钟）

- 要改变的物理结果：保留物体表面落在 half-open 真实指腹工作区、但不在 open 瞬时工作区内的较高候选，检验它们能否在顺序闭合时避免扫桌。
- 类型：必要生成接口修正；服务完整接近/闭合路径门。
- 成熟复用：描述器已有 open/half 两个登记 AABB，并分别调用官方点云可见性函数；不复制官方过滤器。
- 唯一主要改变量：每个 raw pose 的保留条件由 `official_open_visibility` 改为 `official_open_visibility OR official_half_visibility`。
- 观察数据：新增 half-only 数、三指闭合数、桌面安全数、FCL安全数及最好最小间隙。
- 失败判据：A/B 同规则重生成后仍没有任何桌面安全三指接触候选，或新增项只产生非指腹/自碰。
- 最简单替代：失败后转向“完整真实允许表面条件化提案”而不是继续调整工作区箱、z-offset或桌面。
- 对 Isaac 影响：当前 open-only 全池路径安全为 0；联合可见性是进入动态前最后一个直接由现有描述器字段支持的有限生成实验。

## 当前任务卡：登记允许表面条件化提案（最多 45 分钟）

- 要改变的物理结果：把六维提案集中到三块指腹实际允许接触的外侧三角面，检验能否从“路径安全候选 0”得到至少一个完整闭合不扫桌的抓法。
- 类型：一次候选生成方法的有界区分实验；若双对象同规则有效，可作为语义条件化消融，不是抓取成功证据。
- 成熟复用：现有 `FaceRoleMap`、确定性按面积采样、官方 `run_planner_on_object()`、完整对象网格闭合/FCL/任务评价；不新增生成器或碰撞后端。
- 唯一主要改变量：GraspGenX 输入从完整对象表面点云改为同一登记网格的允许面 ROI 点云；采样数仍为 8192，种子仍为 `20260824`。
- 观察数据：允许面数与面积、条件点云哈希、diffusion/OBB 数量、三指闭合数、桌面/FCL安全数和任务评价数。
- 失败判据：A/B 同规则固定预算后仍没有三指允许接触且完整路径安全候选。
- 停止边界：失败后不再修改面标签、工作区箱、候选偏移、对象/桌面位置或力量；转入描述器训练域与手部几何适配结论收口。
- 证据边界：条件点云只生成提案；碰撞、非指腹、闭合与受力始终使用完整登记对象网格，GraspGenX 分数只作分布外启发式后置排序。
- 对 Isaac 影响：当前没有安全候选可运行；该实验不推迟已具备条件的动态运行，通过后才允许进入 IK 和 Isaac。

## 当前任务卡：官方手同对象 diffusion 对照（最多 10 分钟）

- 物理/算法问题：区分连接器完整点云是否令学习分支整体失效，还是 KCG 长指描述器/手几何适配更可疑。
- 唯一改变量：固定官方 `robotiq_3f`，同一 A/B 完整点云、种子 `20260824`、每对象 256 条，只运行 `diffusion` 一次。
- 观察数据：有限位姿数、平移相对对象尺度和分数范围；跨手分数不作直接优劣比较。
- 失败判据：命令/API失败或只得到 OBB；不得换手、换种子、调阈值或把官方手结果接入 KCG 物理链。
- 证据边界：只作输入域/描述器域区分，不是 KCG 抓取候选或动态成功。

## 当前任务卡：单描述器可见性根因复盘（最多 45 分钟）

- 要改变的物理/算法结果：区分四个预构型为何有模型原始提案、却没有任何对象表面点落入登记的 open/half 条件箱；避免把变换、尺度或 schema 接线错误误写成模型能力限制。
- 类型：进入完整闭合路径门之前的一次只读根因诊断，直接服务六维多预构型覆盖要求。
- 成熟复用：官方可见性函数、当前冻结描述器、同一对象点云、同一模型、同一随机种子和官方 object-PC 默认参数；不新增生产实现。
- 唯一主要改变量：无；只对现有固定输入保存每个描述器的箱内点数、最近有符号箱距和原始位姿分布。
- 观察数据：A/B 每描述器阈值后原始提案数、open/half 最大与分位箱内点数、最近箱距、平移/接近方向范围，以及刚体变换、点云尺度和箱体单位检查。
- 失败判据：固定输入仍不能区分变换/schema/尺度/模型姿态，或结论依赖调整对象、阈值、条件箱、种子和预算。
- 最简单下一步：若证实接线错误，只做一个保持代码规模上限的替换式修复；否则记录为官方模型与当前长指描述器适配限制并转入收口。
- 对 Isaac 影响：只有恢复多描述器覆盖并随后通过完整闭合、任务与 IK 才可能启动；本诊断本身不是动态授权。

## 当前任务卡：纠正提案顺序后的双对象真实快筛（最多 60 分钟）

- 要改变的物理结果：让每个描述器的高置信度六维姿态先到达真实整手闭合与碰撞检查，确认 A/B 是否至少存在一个三指允许接触且不扫桌的抓法。
- 类型：必要方法接线修复后的离线验证，直接服务首次 Isaac 动态门。
- 成熟复用：官方 object-PC 推理与 scene-PC 粗筛、现有完整顺序闭合、登记全手 FCL、12 N 任务评价；不增加碰撞引擎。
- 唯一主要改变量：open/half 点云可见性由硬门降为审计，每描述器按 `(-score, raw_index)` 留最多 128；其余模型、点云、描述器、种子、预算和物理门不变。
- 观察数据：A/B 每描述器保留数、六维覆盖、scene-PC 粗筛数、闭合预测数、扫桌/FCL拒绝数、12 N 名义任务数和可执行候选数。
- 失败判据：纠正后仍无三指允许接触且完整路径安全候选，或候选只靠未检查的非指腹/自碰关系存活。
- 最简单下一步：若有候选，继续任务和 IK；若没有，按首个真实拒绝阶段收口，不再调整 visibility、阈值、对象、箱体或力量。
- 对 Isaac 影响：只有完整几何、12 N 名义任务和 IK 同时通过才开启研究动态；多描述器恢复本身不授权 Isaac。

## 监督结论（已采纳）

1. 先用一个真实描述器回放验证 GraspGenX 基坐标到 `handbase_link`，再冻结最多 5 个描述器；模型只作隔离的六维提案器。
2. 只加一个薄适配层，复用 V2；手—桌快筛不冒充全手—对象—环境完整路径，Top-3 还要经过绑定 PhysX 预飞。
3. 首次对象 A Isaac 提前至第 6 小时前；正式误差、MoveIt/curobo、完整消融在此之前全部后置。

Lead 采用单进程文件输出，不建立常驻 ZMQ 服务：同一进程一次加载模型、顺序处理描述器和对象并写 JSON/NPZ，功能更小且保持官方模型语义。

## 计划/实际

| UTC | 计划 | 实际 | 偏差 | 下一动作 |
|---|---|---|---|---|
| 2026-08-24T14:46:08Z | 建立分支并核对 GPU/进程 | 新分支从 `bd710ee` 创建；RTX 5070 Ti 可用；无残留 Isaac 进程；官方仓库固定 `b942909` | 按计划 | 完成官方三指手正对照 |
| 2026-08-24T15:18:00Z | 官方正对照与 KCG 描述器并行 | 官方 `uv sync` 持续构建依赖；5 个描述器已生成，9 个预构型在相位 0.50 下共 4203 个空手状态无自碰 | 官方安装约落后 18 分钟；描述器提前完成 | 保留官方安装单次有界执行，同时接入真实对象网格和候选文件适配 |
| 2026-08-24T16:21:43Z | 应已进入 KCG 描述器/推理 | SM120 兼容运行时实际 GPU 通过；描述器和文件适配已完成；官方 checkpoint 两个 LFS 权重并行下载中 | 官方正对照约落后 51 分钟，原因是原锁定轮子不含 SM120；主线接线未等待 | 取消可选文档美化；权重落盘即跑正对照和 A/B 一次加载推理 |
| 2026-08-24T16:39:00Z | 官方正对照后进入 KCG 推理 | 官方三指手样例返回 20/20 个有效六维位姿；模型只加载一次，推理 0.522 s；版本、权重和输出已绑定 | 正对照比原计划约晚 69 分钟，但描述器、对象输入和薄适配已并行完成 | 立即运行 A/B 同进程推理，取消可选论文图和服务化工作 |
| 2026-08-24T17:19:05Z | 应完成 A/B 六维生成并进入物理筛选 | 修正约 0.29 m 描述器原点缺失；A/B 各 640 条经官方工作区语义保留；A 的 256 条闭合存活为 0，B 的 256 条覆盖退化 | 离线物理筛选约落后 49 分钟；原因从运行时转为候选压缩/接触几何区分 | 并行检查 A/B 截断前 640 条；若存在存活者，修正压缩顺序而不改模型或物理门；否则审查描述器/生成语义 |
| 2026-08-24T18:21:11Z | 应进入物理快筛/任务评价 | 世界坐标和 dense top/side 已修正；A/B 旧全局合并各有 5 个三指接触但全扫桌。只读诊断证明 d02 顶向安全候选分数名次 3/7 在每描述器层保留，却在跨描述器最远点层被删 | 约落后 111 分钟；已取消可选图表、消融和 cuRobo | 用固定描述器×方向配额重跑 A/B；有路径幸存者立即补齐非指腹/自碰门并进入任务评价 |
| 2026-08-24T18:49:36Z | 应已进入 Isaac 预检查 | 发现对象表面点云未实际绑定 seed；已修正并两次逐项复现。固定池 A/B 为 2/7 个三指接触、均 0 路径安全；FCL 自碰/非 PAD 门已完成 | 约落后 140 分钟；取消全部可选消融、cuRobo和论文图 | 用最多25分钟扫 A 全部可见 raw pool；按存在性结果选择有限预算修正或描述器/提案根因，不降低物理门 |
| 2026-08-24T19:49:00Z | 应已进入对象 A Isaac | open/half 正式池 A 为 256→闭合 0，B 为 256→闭合 2→扫桌 2；额外完整筛完 A 42、B 22 个 half-only raw 差集，闭合存活均为 0 | 约落后 3 小时；Isaac 因无安全候选失败关闭，已取消可选消融和新规划器 | 一次双对象允许面 ROI 条件化提案；失败即停止局部生成修补，通过则立即进入任务门与 IK |
| 2026-08-24T20:08:58Z | 应处于对象 A 动态 | 同一 ROI 方法的 A 为 256→3 个闭合→0 个桌面安全；B 为 256→0 个闭合；尺度与坐标链复核无错误 | 约落后 3 小时 20 分；生成路线命中预登记停止判据，动态门因无安全候选失败关闭 | 冻结生成器，完成绑定重放、研究/正式门语义、学术局限、定向测试与可恢复提交 |
| 2026-08-24T20:43:00Z | 应处于对象 B 同参数动态 | 官方手对同一 A/B 完整点云各得到 256 个有限位姿；旧 B 的 6 个桌面安全项均被整手 FCL 证实为 `f1Link3` 非指腹碰物；37 项接口/碰撞定向测试通过 | 动态仍落后约 3 小时 57 分；无安全候选使 Isaac 失败关闭，取消全部可选消融和规划器接入 | 提交冻结源码后，对 A/B 从同一 commit 做最终绑定重放并收口事实报告 |
| 2026-08-24T21:06:00Z | 应处于对象 B 同参数动态/重复实验 | `db540b3` 同源重放确认 A `256→3→桌面安全0`，B `256→闭合0`；两对象任务、IK、Isaac均未到达 | 动态落后且已被几何门安全关闭；没有通过降低标准追赶 | 提交小型失败证据与最终报告，保留剩余窗口做只读复核和定向验证 |
| 2026-08-24T21:12:36Z | 应处于对象 B 同参数动态/重复实验 | 双对象证据已 push，事实报告初稿完成；生产源码冻结 | 无安全候选，Isaac 仍失败关闭 | 两位监督者终审、定向复核、报告提交；不新增候选变体 |
| 2026-08-24T21:32:32Z | 应处于对象 B 同参数动态/重复实验 | 参考 `/usr/bin/python3` 重放的 A/B 候选与覆盖逐字节一致；删除运行耗时字段后结果也一致，复现证据已 push | 无安全候选，动态仍未到达；改用不同 NumPy/SciPy 会有数值字节漂移 | 绑定参考运行时，继续只读证据完整性审计 |
| 2026-08-24T21:49:19Z | 应处于对象 B 同参数动态/重复实验 | 核清 `67.3 mm` 是官方程序化配置箱分量而非点云或完整训练分布；发现旧动态 `2 mm` 事后穿透接受阈值无来源且本路线未执行 | 物理主线仍被 0 个安全候选关闭；不以文档审计冒充动态进展 | 修正口径并 push；剩余窗口不改候选或物理参数，只做事实收口 |
| 2026-08-24T22:47:00Z | 应处于对象 A/B 动态与重复 | 官方有限桌面粗筛完成：A `128→29`、B `128→23`；覆盖仍因单一描述器失败，闭合/任务/IK/Isaac 未运行 | 动态落后约 4 小时；没有用粗筛保留项绕过覆盖门 | 对四个零可见描述器做一次固定输入根因诊断；只修证实的坐标/schema 错误，否则收口 |
| 2026-08-24T23:45:19Z | 应处于双对象动态/重复 | 纠正 visibility 硬门和闭合采样后，A `256→scene86→闭合0`、B `231→scene34→闭合0`；过滤前全池也闭合0 | 动态仍被三指允许接触几何门关闭；未降低任何物理门 | 并行做未截断 raw 池闭合诊断并先提交可恢复的当前事实 |
| 2026-08-25T00:10:10Z | 应处于双对象动态/重复 | 未截断 ROI 池 A `547→闭合0`、B `589→闭合0`，Top-128 原因排除；双监督确认完整对象输入纠正 | 动态继续由接触几何门关闭；发现生产输入语义偏离官方 object-mesh | 用替换式小改恢复完整网格 8192 点，重生成/重绑 A/B，不调参数 |

## 当前任务卡：官方 object-PC 默认组合重放（最多 90 分钟）

- 要改变的物理结果：区分旧混合参数中的深层 Z 偏移是否把手掌提案推到闭合扫桌/无接触位置，并重新寻找不扫桌的三指接触候选。
- 归类：方法合规修正，同时直接服务第一次安全 Isaac 动态门。
- 成熟复用：官方 `demo_object_pc.py` 的用户级默认参数、现有一次模型加载、文件适配器与完整 V2 物理筛选；不改模型和碰撞后端。
- 唯一主要改变量：除用户固定的 `num_grasps=256` 外，GraspMoE 参数原样采用当前官方 object-PC 示例默认；A/B 同参数一次重放。
- 预期观察：A/B 原始/保留候选数、三指闭合数、扫桌数、任务可行数以及是否出现研究可执行候选。
- 失败判据：两对象仍无完整路径安全候选，或官方默认组合无法产生固定预算的有效六维提案。
- 时间上限：90 分钟；不追加第三套参数，不做参数扫描。
- 失败后：保留旧混合参数结果作诊断，继续一次薄的官方 scene-PC 碰撞过滤合规补接；仍无候选则收口，不启动 Isaac。

## 当前任务卡：官方 scene-PC 桌面过滤合规补接（最多 45 分钟）

- 要改变的物理结果：在进入 V2 闭合预测前，提前剔除张开手真实注册网格距离有限桌面过近的六维提案，并记录 A/B 各自剔除数；它不能创造新候选。
- 归类：必要方法合规与离线区分实验，直接补齐用户规定的官方碰撞过滤步骤，不是报告美化。
- 成熟复用：官方 `filter_colliding_grasps()`、当前 9 个注册手部 link 网格、既有 FK、描述器坐标变换和有限桌面；不写新碰撞引擎。
- 对 Isaac 的影响：当前多描述器覆盖门已经关闭，过滤结果不会打开动态门；它只防止未来把明显靠桌提案送入昂贵闭合与规划。
- 更简单方案：直接过滤现有 A/B 提案 JSON，不重跑模型；目标连接器点云必须排除，避免误杀预期指腹接触。
- 唯一主要改变量：启用官方场景点云过滤，固定 `0.02 m` 距离、每描述器 2000 个真实手表面点、有限桌面最多 8192 点；两个对象完全相同。
- 预期观察：输入、保留、拒绝数；每描述器计数；桌面点云和注册手网格哈希；当前覆盖门仍按原规则失败关闭。
- 失败判据：不能用真实注册手网格和有限桌面构造官方函数输入，或必须超过本轮源码硬上限；届时保留方法缺口，不用 dummy box 冒充。
- 时间上限：45 分钟；不改描述器、候选位姿、对象、桌面、碰撞阈值、任务力量或 Isaac 控制器。

## 当前任务卡：未截断原始提案的闭合诊断（最多 45 分钟）

- 要改变的物理/算法结果：判断每描述器最高分 128 的固定预算是否丢掉了较低模型分数、但能由三块允许指腹闭合接触的姿态。
- 类型：一次只读候选预算消融，直接区分“分数截断”与“描述器/模型姿态几何不匹配”。
- 成熟复用：官方同一模型推理、当前描述器、真实 PAD、登记对象网格和控制步长闭合预测；不改生产算法或碰撞后端。
- 唯一主要改变量：诊断范围从每描述器 Top-128 扩为阈值后的全部原始提案；对象、种子、模型、描述器、阈值、Z 偏移和闭合规则均不变。
- 观察数据：A/B 每描述器与分数分位的三指闭合幸存数、首个失败原因和运行身份哈希。
- 失败判据：全池仍无三指允许接触，或运行身份漂移；前者指向描述器/模型姿态适配，后者丢弃本次诊断。
- 时间上限：45 分钟；不把诊断池自动提升为生产预算，不启动 Isaac。
- 下一步：若低分区有幸存者，监督审查固定预算语义；若没有，停止候选分数层面的补丁并转入事实收口。

## 当前任务卡：完整对象网格条件化纠正（最多 60 分钟）

- 要改变的物理/算法结果：让 GraspGenX 看见连接器完整外形和真实点云中心，再由后续 PAD 语义门判断允许接触，避免把一小片允许面误当成整个物体。
- 类型：官方 object-mesh 输入语义的必要方法纠正，直接服务三指接触候选生成。
- 成熟复用：官方 object-mesh 按面积采样、现有标准化完整网格、同一推理 runner 和 V2 下游门；不新增生成器或碰撞器。
- 唯一主要改变量：8192 个条件点从 `faces[allowed]` 改为完整登记网格全部三角面；A/B 同规则，种子仍为 `20260824`。
- 保持不变：模型、checkpoint、5 个描述器、256/128 预算、官方阈值与 GraspMoE 参数、0.0015 rad 离线步长、碰撞、12 N、IK 和动态标准。
- 观察数据：完整点云哈希与尺度、每描述器提案、六维覆盖、scene-PC、三指闭合、完整扫掠、任务和 IK 计数。
- 失败判据：同一完整网格方法 A/B 仍无三指允许接触候选，或身份/尺度检查失败。
- 时间上限：60 分钟；不添加 FULL/ROI 双分支，不增加预算或调阈值。
- 下一步：有研究可执行候选则立即进入绑定预检查和 Isaac；无候选则把 ROI vs 完整网格作为消融并转入唯一根因收口。

## 登记对象输入的精确复现命令

以下命令只从同一配置重建两对象的完整米制网格与允许面身份；不会运行 GraspGenX 或 Isaac：

```bash
PYTHONPATH=src/kcg_connector python3 - <<'PY'
from pathlib import Path
from kcg_connector.grasp.carts_v2.models import load_v2_inputs, write_standardized_object_manifest
root = Path.cwd()
config = root / "src/kcg_connector/config/carts_graspgenx_route1.yaml"
objects = ("current_d38999_26kj61sn_public_spec", "te_deutsch_d38999_26fj35pn_step")
inputs = {name: load_v2_inputs(root, config_path=config, object_id=name) for name in objects}
base = root / "artifacts/carts_v2/graspgenx/objects"
write_standardized_object_manifest(inputs, base / "meshes", base / "object_manifest.json", 8192)
PY
```
