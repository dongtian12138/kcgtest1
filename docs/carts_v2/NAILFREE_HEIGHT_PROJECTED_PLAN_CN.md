# NF-HPGS 12 小时自主执行计划

> 执行窗口：`NAILFREE_HEIGHT_PROJECTED_12H`
> 起点：2026-08-25T15:15:34Z
> 硬截止：2026-08-26T03:15:34Z
> 分支：`carts-grasp-nailfree-heightprojected-20260825`

## 一句话路线

先把三块可拆卸指甲从可见、碰撞和物理属性中真实移除，再把“桌面安全高度与三指可接触高度的交集”放在每角 Top-8 之前，最后才进入受力、机械臂路径和 Isaac 动态抓取。

## 冻结边界

- `hardware_authorized=false`，不启动真实硬件。
- `formal_dynamic_pass=false`，研究型仿真不得冒充正式或硬件结论。
- 不改变对象 A/B 的质量、质心、惯量、摩擦和场景身份。
- 12 N 仅为同对象共享的仿真研究操作上限；不扫描 8～15 N。
- 关节安全速度、每周期目标变化、50 mm 抬升、2 s 保持和在线真值隔离不降低。
- 旧带指甲模型、旧蓝色 PAD 语义、旧候选和 89/91 角检查点全部只读保留。

## 已采纳的监督简化

1. 无指甲资产只做一次可追溯转换，不建立通用网格修复框架：源哈希 → 删除面身份 → 无指甲可见网格 → 凸分解碰撞 → 质量/质心/惯量 → Xacro/Isaac 身份。
2. 完整手部路径先在 handbase 坐标系生成一次；有限桌面的最低安全高度由运动包络解析计算。27 种预构型先做对象无关乐观可达性排序，每个六维种子最多两种进入精确高度搜索；其余记为 `BUDGET_NOT_EVALUATED`。
3. 无对象 Isaac 资产和关节烟雾测试前移；动态成功以前不做可选完整消融，也不引入第二套规划器。

## 12 小时阶段

| 时间 | 计划物理结果 | 停止点 |
|---|---|---|
| 0:00–0:30 | 新分支、状态、计划、清单与监督结论可恢复 | 控制面提交 |
| 0:30–2:00 | 三末节指甲真实删除；碰撞与物理属性重算；三视图审计 | 无指甲几何审计通过或留下唯一几何阻塞 |
| 2:00–3:00 | `TASK_GRIP_SURFACE` 注册；无对象 Isaac 加载、关节和耦合一致 | 资产/接触语义提交 |
| 3:00–5:00 | `H_table`、`H_contact`、交集和投影；B131/B133 前后诊断 | 高度门进入 Top-8 前 |
| 5:00–7:00 | 复用或重生 A/B 六维种子；同方法离线候选与受力排序 | A/B 是否有任务可行候选 |
| 7:00–8:00 | bounded IK、接近与携带抬升路径 | 对象 A Top-3 预检查 |
| 8:00–10:30 | 对象 A 顺序三指接触、离桌、50 mm、2 s | 保存 Top-3 全部成功/失败 |
| 10:30–11:15 | 参数冻结后对象 B 原样运行 | 保存跨对象事实 |
| 11:15–12:00 | 定向验证、清理、监督、报告、提交和 push/bundle | 停止新增功能并交付 |

## 超过 30 分钟任务卡

### 无指甲手模型

- 要改变的物理结果：红色工具指甲不再参与外观、碰撞、质量或惯量。
- 直接服务：无指甲真实任务配置与后续桌面间隙。
- 成熟复用：现有 STL 解析、连通分量、CoACD/VHACD、trimesh 质量属性。
- 时间上限：90 分钟。
- 最简单方案：按已审计的唯一 TIP 连通分量删除；不做通用分割或 CAD 重建。
- 失败判据：三末节任一删除身份不唯一，或新碰撞体无法与保留原网格对齐。

### 接触语义与高度可行域

- 要改变的物理结果：候选只有在内侧抓持面能接触且完整路径不扫桌时才获得昂贵验证名额。
- 直接服务：三指有效接触、桌面安全和 Top-8 选择。
- 成熟复用：现有 FK、完整对象网格最近点、FCL 与顺序闭合预测。
- 时间上限：120 分钟。
- 最简单方案：新增一个单职责高度投影模块，避免扩张已到规模上限的快筛文件。
- 失败判据：高度区间为空时未失败关闭，或投影后仍复用旧接触点/停止角。

### 双对象离线与动态

- 要改变的物理结果：A/B 同方法产出安全候选，并在 Isaac 中尝试真实顺序接触和抬升。
- 直接服务：任务受力、IK、动态离桌和保持。
- 成熟复用：旧 5824 条六维种子、现有 task quality、bounded IK、Isaac 控制器和评价器。
- 时间上限：离线 120 分钟；A 动态 150 分钟；B 动态 45 分钟。
- 最简单方案：先跑确定性直接链；只有零候选时才启用最多 64×300 的预登记救援精修。
- 失败判据：继续必须降低碰撞/力量/速度/50 mm/2 s 或读取在线真值。

## 固定筛选顺序

`六维种子 → 无指甲模型 → 预构型 → H_table → H_contact → 高度交集/投影 → 重新计算三指接触与完整闭合 → 每角 Top-8 → 12 N 任务评价 → IK/全路径 → Top-3 Isaac`

## 证据与提交纪律

- 一个候选或一个角失败只淘汰该候选，不使项目停止。
- 未评估预算项不得写成物理失败；离线通过不得写成动态抓取成功。
- 每个大里程碑只提交明确相关文件并普通 push；网络失败保留本地 commit 并生成 bundle。
- 45～60 分钟或大里程碑更新本文件的计划/实际；落后先删可选消融和美化，绝不降低物理标准。

## 当前计划/实际

| 里程碑 | 计划开始 | 实际开始 | 计划完成 | 实际完成 | 状态/下一动作 |
|---|---|---|---|---|---|
| 控制面与监督 | 15:15Z | 15:15Z | 15:45Z | 15:34Z | 完成并推送 `60b5817` |
| 无指甲几何 | 15:45Z | 15:31Z | 17:15Z | 17:36Z | 精确可见网格完成；惯性为暂定估计，运行时碰撞/惯性未绑定 |
| 接触语义/Isaac 资产烟雾 | 17:15Z | 15:58Z | 18:15Z | 17:26Z（静态） | 任务抓持面独立接受；动态资产烟雾仍失败关闭 |
| 高度投影 | 18:15Z | 17:36Z | 20:15Z | 18:40Z | 生产顺序接通；A 0° 单角 64→2 离线幸存 |
| A/B 离线排序 | 20:15Z | 18:40Z | 22:15Z | — | 先运行 A 全池恢复式级联，再原样运行 B |
| IK/路径 | 22:15Z | — | 23:15Z | — | 待开始 |
| A/B 动态与收口 | 23:15Z | — | 03:15Z | — | 待开始 |

## 2026-08-25T16:17:00Z 计划/实际复核

- 计划：完成三末节无指甲外观、碰撞和物理属性；实际：外观与同密度质量估计完成，CoACD 64 块及首轮 Isaac VHACD 都因填回凹槽被失败关闭。
- 关键数据：每末节 `14192→13116` 面，删除 TIP 1076 面，新质量约 0.057130736 kg；CoACD 仍覆盖 365–377 个 TIP-only 样本。
- 接触语义并行提前完成：每指 4524–4589 个对象无关内侧任务面，旧 PAD 接触面 490/492 个均保留；尚未运行对象接触或候选。
- 进度判断：无指甲动态碰撞比计划落后约 20 分钟，高度数值核心提前完成；删除可选美化和消融，不降低碰撞误差门。
- 下一动作：仅运行一组参数合法、误差界导出的官方 VHACD 高保真配置；随后决定 Xacro/Isaac 接线，不做参数网格。

## 2026-08-25T17:36:00Z 计划/实际复核

- 几何身份纠正：每末节删除组件 155/156/157，共 `2356` 面；精确无指甲可见网格为 `11836` 面，旧 `1076/13116` 只覆盖指甲壳体而漏掉两个安装柱。
- f1/f3 暂定质量 `0.056985059 kg`，f2 `0.056997287 kg`；质心与惯量改为从绑定旧 URDF 基线扣除拆卸体质量矩，仍标记 `PROVISIONAL_NOT_RUNTIME_BOUND`。
- `TASK_GRIP_SURFACE` 独立审查接受：f1/f2/f3 为 `2200/2304/2199` 面；允许组件仅末端体和旧蓝色 PAD，关节壳体允许面为 0。
- CoACD/VHACD/闭合体都未通过动态碰撞接受；闭合体 `raw→closed` 最大偏差约 `1.70–1.72 mm`，Isaac 继续关闭。
- 进度落后约 21 分钟；停止碰撞参数调优，先用精确原网格推进高度门和 A/B 离线区分，动态前再解决唯一碰撞绑定。

## 2026-08-25T18:40:00Z 计划/实际复核

- 高度门已真实位于每角 Top-8 前；每种子 27 种预构型先比较，最多 2 种进入高度与原始网格精查，其余明确为预算未评估。
- 性能区分：旧逐高度重复完整闭合为 `26.49 s/种子`；逐控制步抓持面外包区间加投影点精确复核为 `0.77 s/种子`，没有放宽最终接触或碰撞门。
- 对象 A 0° 完整原子角：`64→2` 个投影点离线幸存；57 个保守高度交集为空，5 个只在所选投影点复核失败；总耗时 `109.798 s`。
- 证据只到单角、离线、逐控制步采样的整手高度可行；未做 12 N、IK、全臂路径或 Isaac，动态碰撞/惯性仍失败关闭。
- 相关 FCL 环境回归 `28 passed / 17.32 s`；下一动作是提交高度里程碑并启动对象 A 全池恢复式级联。

## 2026-08-25T19:21:33Z 计划/实际复核

- 高度链已绑定提交 `c7d07d8`；同种子重复指面位姿只查询一次，桌面累计下界严格超过接触上界时失败关闭早退，首角候选和投影结果与优化前一致。
- A/B 使用同一提交、配置和预算并行运行；A 已完成 `2/91` 角并累计 10 个几何候选，B 正在首角初始化，结果均未进入 12 N 排序。
- 监督只批准一次固定参数 PhysX SDF 资产审计；双向最大偏差、删除区零占用和烹饪日志未通过前，Isaac 保持关闭。
- 相关回归 `29 passed / 17.37 s`；生产新增 2186 NLOC，低于 2200 硬上限，后续不再增加生产算法行。

## 2026-08-25T19:42:48Z 计划/实际复核

- 同一提交与配置下，A 已原子保存 `15/91` 角、32 个高度/闭合几何候选；B 已保存 `18/91` 角、1 个候选。
- B 在预定 `1490 s` 切片边界退出；未完成角没有写入检查点，退出码 124 不解释为算法或物理失败。
- 无指甲 SDF 准备稿因单文件超限、只审第一指却可能升级整手绑定、查询坐标和固定参数回读不完整而被监督拒绝运行。
- 下一动作：A 到达同类安全边界后，只运行精简且失败关闭的第一指固定参数 SDF 诊断；随后 A/B 从检查点原样续跑。Isaac 抓取仍未启动。
