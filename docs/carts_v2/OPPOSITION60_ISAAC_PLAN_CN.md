# 60°对向抓取族与 Isaac 研究动态计划

> 执行窗口：`OPPOSITION60_ISAAC_12H`
>
> 开始：2026-08-25T22:17:59Z
>
> 硬截止：2026-08-26T10:17:59Z
>
> 分支：`carts-grasp-opposition60-isaac-20260826`

## 一句话路线

先用真实三指几何证明约 60°的一指对两指抓取族是否存在，再修正预构型、首次接触即停、桌面距离和多见证接触，最后用经审计的复合凸碰撞体先做对象 B 局部 50 mm/2 s 动态，再接完整机械臂和对象 A。

## 冻结边界

- `hardware_authorized=false`；不运行真实机械臂或三指手。
- `formal_dynamic_pass=false`；复合凸只服务研究型 Isaac，不能替代正式精确碰撞证据。
- 12 N、3 rad/s、0.18 rad/s、1/120 s、0.0015 rad/周期、50 mm、2 s和在线真值防火墙不变。
- 对象 A/B 的质量、质心、惯量、摩擦、桌面和初始位姿不变；对象 B结果不得回调方法参数。
- 不回到 H102，不建立连续碰撞证明器或 manager/contract/ledger/bridge/binder/certificate。

## 已知根因与唯一主要变量

旧链把对向族的 27 种预构型缩到2种、把手指推进到最大闭合作为高度包络、用倾斜连杆 AABB 作桌面硬门、只改世界 Z、只看一个全局最近见证点，并用旧 PAD 中心平均值定义“向内”。本轮先改变这些接触几何假设，不改摩擦、力量或控制增益。

## 固定执行顺序

1. 审计既有研究碰撞资产；旧资产若重新占据已拆指甲区域，不得直接用于动态，只做一次绑定 11836 面源网格的确定性复合凸构建，精确网格仍保留作离线和事后重放。
2. 在 `q_p=60°` 逐项保存同一组 27 种预构型的旧门/新门配对结果，回答三指允许面接触与桌面安全能否同时成立；一旦出现精确几何正对照即优先进入局部 Isaac。
3. `q_p∈[45°,75°]` 每1°、27预构型、12周向、3轴向生成结构化锚点；每角最多12个进入精修。
4. 多见证接触直接复用每指已有 33 个表面片并确定性选 16 个 → 首次接触即停路径 → 精确手网格—有限桌面距离 → 7变量有界精修。
5. 按几何、12 N任务和误差场景字典序得到对象 B Top-3。
6. 局部 handbase Isaac：逐指接触、预紧、离桌、50 mm、2 s；全部轨迹用精确网格事后重放。
7. 局部成功后只选一个成熟规划后端完成七轴路径，再冻结参数运行对象 A。

## 超过30分钟任务卡

### 复合凸研究碰撞资产

- 改变的物理结果：PhysX能产生无指甲手—连接器接触，同时不把已删指甲体积填回。
- 类型：必要实现与动态前置实验。
- 成熟复用：既有 CoACD/VHACD 产物、Isaac convex decomposition、trimesh/FCL审计。
- 时间上限：60分钟。
- 最简单方案：旧资产只作反例；一次性从正确无指甲源网格生成三末节同规则多凸部件并薄绑定，不重做通用分解器。
- 失败判据：删除区、任务抓持面或桌面侧误差无法界定，或关键60°轨迹精确重放穿透。

### 60°几何正对照与方法修复

- 改变的物理结果：确认一指对两指布局是否能三指接触且不扫桌。
- 类型：方法和高信息量区分实验。
- 成熟复用：现有FK、FCL、任务抓持面、SciPy优化器。
- 时间上限：150分钟。
- 最简单方案：先固定60°的27种配对正对照；直接锚点先用 Powell/SLSQP 有界局部精修，只有没有三指接触时才启用固定小预算 differential evolution，再扩大31角。
- 失败判据：全部27预构型在真实网格下均因同一首要几何原因失败。

### 对象B局部Isaac

- 改变的物理结果：三指真实接触后连接器离桌、抬升50 mm并保持2 s。
- 类型：研究动态实验。
- 成熟复用：现有顺序接触控制器、真值隔离、Isaac评价器和GPU容量门。
- 时间上限：240分钟。
- 最简单方案：handbase受控基座，不先引入七轴规划；Top-3依序尝试。
- 失败判据：继续必须放宽固定安全门或使用在线对象/接触真值。

## 12小时计划/实际

| 阶段 | 计划 | 实际 | 状态 |
|---|---|---|---|
| 分支、状态、监督 | 22:18–22:48Z | 22:18–22:29Z | 完成 |
| 复合凸资产与无对象烟雾 | 22:48–23:48Z | 22:29–23:03Z三条固定线拒绝；03:03Z残差vertex64静态门通过 | 运行时四门待验 |
| 接触/预构型/条件化路径修复 | 23:48–00:48Z | 22:31–23:03Z提前完成，33项定向检查通过 | 完成代码门，待真实正对照 |
| 60°正对照与45°–75°Top-3 | 00:48–02:18Z | 23:04Z开始；27组在1500秒中断，单组于23:42Z完成 | 进入查询性能根因复盘，仍提前约1小时 |
| 对象B局部Isaac | 02:18–04:18Z | — | 待开始 |
| 一个共同控制根因 | 04:18–06:18Z | — | 有需要才执行 |
| 全臂对象B | 06:18–08:18Z | — | 局部成功后执行 |
| 冻结参数对象A | 08:18–09:18Z | — | B成功后执行 |
| 重复、收口、push | 09:18–10:18Z | — | 待开始 |

## 监督与停止

初始三角色审查最多20分钟，只能提出3项更简单/更直接的修改。单个候选、一次API、一次测试、一次SDF或一次凸分解失败不能使项目停止。只有用户STOP、需要真实硬件、需要降低固定安全门/真值防火墙、Isaac/GPU最小正对照和一次恢复后仍不可用、不可逆仓库风险或硬截止到点，才结束窗口。

## 03:04Z 运行时资产与局部闭合任务卡

- 改变的物理结果：确认三末节残差vertex64碰撞体能被PhysX真实加载，初态无未授权穿透，并能按anchor2的60°预构型完成三指顺序闭合。
- 类型：动态前置必要实现和高信息量研究实验；不是正式碰撞证明。
- 成熟复用：现有无指甲URDF/Xacro、`import_robot.py`、对象B有限桌面场景、PhysX健康审计、顺序接触控制器和精确网格事后重放。
- 唯一主要改变量：把静态通过的192个凸块作为三末节多collision绑定；不改候选、关节轨迹、质量、摩擦、12 N或安全门。
- 最简单方案：先做无对象运行时导入与关节一致性，再做anchor2端点初态/闭合重放；未通过前不接完整抓举或七轴路径。
- 预期数据：凸块/哈希、初始接触、每指位置速度受力、目标单步变化、三指真实接触、精确网格穿透、PhysX警告和容量峰值。
- 失败判据：资产身份不闭合、初态穿透、PhysX健康失败、速度/受力急停或三指接触并非允许内侧面；保存后做一个能区分原因的最小实验，不降低门限。
- 时间上限：运行时资产生成与四门60分钟，局部闭合诊断60分钟；若现有完整抓举入口耦合过重，保留局部诊断而不重构大框架。

## 证据边界

- 60°静态图只证明几何正对照，不证明动态抓取。
- 复合凸局部动态通过只可写 `LOCAL_HAND_NOMINAL_RESEARCH_DYNAMIC_PASS`。
- 完整机械臂与对象A/B同参数成功以前，不能写跨对象动态成功。
- 退出码、离线候选、逆解、视频和测试都不能代替三指接触、离桌、50 mm与2 s的物理数据。

## 23:03Z 检查点

- 计划/实际：方法修正提前完成；碰撞资产虽按时完成，但三种候选表示均未通过预登记保真门。
- 唯一主要改变量：先固定60°与一个分层锚点，只比较27种预构型的真实接触停止路径。
- 预期观察：每指兼容接触、停止相位、禁抓面守卫、完整逐指路径桌面最小间隙。
- 失败判据：27种都无三指接触或全被同一真实路径原因拒绝；负结果保留为采样未解析时不得写几何无解。
- 下一步：若有几何幸存直接进入有界局部位姿精修；若没有，先按首要失败类型做一次区分实验，不扩展整池。

## 23:42Z 检查点与任务卡

- 要改变的物理结果：不改变接触判据，只减少手指明显远离对象时重复查询33个面片的规划时间，使27种预构型能在有界时间内完成。
- 服务对象：完整多见证三指接触和尽早进入Isaac；属于必要实现，不是论文美化。
- 现成实现：继续使用当前FCL完整对象距离与33面片查询；不接入新碰撞库。
- 唯一主要改变量：完整对象精确距离大于冻结接触阈值时返回“尚未接触”，只有进入阈值后查询全部面片与禁止面。
- 观察数据：单组结果一致性、FCL调用数、累计时间和27组外推墙时。
- 失败判据：任何接触/禁止面分类变化，或单组墙时没有明显下降。
- 时间上限：实现、3项以内回归和单组对照共45分钟；失败则回退，不做第三套接触算法。

### 23:55Z 结果

- 单组耗时由 `214.017 s` 降为 `136.484 s`，快 `36.2%`。
- 优化前后完整高度搜索结果逐字一致；129个探针及全部禁止面编号计数未变化。
- 全面片与禁止面精查调用数各由 `52754` 降为 `122`；监督结论 `APPROVE`。
- 下一步不再改接触数学，只增加可恢复的确定性分片执行，完成其余26种预构型。

## 00:38Z 首锚点27预构型完成与下一任务卡

- 实际结果：4个确定性分片覆盖完整27种预构型，共3477个高度探针；2808个无有效接触、669个仅一指接触，从未出现二指或三指同时接触，Isaac未启动。
- 证据边界：全部结果是离散采样未找到见证的 `UNRESOLVED`；只能说周向0°/低轴向首锚点的本次采样未找到三指见证，不能外推其余11锚点或整个60°抓取族。
- 主要分支原因：1782个探针由第一指先碰禁止面、1026个探针第一指无有效接触；另有669个探针在第一指接触后由第二指先碰禁止面，其中窄切向凸齿/槽侧壁face21232最频繁。当前不放宽面语义。
- 下一实验改变的物理结果：判断失败是否局限于该周向/轴向位置，还是跨空间锚点仍无法形成第二指接触。
- 成熟复用：原生成器、同一27预构型排序、同一高度搜索和FCL；只增加诊断脚本的锚点索引，不改数学。
- 固定样本：已有index 0（0°/低层），先新增index 1（30°/中层）的同一 `[0.2,0.2,0.2]` 预构型；30°错相用于避开对象外圈约45°重复凸齿，直接检验首锚点是否只是周向相位不合适。
- 时间上限：该探针1500秒；若出现三指见证即进入该锚点完整27种与局部精修，若仍命中同类切向槽壁，再按监督结果选择一个纯轴向或更大周向区分，不盲扫12锚点。

## 01:13Z 三指接触/桌面冲突与局部精修任务卡

- 实际结果：anchor1的固定 `[0.2,0.2,0.2]` 在 `z=0.628937548 m` 找到三指允许接触，停止相位约 `0.4718/0.3783/0.4707`；完整路径桌面间隙为 `-1.719 mm`。
- 唯一主要改变量：在用户登记的有限范围内联合调整handbase平移、姿态和 `q_p`，预构型lane保持不变。
- 改变的物理结果：让同一个一指对两指抓法同时保留三指接触并达到完整路径1 mm桌面余量。
- 成熟复用：SciPy有界优化、当前多见证接触、顺序闭合预测与完整路径FCL；不写新优化器或碰撞器。
- 分阶段：阶段A只比较三指最大接触间隙；阶段B只对阶段A少量前列点比较桌面/禁抓面路径；不同单位不求和。
- 预算：只处理anchor1和固定预构型，Stage A+B总几何评价不超过300，墙时不超过30分钟；出现A+B候选立即停止。
- 失败判据：预算内无同时通过点则记有界精修未找到见证，不扩大范围、不换预构型、不降低1 mm桌面余量。

## 02:15Z anchor1有界精修结果与anchor2区分实验卡

- 实际结果：总评价300次，其中291次唯一候选位姿引导、1次源见证完整桌面重放、8次Top候选完整重验；无完整通过点。
- 最好点：三指接触、预抓取路径和快筛都保留，完整路径桌面间隙 `0.441636 mm`，距1 mm硬门差 `0.558364 mm`。
- 证据边界：只表示anchor1、`[0.2,0.2,0.2]`和预登记七变量边界内的确定性300次搜索未找到通过点；不是连续无解证明，不否定60°抓取族。
- 要改变的物理结果：使用预登记anchor2的高轴向、60°周向组合探针继续寻找“三接触 + 1 mm桌面余量”；它同时改变轴向层和周向，不作单因子因果声明。
- 服务范围：直接服务三指接触与桌面安全正对照，属方法区分实验，不是参数调优或报告美化。
- 成熟复用：原生12锚点、同一 `[0.2,0.2,0.2]` 预构型、同一高度搜索、多见证接触和FCL；不改任何阈值或数学。
- 唯一实验输入变化：从锚点index 1切到index 2，它包含周向和轴向的预登记联合变化；对象、q_p、预构型、配置和处理顺序不变。
- 预期数据：高度探针三接触计数、停止相位、完整路径最小桌面间隙、首个禁止接触面和关节合法性。
- 失败判据：无三指见证或三指见证仍低于1 mm桌面余量；保存后继续未评估的预登记组合，不扩展anchor1局部预算。
- 时间上限：单预构型原子命令1500秒；命令结束即停，不在同一运行中扫其他锚点。
- 更简单方案：先运行一个已登记高层锚点，而不是立即打开31角×12锚点×27预构型整池。

## 02:32Z anchor2桌面安全三接触与受力/IK任务卡

- 实际结果：对象B、q_p=60°、anchor2、`[0.2,0.2,0.2]` 得到一个离线几何幸存者；三指接触、预抓取和逐指闭合通过，最小桌面间隙 `10.019253 mm`。
- 要改变的物理结果：回答这三个接触是否能在每指12 N仿真操作上限内平衡重力与抬升载荷，以及机械臂是否有有界逆解。
- 服务范围：直接服务任务受力、IK和局部Isaac前置门；是必要实现/实验，不是可选消融。
- 成熟复用：现有 `task_quality.py`、误差场景和bounded IK；只做单候选薄接线，不重写力分配或规划器。
- 唯一主要输入：固定anchor2幸存候选及其重新预测的三指停止点；不改力量、摩擦、质量、质心或候选位姿。
- 预期数据：名义可行性、最差误差余量、较差20%平均余量、最大单指力、关节/腕部利用率、IK误差和关节余量。
- 失败判据：名义载荷余量小于1或单指力超过12 N，则只标记该候选任务不可行并继续其他预登记候选；不扫力量。
- 时间上限：薄接线、单候选受力和IK共45分钟；若现有接口不能薄复用，先做单候选受力，不为IK重构管线。
- 更简单方案：直接评价当前一个幸存者，而不是为了排名重跑全部锚点池。

## 03:41Z 运行时资产导入里程碑与局部初始穿透任务卡

- 实际结果：完整无指甲手臂已在Isaac 6.0.1 GPU物理里推进3步；15个DOF、4组mimic、三末节192个凸碰撞块与新质量属性读回通过，PhysX容量警告/错误为0。
- 证据边界：没有加载连接器，所以只关闭 `ISAAC_IMPORT`门；不宣称初始无穿透、60°轨迹可执行或抓取成功。
- 要改变的物理结果：证明对象B与有限桌面加载后，anchor2预抓取位姿不会在第一个物理步由复合凸资产产生初始穿透或数值爆发。
- 服务范围：直接服务局部三指接触和后续50 mm抓举；是必要动态前置实验，不是资产报告美化。
- 成熟复用：同一URDF子树生成的hand-only articulation、现有对象B加载器、有限桌面和 `engine_health.py`；不写新碰撞引擎。
- 唯一主要改变：从“无对象完整手臂导入”切到“hand-only + 冻结对象B + 有限桌面的预抓取初态”；不启动手指闭合。
- 预期数据：物理时间、初始关节位置/速度/等效受力、对象与桌面姿态变化、实际碰撞身份、PhysX容量峰值和完整日志审计。
- 失败判据：初始穿透、非有限数、超速/超力、对象被无指令推走、容量警告或PhysX错误；失败时只定位资产/初态根因，不调摩擦、力量或安全门。
- 时间上限：hand-only导入与一次初态物理预检共60分钟；更简单方案是先固定handbase执行初态门，而不先接入七轴机械臂轨迹。

### 02:42Z 结果

- 12 N下名义重力与抬升平衡可行；名义物理参数下完整预登记扰动余量 `0.339826`，16误差场景最差/较差20%平均余量为 `0.111556/0.136928`。
- 研究任务分类为合格但尚不可执行；完整扰动和误差门不合格，`formal=false`。这两者不矛盾。
- bounded IK通过，位置/姿态误差 `3.68e-13 m / 3.16e-12 rad`；完整机械臂碰撞和路径尚未检查。
- 下一步只收口研究复合凸资产和局部Isaac预检；不为了提高扰动余量扫力量或改候选。
