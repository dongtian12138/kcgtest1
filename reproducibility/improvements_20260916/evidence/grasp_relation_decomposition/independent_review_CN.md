独立只读复核：**原表的变换顺序、前一拍对齐和四个关键数值正确，中文结论的主要范围合理。** 本次只从已封存的旧完整 14 读取 3 个唯一物理帧，从已封存 load10ms_repeat01 读取 4 帧，并读取对应小型控制/腕部传感文件；没有全档扫描、物理运行、生产改动或 repeat02 真值读取。

旧完整 14 第四段的四个关键量都来自同一条控制记录 **260041**，对应物理观测 **260040**，不是各自选择不同帧的峰值：

| 横向 XY 量 | 独立复算 |
|---|---:|
| 保存的虚拟中心控制误差 | 0.607843871317 mm |
| 实际 Nut 中心相对物理插座轴线偏心 | 0.029618279789 mm |
| 实际 Nut 中心减固定初始抓持关系预测 | 0.530466854914 mm |
| 原生手姿态与编码器 FK 得到的同一虚拟点之差 | 0.000167807013 mm，即约 0.168 µm |

四元数从 wxyz 转为 xyzw 后构建手的世界变换 H；初始实际 Nut 中心的手内坐标为 `H0⁻¹·[N0,1]`，当前固定关系预测为 `P=Ht·H0⁻¹·[N0,1]`，顺序正确。USD 保存的插座矩阵先转置为列向量约定，再用 `R_socketᵀ·(N−p_socket)` 求局部偏心，也正确。两回合物理插座旋转均恰为单位阵，因此这些例子的世界 XY 与插座横向平面一致。

除查源码时序外，还用独立保存的腕部记录验证：每个抽核控制行的前一拍 FK 位姿乘 hand_from_virtual_nut_axis_frame，重建出的 encoder_pivot_world_m 与控制行逐值相同（本次最大绝对残差为 0）；同编号的步后 active_targets 又与该控制行 nominal_arm_target 完全相同。若在旧段最差处误用同编号步后 FK，其虚拟点会相差 0.005469612 mm。这支持当前 `control step−1` 配对。0.607844 是该段**已保存控制行**中的最大值，也是最后一条控制行；其配对物理帧仍是 260040，不能改称最后推进后的状态或未另行保存的停止决定观测。

设 V 为编码器推算的虚拟中心，N 为实际 Nut 中心，P 为固定初始关系预测，T 为保存控制误差反推的目标，则 `e=T−V=(T−N)+(N−P)+(P−V)`。旧段最差处的 XY 向量（mm）为：

```text
目标减实际Nut        (−0.021406, −0.025466)
实际Nut减固定预测    (+0.149560, −0.508947)
固定预测减虚拟中心   (+0.067187, −0.041188)
相加                (+0.195341, −0.575601)，范数 0.607844
```

三项横向模长分别约 0.033267、0.530467、0.078807 mm，不能直接相加。最后一项同时包含初始实际中心与虚拟标定中心的偏置，以及很小的原生/FK 差异；它不等于 0.000168 mm 的纯原生/FK 比较项。0.029618 mm 是相对物理插座轴线的偏心，而目标 T 并非完全等于物理插座轴线，故也不必等于首项 0.033267 mm。

向量闭合本身是代数恒等式，因为 T 来自保存的 V+e；单靠原脚本 assert 不能证明时间配对。本次额外的前拍 FK/步后命令校验提供了这一证据。独立采用线性求解重建初始局部中心，与原脚本显式逆矩阵的全部标量/向量表差异不超过约 1.25e−13 mm，仅为浮点求值顺序差。

结论应保留以下边界：

- 0.530467 mm 表征**手坐标系中 Nut 中心的初始相对位置关系没有保持**所对应的世界横向差；不是接触材料点的纯摩擦滑移，也没有测量完整六维相对姿态变化。手指输出角变化、机构/求解器柔顺、滚动、滑移等可能参与，当前数据未把它们分别识别或量化。中文原文已经使用“可能/未分离”，是合适的；JSON 中 combines 应按这种可能包含的含义理解。
- 0.607844 mm 仍是所选虚拟点相对目标的实际控制跟踪误差。分解说明它不能直接充当 Nut 实体偏心；它没有单独测定机械臂关节伺服误差，也不能推出“全部误差就是机械臂跟踪”或“机械臂跟踪完全正常”。
- 两个完整回合的抓握/进入历史不同。load10ms_repeat01 的最大行 256753/前拍 256752 重算为 0.415889/0.123837/0.299609 mm，与原表一致，但两回合这张表本身不是单变量 50→10 ms 因果对照。
- 旧表“最大行”和“末行”重复了同一条控制 260041；这是选择逻辑的重复展示，不是两个独立见证。没有据此增加样本量或扩大统计结论。

本次没有发现需要修正数值、变换顺序或采样对齐的错误。证据：[独立重算 JSON](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/control_review/grasp_relation_decomposition/independent_pose_check.json)；[有界复算脚本](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/control_review/grasp_relation_decomposition/independent_pose_check.py)。原 comparison.json、review.py 和中文说明均保持不变。
