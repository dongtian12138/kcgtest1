本次独立只读结论（2026-09-17 UTC）：首轮改进版已真正张手并稳定保持 3 秒，但没有到达原机械止挡坐底状态，不能判为完整装配成功。**簧套承载 + 稳定 ≠ 原机械止挡坐底。** 本报告仅核对最终释放区间，并补核第五段正常停止的机器人/有限传动记录；不代替前段抓取、全流键槽或影像审查。

被核验回合：`/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/full_validation/load10ms_repeat01/run`。物理运行已结束；`motion_timing.json` 存在，归档索引为 277530 帧、末偏移 19090801262，与归档字节数一致。未启动物理、未修改生产源码、配置、原始记录或原验收标准。

最终释放结果记录 `socket_transport/nut_terminal_release/nut_reindex_controller_result.json` 为 completed=true、outer_abort_reason=null、failure_reason=null。边界与原始相位一致：卸载 [273690,273882) 共 192 帧；张手 [273882,274650) 共 768 帧；自由张手保持 [274650,277530) 共 2880 帧，按 960 Hz 恰为 3 秒。全部 3840 帧都记录一次物理回调、原生接触点完整保留、两接触报告通道一致。

| 3 秒保持区间的逐帧实测 | 结果 |
|---|---|
| 所有手部原始接触冲量范数之和 | 每帧精确为 0，无冲量阈值过滤 |
| Body 深度范围 | [14.582080096006367, 14.582080096006367] mm |
| 距 14.605 mm 的差 | 22.919903993633 µm，超出原 10 µm 到位范围 |
| 原 StopBox 正载接触组 | 每帧 0 组；any 与 every 均为 false |
| 正载簧套 | 每帧 128 个，始终同一组 128 个 |
| 原止挡与 128 簧套同时加载 | 每帧条件不成立 |
| Body/Nut 休眠标记 | 全部 2880 帧均为 [false,false] |

止挡/簧套正载仍按原报告的 header 冲量范数和 >1e-10 N·s 判据；“无止挡正载”不是声称所有浮点止挡冲量必为精确零，也不把单纯缺记录自动判为几何接触消失。本段记录覆盖完整且无休眠，与深度欠缺共同支持“未坐底”。释放区间的推力轴坐标绝对最大值 0.504702948 mm，小于原后备限位检查线 0.5999 mm；该结论仅覆盖末段，不能替代全回合后备限位检查。

卸载与张手的 960 帧中，正载手接触演员仅为 f1Link3/f2Link2/f3Link3 对 CouplingNut。按原源面投影/区域规则：

| 手部演员 | 实际源面 | 正载点数 / 涉及帧数 | 最大投影残差 | 允许区域外点 / 超过原 500 µm 投影范围点 |
|---|---|---:|---:|---:|
| f1Link3 | 原指甲 | 378 / 378 | 1.785299 µm | 0 / 0 |
| f2Link2 | 原指腹 | 341 / 341 | 1.788012 µm | 0 / 0 |
| f3Link3 | 原指甲 | 765 / 399 | 30.716119 µm | 0 / 0 |

没有意外演员或未分类正载点。正载最后一拍分别为 274067、274030、274088，均早于自由保持起点 274650。以上逐帧补核比旧报告仅用 any/max 的存在性检查覆盖更强，但未将补核另造为新的验收标准。旧 any/max 本身不能证明连续 3 秒同一组承载；本次实际遍历了全 2880 帧。

第五段从 272044 开始，末次成功归档控制/物理帧为 273689；下一次决定编号为 273690。有限传动独立检查 [272043,273690) 每指 1647 帧，包含进入该段时所用的前置观测帧。使用原配置 `4 - abs(120*(input_angle-output_angle))` Nm 重建弹性余量，结果如下。

| 传动关节 | 首次余量 ≤0.5 Nm | 停止所依据的 273689 帧余量 | 此核验区间最小余量 |
|---|---:|---:|---:|
| f1j2 | 273689 | 0.498682094925581 Nm | 0.498682094925581 Nm |
| f2j1 | 未发生 | 1.173532229131685 Nm | 0.829758864110933 Nm |
| f3j2 | 未发生 | 0.797685774032307 Nm | 0.677665861312581 Nm |

f1 在 273688 帧余量仍为 0.502272678726850 Nm；273689 帧弹性力矩为 3.501317905074419 Nm。三指余量与 early_regrasp 事件逐值相等，末机制输出角与末机器人编码器逐值相等；末帧三指的 elastic_effort_boundary_exceeded 与 native_drive_clipping_predicted 均为 false。输入自锁与弹性力矩不等同于传感器实测电机扭矩。

`te_coaxial_nut_interval.py:245` 在 advance 前判断余量并 break；`:278` 仅在成功 advance 后写控制行，`:299`/`:306` 保存并累计 last_applied_angle。因此本段已发角是 37.8760591609241°，累计已发角是 **354.7504418890766°**；仍有 **5.249558110923374°** 属于原 360° 总指令预算。未把未发候选角或卸载后的状态冒充停止点。报告中 sum_requested_stroke_angles_deg=403.125617° 累加的是各段曾请求的角度，不能拿它冒充实际已发角或认定已执行超 360°。

最后保存的控制行 step273689 使用传感器 step273688 的 50 ms 观测：合力 31.183422770 N，横向合力 3.331874846 N，弯矩 0.305812477 Nm，轴向扭矩 0.803025086 Nm；虚拟中心目标跟踪误差为 [0.518965284,-0.134883267,0.252175336] mm，XY 范数 0.536207480 mm。停止事件只保存三指余量，没有保存决定 273690 那次 update 的同拍过滤腕力或新目标误差，不能把上述数值说成精确停止决定拍观测。末机器人帧 273689 的原始腕信号和独立保护合力 30.373685280 N 已按各自字段保存在补证 JSON 中，没有改名为缺失的控制器过滤信号。

同拍编码器可直接重建虚拟中心：在 273689 帧相对本段初始中心的世界 XY 位移为 [-0.524587689,+0.136200712] mm，范数 **0.541980514 mm**。这是编码器中心位移，不是物体位姿，也不是未保存的新目标跟踪误差；使用同一算法重建上一帧，与保存控制行中心的差小于 1e-10 m。

对“仅增加一次换抓”的独立判断：当前最大追加旋拧段数为 3，已用完；总 360° 指令预算却未用完。源码现已允许最多 6 个追加段；按本次保存角度计算，把次数 3 改 4 会保留前五段角度分配，并在原换抓路径后允许最多再发 5.249558°。配置层面，追加一次原换抓路径可直接检验“次数耗尽而未用余角”，无需提高总角预算或改变原 0.5 Nm 余量、90/110 N 力限、1/1.2 Nm 弯矩规则、4/8.8 Nm 传动限制和 2 Nm 到位候选。

主代理随后明确了实际启动的最小局部试验：`final_regrasp_remaining360` 从本回合 277529 的最终松手状态冷恢复，`perform_current_regrasp=true`，重新抓握后补 5.249558° 并释放，**不执行默认 continuation 第六段会包含的 90° 空手回转**。因此该试验仅能判定“当前末态重新抓握后，原预算内余角能否使原止挡坐底”，不能冒充默认第六段路径的精确复现或证明次数限制是唯一原因。若再次因余量停住，说明本次抓握/受载状态仍有限制；若用完余角仍未到位，说明本次总指令与实际跟随组合仍不足。默认追加段和完整候选仍由各自实际物理结果判断；局部冷恢复结果不能并入原已封存回合冒充完整重复成功。本独立审查没有启动该试验，也没有读取其未封存真值。

可复用入口与本次产物：

- 原末段补核调用：`PYTHONDONTWRITEBYTECODE=1 /home/noob/WorkPlace/isaacsim/.conda-env/bin/python reproducibility/improvements_20260916/review_completed_assembly.py artifacts/full_validation/load10ms_repeat01/run --part terminal --output artifacts/full_validation/load10ms_repeat01/review/independent_terminal_release.json`，工作目录为改进树；重跑请指定新的外部输出文件，原输出采用独占创建。
- [末段逐帧补核 JSON](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/full_validation/load10ms_repeat01/review/independent_terminal_release.json)。复用现有 postreview 算法，仅读已封存末段。
- [第五段机器人/有限传动补证 JSON](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/full_validation/load10ms_repeat01/review/independent_fifth_stop_robot_evidence.json)。没有读取任何对象/接触真值；机制 gzip 无索引，顺序穿过此前字节，只解析限定区间，不读取 19.09 GB 真值全流。
- [第五段补证脚本](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/full_validation/load10ms_repeat01/review/extract_fifth_stop_robot_evidence.py)，调用方式：`.venv/bin/python SCRIPT RUN NEW_OUTPUT_JSON`（本机实际解释器为 `/home/noob/WorkPlace/kcgtest1/.venv/bin/python`）；该脚本明确绑定本回合第五段文件名与停止拍，不是任意阶段通用框架。
