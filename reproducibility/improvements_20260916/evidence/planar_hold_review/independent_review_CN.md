独立机器人控制复核（2026-09-17 UTC）：**是的，当前两轮第四段的 planar_hold=true，使横向腕力不会直接改变接口坐标系的 XY 参考偏置；横向力仍用于六维接触负载补偿及原力/弯矩保护。没有发现这两个区间另有生效分支恢复了横向力导纳输出。** 本次只读已结束第四控制区间及既有在线腕部视觉元数据，没有读取 repeat02 对象/接触真值、启动物理或改动任何配置/源码/验收规则。

代码事实：`te_local_interface_following.py:453–458` 先由 50 ms 界面腕力计算平面导纳，当前配方会使用 `wanted=C·Fxy`、1 mm 偏置限幅、0.1 s 响应和 2 mm/s 速度限幅；随后 `:463–467` 因 planar_hold=true 无条件把 vxy 设为 0。sidewall_sequence 缺省 false；即使为 true，该分支也只会清零，不会恢复导纳。`:520` 积分 offset，`:523` 的位置目标为 `pivot0 + axes·offset`；`:530` 再加入 20/s 的位置误差反馈。因此当前 20 µm/N 和 0.1 s 参数没有产生接口 XY 参考偏置，1 mm 偏置限位未触及；**2 mm/s 上限仍在 :536–543 限制加入位置反馈后的总接口横向速度**，不能把这些参数统称为完全无作用。

普通旋拧调用使用 `hold_pose=False、force_follow_enabled=True、preserve_stroke_pose=False、stroke_index=0`，全部保存行均为 rotation_turn。能重新设置 offset 的另一段代码 `:495–509` 属于释放/换位保持分支，本段未进入；当前 restore_coaxial_center_on_reindex=true 即使进入相应释放分支也会重置 XY 为 0。两轮 angular_admittance_rad_per_nm_s=0，保存的 angular_follow_offset 也全部为零；没有另一条弯矩导纳在这里改变姿态参考。

横向力并未被丢弃。`:357–363` 用 10 ms 六维信号，经接口到世界坐标变换、载荷重力/力矩原点处理和雅可比转置，生成 arm_load_compensation；普通旋拧中该补偿没有被 acquiring/hold 分支抑制，并由 te_coaxial_nut_interval 送入原 stepper。`:403` 把原 50 ms 腕力矩移到当前编码器虚拟原点，`:480–488` 保留合力/弯矩/扭矩硬保护，外层正常换抓仍在弯矩 ≥1.0 Nm 时停止。手臂仍可因位置回正和负载补偿产生横向运动；planar_hold 固定的是这一条力导纳的横向参考输出。

接口坐标恢复采用每回合 `socket_transport/wrist_socket/camera_and_estimate.json` 中的**在线深度视觉** world_from_receptacle_row_major。源码 te_body_assembly_motion.py:310 将同一矩阵作为 wrist_socket 传至后续各段，LocalInterfaceFollowing 直接使用其旋转部分作为 self.axes。该视觉文件明确 online_object_or_contact_truth_used=false，未使用物理插座真值。两回合 axes 都略偏离世界单位阵；所有下列速度/误差的接口 XY 指标均先作 `axesᵀ·world_vector`，没有用世界 XY 替代。

逐行检查首轮 2162 条、第二轮 938 条记录：offset_socket_m[:2] **全部精确为 [0,0]**；`pivot0+axes·offset` 与 `encoder_pivot+保存误差` 完全相同。以 offset 差分重建轴向前馈，再加 20/s 位置反馈，和保存的 unlimited 线速度一致到 4.3e−22 m/s 以内；按原接口平面 2 mm/s、轴向 3 mm/s 重建总速度限制，和保存 requested 速度一致到 2.2e−18 m/s。由于视觉轴有微小倾斜，纯接口轴向参考在世界 XY 仍有约 0.94/3.09 nm 的总投影变化，不应把“接口偏置为零”写成世界 XY 数值绝对不变。

| 已保存控制记录指标 | 首轮第四段 | 第二轮第四段 |
|---|---:|---:|
| 控制区间 | [259010,261172) | [259322,260260) |
| 最后已发腕指令角 | 71.152244° | 13.417704° |
| 完整区间触发横向截速行数 | 820 / 2162（37.93%） | 11 / 938（1.17%） |
| 首次截速指令角 | 12.963712° | 13.067807° |
| 最大未限横向请求 | 5.653809 mm/s | 3.307753 mm/s |
| 最大受限接口横向请求 | 2 mm/s | 2 mm/s |
| 最小横向请求保留比例 | 0.353744 | 0.604640 |
| 最大线性任务分配残差范数 | 3.8154e−7 m/s | 2.5840e−7 m/s |

两段持续长度不同，完整区间比例不可直接作为因果比较。另核对共同的前 **938** 条、到 13.417704° 为止的已发角序列：两轮指令角逐值相同，首轮其中 **14** 行截速，第二轮 **11** 行；相应最大未限请求为 2.321361 与 3.307753 mm/s。截速既不是第二轮独有，也没有覆盖第二轮大部分弯矩增长阶段。

| 相近已发角的接口量 | 首轮 | 第二轮 |
|---|---:|---:|
| 起始弯矩 | 0.238139 Nm | 0.203400 Nm |
| 10.002948° 弯矩 | 0.678594 Nm | 0.789501 Nm |
| 10.002948° 横向请求（均未截速） | 1.809859 mm/s | 0.654653 mm/s |
| 13.382513° 弯矩 | 0.815438 Nm | 0.987424 Nm |
| 13.382513° 未限→受限横向请求 | 2.296517→2 mm/s | 3.214246→2 mm/s |
| 13.382513° 请求保留比例 | 0.870884 | 0.622230 |

第二轮最后保存行 260259 使用传感器前拍 260258，弯矩为 0.987839 Nm；下一次停止决定 260260 使用新的机器人观测，弯矩达到 **1.008883600671 Nm**，触发原 1.0 Nm 正常换抓线，未再推进新的旋拧指令。首轮对应停止决定 261172 为 1.001390341404 Nm。两者 outer_abort_reason 均为 null，原 1.2 Nm 硬上限未被改动。13.4°比较点不是停止决定本身，也不是实体 Nut 的实际旋转角。

关节速度分配残差的含义是雅可比速度求解对“已限速任务请求”的数值拟合；记录前三分量单位 m/s，后三分量是乘过 0.05 的角速度残差。本报告已按该尺度处理。它很小支持“求解器能够数值表示已限速请求”，但不等于物理执行后的关节跟踪误差小，不能单凭它证明提高横向速度上限会降低弯矩。

解释范围：planar_hold 阻止横向力直接移动横向目标，是两轮共同的控制事实，可以成为“缺少力驱动横向让位”这一待检验机制的依据。它本身不是两轮之间改变的变量；两次既有进度、夹持参考和接触历史不同，不能用本比较证明它造成第二轮更早换抓。第二轮在 10° 时已经有较大弯矩、且尚未截速，所以“只是速度上限太低”也没有被现有记录证明。尚未做改变 planar_hold 或速度上限的单变量物理实验；本次不建议在运行中更改，也不把任何假设说成已验证解决方案。

精确数值与输入路径见 [机器人/视觉复核 JSON](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/control_review/planar_hold_20260917/robot_only_review.json)；[只读复算脚本](/home/noob/WorkPlace/kcgtest1-improvements-20260916/artifacts/control_review/planar_hold_20260917/review_robot_only.py)。原第四段比较文件及生产记录保持不变。
