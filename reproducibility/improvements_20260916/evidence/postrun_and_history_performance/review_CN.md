# 完整回合后的第二轮性能诊断

以下使用第一轮已封存数据，当前第二次完整物理运行仍固定baf6d4f，没有把新候选加入运行中。

| 已检验的局部问题 | 结果 | 当前结论 |
| --- | --- | --- |
| 相对运动统计构造了不需要的接触数据 | 1024帧原读取19.763s、字段读取8.850s；294560帧完整新读取449.242s，所有统计字段与原报告精确相同 | 可准备仅在运动结束后的读取修正；原完整单函数耗时未单列，不外推整场收益 |
| 早段传感历史的重复内存扫描 | 32768条、step0–32767，列表0.112760s、tuple0.026396s；字段编码完全相同 | 这是固定历史堆GC稳态扫描中位数，非整段记录速度 |
| 晚段旋拧/换抓历史的相同问题 | 32768条、step249000–281767，列表0.107628s、tuple0.027487s；字段编码完全相同 | 补足Nut阶段代表性，仍是离线内存扫描测量 |

早段与晚段数据转换分别约0.424s、0.420s，约13微秒/行；摄入、解析、编码和检查区间因此有额外成本。每个模式一个独立进程、六次GC取后四次稳态中位数，没有把它写成重复统计置信区间。全部外层记录字典仍被GC追踪，历史总量仍随运行增长。

独立检查找到：最新腕力、已发关节目标、去皮和近期夹持历史，以及异常恢复分支都会读取这些历史数据。没有发现依赖嵌套数组原地修改的消费者；NumPy读取和两类JSON导出、MessagePack在去皮/抬升/晚段旋拧/卸载四条代表记录上保持一致。静态兼容不是控制器已重新运行的证明。因此数组表示候选尚未加入正式版本，需要匹配的控制/物理短窗口验证后再决定。

相对运动统计的候选不增加控制输入，只在归档已经关闭、完整运动结束标记存在时使用原封存字段读取器。准备补丁保存在对应目录，没有修改当前生产源文件或原始报告。
