# 掌心端面遮挡轮廓修复

smooth_band_full_02已完成初抓抬升保持，但第一张掌心图像的端面圆拟合被拒绝（0.234555mm>原0.110925mm）。原图可见手指遮住局部外缘；其遮挡切边被作为完整圆轮廓参与计算。抓取位置已通过物理前段验收，继续保留平直圆环抓取。

修复仅在原完整轮廓和同平面补全均不通过时启用：依据当前深度图中“比当前拟合端面更靠近相机”的像素识别前景遮挡，排除其相邻切边后，只用实际可见圆弧拟合原CAD半径。保留原RMS门，记录原完整轮廓RMS；必须至少100个可见边缘点、36个10°角度分箱中至少18箱覆盖，以及方向信息矩阵最小特征值≥0.2。没有足够弧段或没有实测前景解释时继续拒绝。轴向转角仍不观测。

同一失败深度帧，排除161个实测遮挡边缘点后保留1074个点，覆盖34/36角度箱；新RMS0.041191mm通过原0.110925mm，方向信息最小特征值0.44495。新位置误差经结束后真值独立评价为8.54微米、轴向角误差0.000627°；真值没有作为估计输入。按现有同步G2/掌心的宽ROI规则重放同一帧也通过。

24张既有成功观测的位姿矩阵保持不变；完整圆、前景遮挡、可见弧不足拒绝、错误半径不放行等单测与高位G1相关检查均过。成功帧继续走原算法。额外保留measurement_input.json，使后续即使估计失败也保留当帧标定、编码器和搜索预测，均不含对象真值。

代码修复与图像重放不代表完整装配成功，须继续新完整回合。物理模型/相机布局/抓取高度/夹持目标/停止门均不改。
