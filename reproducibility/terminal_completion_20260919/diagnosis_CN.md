# 最终释放状态的传感判定修正

本次完整物理回合：artifacts/initial_contact_fix_20260919/balanced_band_full_01/run；实际执行源码686734f4d4324993e1fbd2024e95caf2ed437ec6；计算10298.020652秒，退出2。全部动作及最终3秒完全松手已经实际执行。旧控制器在线完成标记为false，原记录保持原样。

旧判定比较松手前与松手后的两个掌心位姿，要求二者深度差≤5微米。原始数据表明：松手前实际深度14.597428292mm，松手后14.604789466mm；卸力使连接器继续落座7.361174微米。视觉对应14.597055077与14.604327098mm，测量吻合实际运动。问题是把两种载荷状态之间的落座位移，误当成已经完全松手之后的不稳定。

原3秒物理检查通过：2880个连续物理步，手部原始冲量逐帧严格为0、金属止挡逐帧受载、相同128夹片逐帧受载、Body/Nut均清醒；深度全程14.604789466mm。在这同一个松手窗口内，当时实际已在线采集并消费15个掌心观测，视觉深度全范围只有0.259338微米。

修复仅在所有动作结束后的判断中使用该已消费观测序列，不新增物理步、相机采集或运动指令。稳定性仍要求所有选中深度的极差≤原5微米；名义深度及原20微米视觉深度门、原横向/轴线门、载荷证据、真实3秒张手及所有独立几何/接触验收保留。按既有0.2秒请求周期、单帧处理延迟及0.5秒观测最大年龄核对完整窗口覆盖，检查所有帧，不挑选最有利的两帧；夹持及开手阶段的观测不混入释放稳定性计算。

同回合重放使用原PALM_MEASUREMENT_CONSUMED事件、原相机观测JSON、已记录腕部视觉插座坐标、机器人步数与载荷，只接受在原判断时刻前已消费的帧；不把仿真对象或接触真值送入判定。原始真值仅用于上述独立故障诊断及物理验收。旧成功基线socket_plus20_full_01的14帧也通过，极差0.427170微米。11项边界检查通过，覆盖真实释放中间的6微米漂移、缺帧、合法处理延迟、重复帧、未来观测、短保持、未到深度和横向失准。

验证边界：本次末端判定修正采用同回合真实传感输入重放，未声称新判定代码又执行了一整轮动力学。实际完整装配和原始视频来自上述686734f回合。修复点位于four_camera_post_entry末尾，动作调用均已返回，随后four_camera_body_transport只暂停并保存。新旧判断不会改变这段已执行的物理轨迹。原控制器false记录及原版whole审核另存保留；修正审核明确注明传感判定重放，复用同回合已实际完成的几何/接触审查，不改其任何数值或门限。

源码：src/kcg_connector/isaac/four_camera_online_completion.py、four_camera_post_entry.py。独立重放：evaluate_terminal_completion_sensors.py。整体验收中的终态判定复核：evaluate_visual_assembly_v1.py。无需重复已经完成且没有变化的动力学来测试这个动作后的纯判断函数。
